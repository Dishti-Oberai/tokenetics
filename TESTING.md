# Testing

Tokenetics has two distinct testing layers, and they're deliberately kept separate — see [CLAUDE.md](CLAUDE.md)'s "Incremental runnability" section for why:

1. **Automated tests** (pytest + ruff + mypy) — free, fast, run on every push/PR via CI. Never hits the network.
2. **Manual dev-loop testing** (`scripts/dev_demo.py`) — hits the real Anthropic API, costs real money, run by hand when you want to eyeball actual behavior.

## Prerequisites

```bash
uv sync
```

## 1. Automated tests

```bash
uv run pytest          # run the test suite
uv run pytest -v       # verbose — see each test by name
uv run ruff check .    # lint
uv run mypy src        # type check (strict mode)
```

All three are required to pass CI (`.github/workflows/ci.yml`). Run all three locally before pushing:

```bash
uv run ruff check . && uv run mypy src && uv run pytest
```

### Current coverage (Phases 0-11)

| File | What it checks |
|---|---|
| `tests/test_import.py` | Package imports and `__version__` is set |
| `tests/test_request.py` | `from_api_kwargs()` / `to_api_kwargs()` round-trip: known fields survive unchanged, unmodeled kwargs (e.g. `temperature`, `thinking`) pass through via `extra` instead of being dropped, unset optional fields are omitted rather than sent empty |
| `tests/test_orchestrator.py` | The plugin contract round-trips through `Tokenetics.prepare()` unchanged with a no-op stage; a disabled stage calls `degraded_fallback()` instead of `run()`; a custom logger can be injected; the default pipeline runs the real foundation stages in the correct fixed order; default stage instances are never shared/mutated across separate `Tokenetics()` calls |
| `tests/test_tokenizer.py` | `count_tokens()` returns a real count and forwards only token-relevant fields (never `max_tokens`/`temperature`); `count_text_tokens()` correctly wraps plain text for response-side stages |
| `tests/test_logger.py` | `InMemoryCostLogger` records entries in order with all fields intact; `NullLogger` genuinely does nothing; **`FileCostLogger`** (Phase 11) appends one JSON line per `log_stage()` call with a stable `run_id` shared across calls from the same instance, and appends to (never overwrites) an existing file across separate instances; **`log_event()`** (Phase 11, second pass) on all three logger types — `FileCostLogger` tags records `"type": "event"` and shares the same `run_id` as `log_stage()` calls from the same instance, `InMemoryCostLogger` stores them in a separate `events` list, `NullLogger` no-ops; `log_stage()` records are now explicitly tagged `"type": "stage"` |
| `tests/test_fail_open.py` | **The deliberately-broken-plugin test**, for both the request-side and response-side paths: a stage that raises is caught, the request/text passes through unmodified, an error-level log line is actually emitted, the cost logger records the failure, and the pipeline continues to the next stage afterward |
| `tests/test_dedup.py` | Exact duplicates removed, first occurrence kept, same text under a different role is *not* treated as a duplicate, drops are logged |
| `tests/test_near_dup.py` | Shingling and MinHash-similarity helpers directly; identical messages get merged (older dropped); the most-recent message is never the one dropped; clearly dissimilar messages are left alone; tool-output threshold is confirmed looser than the conversation-turn threshold |
| `tests/test_schema_minification.py` | Inert JSON-Schema keys stripped, description whitespace collapsed, functionally important fields (`enum`, `additionalProperties`, `required`) preserved exactly |
| `tests/test_post_hoc_trim.py` | Trailing boilerplate stripped, stacked sign-offs fully removed via the iterate-until-stable loop, no false positives on legitimate content that merely resembles a sign-off phrase mid-answer |
| `tests/test_finalize.py` | `Tokenetics.finalize()` extracts text from a response and runs it through the default response-side pipeline; an empty `response_stages` list still strips trailing whitespace (a real API validity requirement, not a stage's job) but otherwise leaves text unchanged; a cost-logger entry gets recorded; **regression test** for a real crash caught via a live `dev_demo.py` run — a response whose visible text ended in a trailing newline crashed `count_text_tokens()`'s real `count_tokens` call with a 400 (`final assistant content cannot end with trailing whitespace`), fixed by stripping trailing whitespace at text-extraction time, before any response-stage processing |
| `tests/test_task_classifier.py` | Per-category detection (code, extraction, tool-heavy via structural `tool_use`/`tool_result` scan, conversational fallback); an empty message is left `None` ("unclassified") rather than guessed; `confidence` is always set; the stage never mutates `messages`; `task_type`/`confidence` are noted for the cost logger |
| `tests/test_task_classifier_accuracy.py` | **The Phase 3 accuracy gate**: the classifier must clear 85% accuracy against `tests/fixtures/classifier_validation_set.json` (32 hand-labeled examples across all 4 categories + 2 unclassified edge cases) before Phase 4 is allowed to consume its output |
| `tests/test_lexical.py` | The shared `overlap_score`/`latest_user_text` helpers directly: identical text scores 1.0, disjoint text scores 0.0, case-insensitive, empty-string sides score 0.0, `latest_user_text` correctly finds the last user turn and ignores non-text content blocks |
| `tests/test_model_compatibility.py` | Current-generation models resolve to `"adaptive"`; an unknown model and an unconfirmed one (Haiku 4.5) both conservatively resolve to `"unsupported"` rather than a guessed value |
| `tests/test_schema_minification.py` (extended) | Tool-relevance filtering: a tool with zero word overlap against the latest user message is dropped, any real overlap keeps it, no user text to compare against means every tool is kept, drops are noted with their score; **a recently-used tool (a real `tool_use` block within `_TOOL_REUSE_WINDOW_TURNS` genuine follow-ups) is exempt from the drop entirely**, even at zero lexical overlap with the latest turn — regression test for a real quality bug caught via Phase 9's benchmark corpus and confirmed via an actual billed `quality-check` run (`missing=['check_inventory']` with the bug present, `passed=True` after the fix); a never-used irrelevant tool alongside an exempt one is still correctly dropped; **windowed, not permanent** (revised 2026-08-09): exactly at the window boundary stays exempt, one follow-up past it correctly reverts to relevance scoring and gets dropped again, and `tool_result` deliveries across several tool round-trips don't consume the window (only genuine follow-up questions do) |
| `tests/test_structured_output.py` | `tool_choice` is forced only when task_type is `extraction` *and* a registered tool clears the lexical-match bar; no-ops when there are no tools, no match, or the caller already set their own `tool_choice`; match is noted with its score |
| `tests/test_brevity_injector.py` | `extraction` gets the MODERATE instruction appended to (or set as) the system prompt; `code`/`conversational`/unclassified stay OFF; the selected bucket is noted, nothing is noted when OFF |
| `tests/test_adaptive_budget.py` | `max_tokens` widens for a known task type but never narrows below the caller's own value; unclassified requests are left untouched; a caller-supplied `truncation_stats` entry above threshold widens further; **thinking effort is opt-in only (revised 2026-09-04, see below)** — `test_thinking_effort_not_set_by_default_without_opt_in` confirms nothing is set without `config["enable_thinking_effort"]=True`, and `test_notes_skip_reason_when_opt_in_not_enabled` confirms it's logged; every other thinking-effort test (varies by task type, skipped when unclassified, respects a caller-set `thinking` config, fails open on an unsupported model) now explicitly opts in first so each still isolates the specific condition it claims to test |
| `tests/test_context_window.py` | Context-window lookup for known/unknown models (unknown conservatively resolves to the smallest known window); the fallback budget scales by `task_type` and stays a fraction of the window; unclassified `task_type` matches the `conversational` default |
| `tests/test_context_scheduler.py` | Per-turn classification for all 5 types; `decision` always pinned regardless of budget; `error` pinned by default and only unpinned by an explicit **user** resolution signal in a later turn (an assistant claiming resolution does *not* count); caller-supplied vs. estimated-fallback token budget; **DP knapsack verified against brute-force** on small synthetic cases, including a zero-capacity edge case; greedy fallback engages above the size threshold; degraded-mode last-N truncation both called directly and through the orchestrator with the stage disabled; empty history is a no-op; **role-alternation safety net**: adjacent same-role survivors merge losslessly (str and list content), a lone leading non-`user` survivor is dropped, an all-assistant input reduces to empty, an already-valid conversation is left untouched, `run()` fails open (passes the original request through) when the fix would empty the result entirely, and a lossless merge is never mislogged as a dropped lead turn (regression test for a bug caught while verifying the `long_history` demo scenario) |
| `tests/test_orchestrator.py` (extended) | `stage_config` actually threads through `Tokenetics.prepare()` to the named stage (regression test for a real gap: `prepare()` previously always passed an empty `{}` config to every stage, making Phase 4's `adaptive_budget` `truncation_stats` and Phase 5's `context_scheduler` `token_budget` unreachable through the public API); an unconfigured stage still gets `{}` |
| `tests/test_cache_reorder_guard.py` | **The standalone hard-raise test**, required before anything else in this phase per CLAUDE.md: `CacheSafetyError` is raised when the system prompt or tools differ from a caller-supplied `previous_request`, passes silently when byte-identical, is skipped entirely with no `previous_request` supplied; the reorder half is confirmed a no-op given today's already-stable-first shape; **regression test that the orchestrator's generic fail-open framework does NOT swallow `CacheSafetyError`** — it must propagate out of `Tokenetics.prepare()`, unlike every other exception |
| `tests/test_cache_breakpoint_optimizer.py` | No breakpoint with no stable prefix, insufficient history (<2 matching entries), or non-matching history hashes; frequent repeats (gap ≤ 5m) choose the 5m tier, a ~10-minute gap chooses 1h (5m would expire between calls), gaps beyond 1h place no breakpoint; below the model's minimum cacheable prefix length places no breakpoint even with a favorable repeat gap; **anchors to `system` whenever one exists** (covers tools+system together per Anthropic's `tools -> system -> messages` cache hierarchy — anchoring to `tools` only covers the tools array) and only falls back to `tools` when there's no system to fold in. **`test_a_few_fast_gaps_then_one_slow_gap_does_not_pick_the_5m_tier`** (added 2026-09-05) — a **regression test for a real bug caught via a live 8-turn session benchmark**: tier selection used to compare the *average* observed repeat gap against each window, so a few fast gaps followed by one slow one (60s/60s/60s/400s, average 145s -- comfortably under 5m) still incorrectly chose the 5m tier, which then expired and forced an unplanned second write when that slow gap actually recurred. Fixed to compare the *maximum* observed gap instead; this test's non-uniform gaps now correctly land on the 1h tier. All 9 pre-existing tests use uniform gap spacing (average == maximum), so none of them could have caught this — a real illustration of why a dedicated non-uniform case was needed |
| `tests/test_cache_pricing.py` | Known-model base price lookup; unknown model conservatively resolves to the highest known base price, not a cheaper guess; known/unknown-model minimum-cacheable-token lookup, unknown conservatively resolves to the highest known minimum |
| `tests/test_cache_hash.py` | Identical system+tools hash identically; different system, different tools, or different tool *order* (caching is prefix-based on literal bytes) all hash differently |
| `tests/test_request.py` (extended) | `cache_breakpoint` round-trips through `to_api_kwargs`/`from_api_kwargs` for both anchors (system as a block-list, tools with `cache_control` on the last entry), correctly stripping `cache_control` back out of the parsed `ToolSpec`/`system` text; no breakpoint by default |
| `tests/test_delta.py` | `compute_delta`/`apply_delta` round-trip correctness for both content shapes: line-patch (byte-exact) and json-patch (semantically exact, `json.loads` comparison); a no-change input still produces a tiny valid delta; totally dissimilar content returns `None` (caller falls back to full payload) rather than a bloated delta; JSON path-escaping for keys containing `/`; a JSON top-level type change still round-trips if a delta is chosen at all; malformed delta text (missing marker) raises rather than silently misparsing |
| `tests/test_delta_compression.py` | Only `tool_result` blocks matching a caller-supplied `tool_use_id` in `previous_payloads` are targeted; non-matching ids, non-`tool_result` blocks, and plain-string message content are all left untouched; list-of-blocks `tool_result` content (e.g. an image) is skipped, not guessed at; a delta that isn't meaningfully smaller falls back to the full payload; multiple matching blocks in one message all compress independently; **the deliberately-broken-plugin test**, required before this stage counts as done — a malformed content block crashes the stage internally, and `Tokenetics.prepare()`'s fail-open framework catches it and returns the request unmodified rather than propagating |
| `tests/test_tier0_integration.py` | **Phase 8's three deliverables**: end-to-end integration test over a realistic multi-turn tool-using conversation, asserting the logger's *runtime* stage trace matches CLAUDE.md's fixed order exactly (not just the static stage list) and that the output is well-formed enough for the real API (alternating roles, first message `user`, the relevant tool survived); the **stage 4/5 double-prune regression test** — schema minification and the context scheduler each only ever mention their own kind of drop (`dropped_tools` vs. `dropped_turns`) even when both have something to prune in the same request; and a **frozen-API contract test** on `Tokenetics.__init__`/`prepare()`/`finalize()` via `inspect.signature`, so a later phase changing these silently fails a test instead of drifting unnoticed |
| `tests/test_cache_pricing.py` (extended) | Now also covers `output_price_for` (known/unknown-model lookup); the known-model input-price test was corrected from the stale $3.00 to the live-verified $2.00 introductory price for `claude-sonnet-5` (a real bug caught during Phase 9 pricing research, not a test-only change) |
| `tests/test_benchmark.py` | Corpus/quality-check JSON loading from an arbitrary directory, including optional `stage_config`/`required_tool_calls` fields on both `CorpusSample` and `QualityCheckSample` (default to `None` when absent, not silently `{}`/`[]`); `estimate_cost_usd` scales linearly with `completions_per_sample`, zero for an empty sample list; `aggregate_savings_by_category` reports a min-max range per category (including the single-sample degenerate case and empty input); `check_required_elements` is case-insensitive, reports exactly which elements are missing on failure, and (as of Phase 9's second-run fix) supports OR-groups — a `list[str]` entry passes if any one alternate phrasing matches; `check_tool_calls` passes only when the named tool actually appears as a `tool_use` block, reports missing tool names on failure, and an empty requirement always passes; **`response_text_with_tool_inputs` combines text and JSON-serialized `tool_use` input** (regression coverage for a real bug caught via a live `quality-check` run: an extraction-shaped sample's real answer lives in a forced tool call's arguments, not text); tests loading the real `benchmarks/corpus`/`benchmarks/quality_checks` directories (so a malformed or incomplete real file fails a test immediately rather than silently loading wrong at runtime), including asserting all 6 risk-bearing stage concerns are covered, that every `stage_config`-equipped sample actually engages its target mechanism (not just parses), and that the tool-reuse-window-expiry sample behaves correctly |
| `tests/test_tale.py` (Phase 10a, `src/tokenetics/extras/tale.py`) | Offline, via a fake `messages.create`-only client (no network): a clean single-integer reply parses correctly; a reply with extra prose around the number still parses (fail-open should extract what it can); an unparseable reply and a raised API exception both return `None` rather than raising, per the fail-open contract; defaults to a cheaper estimation model (`claude-haiku-4-5`) than the target model; the estimation prompt actually includes the real target model, the conversation content, and the system prompt when supplied |
| `tests/test_adaptive_budget.py` (extended, Phase 10a) | `config["external_budget_estimate"]` replaces the free task-type heuristic as the estimate source when present; works even when `task_type` is unclassified (unlike the heuristic, which needs a known type); still never narrows below the caller's own `max_tokens`; notes `max_tokens_widen_reason="external_estimate"` so the source is distinguishable in the per-stage log |
| `tests/test_compress.py` (Phase 10b, `src/tokenetics/extras/compress.py`, guarded by `pytest.importorskip`) | Run for real against `distilgpt2`, not mocked: compression achieves roughly the requested word-drop ratio; kept words are a provable subsequence of the original (order preserved, not reordered or hallucinated); an extreme ratio still keeps at least one word; empty text, an out-of-range ratio (`0.0`/`1.0`), and an unknown model name all fail open (`success=False`, original text returned unchanged) rather than raising; a `Compressor` instance genuinely reuses its loaded model across two calls (not reloaded per call); **a ratio above `RECOMMENDED_MAX_RATIO` (0.4, empirically set from the real quality-degradation benchmark) is clamped down to it by default, a ratio at/below it is untouched, and `allow_above_recommended_max=True` opts back out of the clamp** |
| `tests/test_semantic_cache.py` (Phase 10c, `src/tokenetics/extras/semantic_cache.py`, guarded by `pytest.importorskip`) | Run for real against `all-MiniLM-L6-v2`, not mocked: empty-cache miss; a genuine paraphrase hits and returns the cached response; **the exact near-miss pair the 0.90 threshold was empirically tuned against still correctly misses** (a related-topic-but-different-question pair — the single test that matters most in this file, since it's the false-positive failure mode this whole extra exists to avoid); a clearly unrelated query misses; the best match among multiple stored entries is returned correctly; `invalidate()` removes matching entries and reports the count; `clear()` empties the cache; TTL expiry removes stale entries on the next lookup; a nonexistent embedding-model name fails open to a clean miss rather than raising; a data-integrity check on `benchmarks/semantic_cache/near_miss_set.json` (no duplicate stored-entry ids, every `expected_hit_id` resolves to a real entry) |
| `tests/test_benchmark.py` (extended, Phase 10, mixed-workload) | `benchmarks/corpus/`'s 4-category assertion loosened from exact equality to a subset check (`mixed_workload` is a legitimate 5th, non-task-type bucket); the corpus directory has a `mixed_workload` bucket with >=2 samples; all `mixed_workload` samples actually engage `near_dup` through the real pipeline (>=2 merges each), not just parse |
| `tests/test_dashboard.py` (Phase 11, `src/tokenetics/dashboard/`) | `load_entries`: empty list for a missing file, skips malformed/blank lines rather than raising. `aggregate`: per-run overall before/after **restricted to request-side (`phase="request"`) stages only** (third pass — see below), per-stage rollup (times seen/enabled, token totals, avg timing) across runs, error counting via `extra.error`, zero-entries and missing-`run_id` edge cases, and explicitly ignores `"type": "event"` records. **`aggregate_events`** (second pass): cache hit rate math, TALE's avg budget/estimation-cost, compress's clamp rate and avg achieved ratio, semantic-cache's real hit rate, an unknown `event_type` is silently ignored rather than raising, `"type": "stage"` records are ignored, and `Tier2Stats.has_any_data` reflects whether any of the four mechanisms have logged anything. `render_html`: empty-state message (with the exact command to generate data), a populated stage table, that the log path is HTML-escaped (a literal `<script>` tag in the path must not survive into the rendered page), and (second pass) the Tier 2 section shows real numbers per-mechanism when data exists and an honest "no data logged" placeholder per-mechanism when it doesn't, without the whole page falsely reporting "no runs logged" when only event data (no stage data) exists. `server`: **real loopback HTTP round-trip tests** against an actually-running `ThreadingHTTPServer` (not mocked) — a normal GET renders the expected content, a second GET after a new log entry is written between requests picks it up (confirming the "re-reads fresh every request, no caching" decoupled-from-request-path requirement actually holds), and (second pass) a GET after a `log_event()` call renders real Tier 2 data while untouched mechanisms still show their placeholder. **Third pass**: `test_aggregate_overall_savings_ignores_response_side_stages` — a **regression test for a real bug caught via a live dashboard run** — reproduces a run pairing a well-optimized request (100->80 tokens) with a long, untouched reply (500->500 tokens, measured on the entirely different response-side axis via `post_hoc_trim`) and asserts the overall summary reflects only the request side (100->80), not a number contaminated by the reply's length. **`test_aggregate_excludes_post_hoc_trim_even_when_untagged`** — a regression test for a SECOND real bug found immediately after: the first fix's backward-compatibility fallback defaulted any untagged entry to `"request"`, which silently undid the fix for any log file written before the `phase` tag existed (including `post_hoc_trim`'s own untagged entries) -- exactly the user's real `costs.jsonl`, which still showed the identical bug after the first fix. Corrected to identify `post_hoc_trim` by stage name when no `phase` tag is present, not a blanket default |

See [ROADMAP.md](ROADMAP.md) for what's coming next.

### Synthetic-traffic benchmark (`scripts/cache_benchmark.py`)

Distinct from both layers above: makes **no network calls at all** (unlike `dev_demo.py`) and needs no API key, but isn't part of the pytest suite either, since CLAUDE.md's build order calls for this specifically as a Phase 6 deliverable — "synthetic-traffic benchmark (the headline number)" — separate from Phase 9's real, measured, held-out-corpus benchmark suite.

```bash
uv run python scripts/cache_benchmark.py
uv run python scripts/cache_benchmark.py --calls 5000 --seed 7
```

Simulates a stream of calls across three traffic profiles (a hot chat-agent config called every ~30s, a warm batch job every ~7min, a cold one-off every ~80min), each with a system prompt sized above the real per-model minimum cacheable prefix length (see the `MIN_CACHEABLE_TOKENS` note below), using the real `CacheBreakpointOptimizerStage` and `core/cache_pricing.py` to decide breakpoints, with a simulated clock standing in for Anthropic's actual cache expiry (nothing local can reproduce that). Reports cache hit rate and estimated cost vs. two baselines (never caching, and a naive always-place-a-5m-breakpoint habit). Consistent across seeds/scales: roughly **28-31% cheaper than never caching, ~10-11% cheaper than the naive habit, ~80% cache hit rate**.

### Phase 9 benchmark suite (`scripts/benchmark_runner.py`)

The real thing `cache_benchmark.py` and `dev_demo.py` are both explicitly *not* substitutes for — real, measured numbers against a real (if still starter-sized) corpus. Two subcommands with very different cost profiles:

```bash
uv run python scripts/benchmark_runner.py corpus                              # free
uv run python scripts/benchmark_runner.py quality-check --dry-run             # free, cost estimate only
uv run python scripts/benchmark_runner.py quality-check --confirm-spend       # real, billed
```

`corpus` runs every sample in `benchmarks/corpus/` through `prepare()` only (free — same cost model as `dev_demo.py --all-scenarios`) and reports token savings as a **min-max range per task-type category**, never a single flat percentage, per CLAUDE.md's honest-benchmarking discipline. `quality-check` runs every sample in `benchmarks/quality_checks/` as a real completion, once with its named stage enabled and once disabled, and checks the response against that sample's `required_elements` — the authoritative pass/fail gate (an optional LLM-judge score is a secondary signal only, never sole authority, per the 2026-08-02 resolution in ROADMAP.md). Since this is the only part of Phase 9 that spends real money, it's gated: an upfront cost estimate always prints first; the runner refuses to proceed past `--cost-ceiling` (default $1.00) even with `--confirm-spend`, and refuses to run at all without `--confirm-spend` regardless of estimate size. Both refusal paths and `--dry-run` are verified to make zero network calls.

**Grown 2026-08-02, revised 2026-08-09**: `benchmarks/corpus/` now has 63 samples (15 each in `code`/`conversational`/`extraction`, 18 in `tool-heavy`), clearing CLAUDE.md's 15-20+ per category floor (the low end, not padded further for its own sake). `benchmarks/quality_checks/` covers all 5 risk-bearing concerns CLAUDE.md names (brevity, cap tuning, thinking-effort tuning, context-scheduler pruning, delta-compression interpretation) PLUS a 6th, `schema_minification`, added after a real `corpus` run's per-stage trace surfaced a genuine risk: the tool-relevance filter dropping an already-used tool based only on the latest turn's lexical content (found via `tool_heavy_003`/`tool_heavy_014` hitting ~80% "savings" — traced, not guessed, using the trace feature below). That sample needed a real mechanism extension: `check_tool_calls`/`required_tool_calls` in `core/benchmark.py`, since the correct response there is a `tool_use` call with no text to check. The `schema_minification` fix was then confirmed against the real API (pre- and post-fix), and re-running `corpus` confirmed `tool-heavy`'s range dropped from the false `0.0%-80.1%` to a flat `0.0%-0.0%` — which surfaced a second finding: `cache_breakpoint_optimizer`/`delta_compression` couldn't fire on any tool-heavy sample at all, since none supplied `stage_config`. Extended `CorpusSample` with the same optional `stage_config` field, added `tool_heavy_016` (delta compression) and `tool_heavy_017` (cache breakpoint optimizer — deliberately documented as never moving this report's percentage, since caching changes price-per-token, not token count). A third real `corpus` run confirmed both: `tool_heavy_016` measured 1064→569 tokens (46.5% real savings); `tool_heavy_017` measured 1807→1807 (0.0%, `breakpoint_placed=True`) — the correct real-API result, not the stub artifact seen offline. The fix itself was then revised from a permanent to a windowed exemption (3 genuine follow-up turns) to bound its long-conversation token cost; `tool_heavy_018` was added to demonstrate the window actually expiring in a real sample (5 unrelated follow-ups past the window) and confirmed against the real API: 775→350 tokens (54.8% real savings, `get_weather` correctly dropped again). `tool-heavy`'s range is now `0.0%-54.8%` (n=18 at the time). Added `--sample`/`--category` filters to `corpus` so a single sample can be checked without a full run.

**Grown to the top of the range 2026-08-09**: `benchmarks/corpus/` now has 80 samples (20 per category, up from 15/18) and `benchmarks/quality_checks/` has 11 samples (2-3 per stage, up from 1-2), closing the "one probe isn't enough for real confidence" gap CLAUDE.md's held-out-set discipline implies. New quality-check coverage: `brevity_injector` gets a harder 6-field extraction test; `context_scheduler` gets the OTHER pinned-turn type (an unresolved error, not just a decision); `delta_compression` gets the OTHER content-format branch (a text/line-patch config-file diff, not just JSON); `schema_minification` gets a negative control (a never-used irrelevant tool still drops cleanly) and a real-completion test placed exactly at the reuse-window boundary. All 11 quality-check samples and all `stage_config`-equipped corpus samples are offline-verified to actually engage their target mechanism, not just parse (`test_real_quality_check_samples_actually_engage_their_mechanism`). Full quality-check run now estimated at ~$0.04.

**Both real runs happened same day, and the quality-check run caught a genuine harness bug**: `quality_brevity_001`/`002` failed identically with and without `brevity_injector` — impossible if the stage caused it. Root cause: both are extraction-shaped, so `structured_output` forces `tool_choice`, putting the real answer in the `tool_use` block's `input`, not text — `check_required_elements` only ever checked text. Fixed with `response_text_with_tool_inputs()` in `core/benchmark.py` (combines text + JSON-serialized tool_use `input`), wired into the runner's `_grade()`, 4 new unit tests. Also added response-text printing on any failure, since `quality_adaptive_budget_001` failing identically both ways with no visible response text made it impossible to tell a real miss from a rubric-wording mismatch. Clean confirmations: `quality_adaptive_budget_002` (max_tokens truncation) showed the exact intended effect (`with: passed=True`, `without: passed=False, missing=['split']`); `context_scheduler`, `delta_compression`, and `schema_minification` all passed both ways on every sample. See ROADMAP.md's Phase 9 section for the one remaining step — re-running `quality-check --confirm-spend` now that the grading bug is fixed.

**Third subcommand added 2026-08-12, after Phase 11: `usage`** — a real, full-$-cost benchmark over the same corpus, built after `dev_demo.py --all-scenarios --measure-usage` surfaced a real finding (output-token cost, priced ~5x higher than input for Sonnet 5, outweighing input-side savings on that specific batch) that couldn't be trusted as *the* answer since `dev_demo.py`'s ~11 scenarios are stage-demonstration fixtures, not a representative sample. `usage` reuses the already-vetted 82+3-sample corpus instead, runs baseline vs. pipeline-enabled real completions, and combines input + output + cache cost (each at its own real per-token price from `core.cache_pricing`) into one true dollar figure per call — the piece nothing in the project computed before (`dev_demo.py`'s own cost verdict only ever priced the cache portion in isolation). Reports $ savings as a min-max range per category, never a flat percentage; an overall total explicitly states its own weighting assumption rather than implying it's representative of real traffic; raw token deltas are always printed alongside the $ verdict so the "token view and dollar view can disagree" finding stays visible. Cost-ceiling-gated identically to `quality-check`. `_full_cost_usd()`'s arithmetic hand-verified against 3 manually-computed cases before spending real money. Dry-run: $0.31 for the full 83-sample corpus. **A real run then caught a genuine truncation confound** (the corpus's `max_tokens` values were deliberately tiny for a different Phase 9 purpose, so every completion in both conditions hit `stop_reason="max_tokens"` — confirmed via a small targeted diagnostic before writing any fix): corrected with `_REALISTIC_MAX_TOKENS_FLOOR=2048` applied identically to both baseline and pipeline calls, without touching the corpus itself. Post-fix real run: net -2.1%, plausible and no longer showing the earlier suspicious identical-percentage clustering. No direct pytest coverage on the script itself, consistent with this project's convention for `scripts/*.py` (validated via `--dry-run`/smoke-testing; the pricing logic it calls already has full coverage from Phase 6/9).

**Fourth subcommand added 2026-09-05: `session`** — built because `usage`'s own honest limitation (83 *isolated* one-shot samples) is structurally blind to the two things Tokenetics is actually designed around: caching amortizing past a single write, and redundancy accumulating across a real conversation for `dedup`/`near_dup`/`context_scheduler` to prune. `session` runs one realistic 8-turn conversation (`benchmarks/session/realistic_session.json`) as two parallel real threads turn by turn — `baseline` (raw, never cached) and `pipeline` (`prepare()`/`finalize()` every turn, with a real, incrementally-grown `cache_usage_history` fed forward exactly as a real caller would track their own repeat pattern). **Caught and fixed a real bug before spending any money**, via a zero-cost offline structural dry-run (stub client, fake replies): the session originally registered one never-used tool, but `schema_minification` (which runs earlier in the fixed pipeline than the cache stage) let it survive on exactly one turn due to a coincidental keyword match ("weather's nice"), which perturbed the stable prefix the cache stage sees and broke the repeat-pattern match against this file's own precomputed hash — confirmed directly (breakpoint fired on only 1 of 8 turns) rather than assumed. Fixed by removing the tool entirely (not this benchmark's concern). Re-verified offline: breakpoint now fires consistently from turn 3 onward, `near_dup` catches the accumulated duplicate padding, `context_scheduler` prunes once there's real content to prune. Same cost-ceiling gating; dry-run: $0.1556 for 8 turns × 2 threads.

## 2. Manual dev-loop testing (`scripts/dev_demo.py`)

This script is **not** part of pytest or CI — it makes a real, billed call to the Anthropic API. Use it to sanity-check real behavior by hand, not as a correctness gate.

**Requirements:**
- `ANTHROPIC_API_KEY` set in your own shell environment (never paste a key into a chat or commit it to a file — see the security note below)
- A funded Anthropic account (check [console.anthropic.com/settings/billing](https://console.anthropic.com/settings/billing) if you get a "credit balance too low" error)

**Run it:**

```bash
export ANTHROPIC_API_KEY=sk-ant-...
uv run python scripts/dev_demo.py --list-scenarios   # see what's available, free, no calls
uv run python scripts/dev_demo.py --all-scenarios    # exercise every stage + corner case, free
uv run python scripts/dev_demo.py --scenario extraction   # one real completion, billed once
```

**As of Phase 4, the script is scenario-based**, not a single fixed sample — one conversation can't exercise eight stages meaningfully (`structured_output`/`brevity_injector` only ever fire on `extraction`-shaped requests, `near_dup` needs genuinely similar-not-identical text, tool-relevance filtering needs an actually-irrelevant tool registered, etc.). `SCENARIOS` in the script is a small library, each one built to make a specific stage or corner case visibly do something:

| Scenario | Exercises |
|---|---|
| `dedup` (default) | Exact-repeat turns → `dedup` drops them |
| `near_dup` | A Q&A pair paraphrased (not identical) elsewhere in the conversation → `near_dup` merges both turns via MinHash similarity, `dedup` correctly leaves it alone |
| `extraction` | An extraction-shaped question + a matching tool → `structured_output` forces `tool_choice`, `brevity_injector` injects the MODERATE instruction, `adaptive_budget` picks low thinking effort |
| `code` | A code question with a deliberately small `max_tokens` cap → `task_classifier` says `code`, `adaptive_budget` widens the cap and picks high thinking effort |
| `tool_heavy` | A conversation already containing a real `tool_use`/`tool_result` exchange → `task_classifier` detects `tool-heavy` structurally, not from text |
| `irrelevant_tool` | One relevant tool + one clearly unrelated tool registered → `schema_minification`'s relevance filter drops only the irrelevant one |
| `caller_sets_thinking` (corner case) | A code-shaped request where the caller already set their own `thinking` config → `adaptive_budget` respects it and doesn't override, even though `code` would normally get `effort: "high"` |
| `long_history` (corner case) | A long conversation with a mix of turn types under a tight `token_budget` (set via `stage_config`) → `context_scheduler` pins the decision and the still-unresolved error, drops low-value padding to fit, and correctly does *not* pin the earlier error that's explicitly resolved by the user. Under this scenario's tight budget, the two same-role pinned survivors get losslessly merged into one message by the role-alternation safety net — see below |
| `cache_hit` | A tool-using request with `cache_usage_history` (via `stage_config`) showing frequent ~30s-gap repeats of the same system+tools → `cache_breakpoint_optimizer` places a 5m breakpoint (cheapest tier that stays continuously warm at this rate) |
| `cache_guard_violation` | A request whose system prompt differs from a caller-supplied `previous_request` → `cache_reorder_guard` hard-raises `CacheSafetyError` — the one stage in the pipeline that doesn't fail open. Caught and printed by the script rather than crashing `--all-scenarios`; always `prepare_only` |
| `delta_hit` | A tool_result whose JSON content differs from a caller-supplied `previous_payloads` entry (via `stage_config`) by one field out of many → `delta_compression` replaces it with a small JSON-patch delta instead of the full payload. Offline-verified 1035 → 113 chars; demonstrates the mechanism, not a claim about reply quality — see the stage's deferred quality-interpretation note |

There's deliberately no `unclassified` scenario here: `task_classifier` only ever returns `task_type=None` for empty/whitespace-only text, and `Tokenetics.prepare()` calls the real `count_tokens` endpoint before any stage runs — which rejects that content outright ("text content blocks must contain non-whitespace text"), free or not. No string can satisfy the API's validation and still trigger the unclassified path, so this corner case can't be demonstrated by running anything through the real API at all. It's covered directly instead, bypassing `count_tokens`/the API entirely, by `tests/test_task_classifier.py::test_empty_message_is_unclassified_not_guessed`.

**Role-alternation safety net (resolved 2026-07-26)**: `context_scheduler` drops individual turns independently based on score, unlike `near_dup`'s pair-aware dropping, so nothing inherently guaranteed the surviving subset stayed role-alternating for the real API. Fixed with `_fix_alternation`, applied as the last step of both `run()` and `degraded_fallback()` — see `src/tokenetics/stages/context_scheduler.py`'s module docstring and `tests/test_context_scheduler.py`'s role-alternation tests for the mechanism and coverage.

**Stub-only artifact, not a real bug**: in `--all-scenarios`/`--prepare-only` mode, `cache_hit`'s per-stage log shows `cache_breakpoint_optimizer` "costing" tokens when it places a breakpoint. This is `StubAnthropicClient`'s `len(str(system))` approximation reacting to `system` becoming a block-list once `cache_control` is attached — not a real cost. The real Anthropic API only counts actual text content, not JSON/cache_control structure; against the real API this stage should show ~0 request-token change.

Most of these stages only touch the *request* — `tk.prepare()` only calls the free `count_tokens` endpoint, never the billed `messages.create()`. `--all-scenarios` uses exactly this to check all of it for $0; only `--scenario NAME` without `--prepare-only` spends money, and only once per run.

For any single scenario, the per-stage log shows each stage's own before/after tokens, timing, and any noted metadata (`dropped_duplicates`, `merges`, `task_type`/`confidence`, `dropped_tools`, `tool_choice_forced`, `brevity_bucket`, `max_tokens_widened_to`, `thinking_effort`, `thinking_effort_skipped`, ...). A stage showing no token change and no notes is a correct no-op for that particular scenario, not a bug — check the table above for which scenario actually exercises which stage.

**On/off comparison:** `--disable STAGE_NAME` (e.g. `--disable dedup`) now does something real — compare `request tokens before -> after` with and without a given stage to see its individual contribution, per [CLAUDE.md](CLAUDE.md)'s "Incremental runnability" section.

**Cost-conscious flags** (see the script's own docstring for more): as of Phase 4, `adaptive_budget` sets a bounded thinking effort automatically for classified requests on supported models, so the script no longer needs to force-disable thinking itself. Pass `--show-thinking` to force `display="summarized"` and inspect the reasoning text yourself (this pre-empts `adaptive_budget`'s own choice, since it always respects a caller-set `thinking` config), or `--model claude-haiku-4-5` for the cheapest possible smoke test.

### Security note

Never paste a real API key into a chat, issue tracker, or commit — treat any key that's been pasted somewhere as compromised and rotate it in the console immediately. Keep keys in your own shell environment or a local `.env` file (already excluded via `.gitignore`).

## Testing conventions for new work

These apply once pipeline stages start landing (Phase 2 onward) — full detail in [CLAUDE.md](CLAUDE.md):

- **Every new stage needs its own test file** before it's considered done.
- **Every fail-open path needs a deliberately-broken test case** proving the request goes out unmodified (or degraded-but-safe), not raising or corrupting the request.
- **Stage 9c (adaptive generation budgets)** is highest test priority: stress-test the `max_tokens` truncation-monitoring against real responses, and validate the thinking-effort knob against the benchmark suite's held-out quality-check set (it has no truncation signal to monitor).
- **Regression test required:** stages 4 (tool-relevance filtering) and 5 (context scheduler) must never double-prune the same content.
- **The cache-safety guard (stage 7)** needs a standalone hard-raise test before reorder or the breakpoint optimizer are built on top of it — it's the one deliberate non-fail-open behavior in the whole project.
- **Type hints everywhere** — `mypy --strict` must stay clean as new modules are added.

## Testing by phase

Concrete testing approach for each build-order phase — both the automated (pytest, free) and manual (`dev_demo.py`/real API, costs money) side. Phases are cumulative: later phases' automated suites still run everything from earlier phases.

| Phase | What's being verified | Automated approach | Manual (`dev_demo.py`) approach |
|---|---|---|---|
| **0** ✅ done | Request type, plugin contract, orchestrator skeleton | Round-trip equality on `from/to_api_kwargs`; no-op stage preserves request; disabled stage calls `degraded_fallback()` not `run()` | Pass-through baseline: identical before/after size, empty stage list, real reply returned |
| **1** | Cost logger, fail-open framework, shared tokenizer | Logger records correct tokens-before/after, timing, on/off per stage; every logged number tagged `measured`/`estimated` correctly; **the deliberately-broken-plugin test** — a stage that raises must be caught, skipped, request passed through unmodified, logged at error level (this must pass before any real stage is built) | Extend the script to print the logger's per-stage summary after a real call |
| **2** | Dedup, near-dup (MinHash), schema minification, post-hoc trim — each independent | Dedup: exact duplicates removed, first occurrence kept, near-duplicates untouched. Near-dup: dual thresholds (~85-90% tool output, ~95%+ conversation turns) hit correctly, most-recent-turn exemption holds, every merge logs its similarity score. Schema minification: stripped schema still validates real tool calls. Post-hoc trim: known boilerplate stripped, no false positives on legitimate content | Send a prompt with deliberately repeated/near-duplicate content, verify size drops and the repeat is gone from `to_api_kwargs()` output; check a boilerplate-heavy reply gets trimmed before it would be re-stored |
| **3** | Task classifier + accuracy gate | Accuracy against the 30–50 hand-labeled examples must clear the gate threshold before Phase 4 starts; per-category precision/recall (code, conversational, extraction, tool-heavy) | Run varied real prompts through it, print `(task_type, confidence)` and eyeball whether it matches intuition |
| **4** ✅ done (structural) | Tool-relevance filtering, structured-output enforcement, brevity injector, adaptive generation budgets (max_tokens + thinking effort level) | **Done**: tool filtering keeps anything ambiguous, drops only zero-overlap tools, logs drops with score. Structured output only fires on `extraction` + a matching registered tool, respects a caller-set `tool_choice`. Brevity: default OFF except `extraction` → MODERATE, overhead counted honestly via the normal token measurement. max_tokens: widen-only, never narrows below the caller's value, conservative on unclassified. Thinking effort: correct per-task-type routing, conservative default (unclassified/caller-set `thinking` → untouched), fail-open tested against a model the compatibility table marks unsupported. **Deferred to Phase 9** (needs the benchmark suite, doesn't exist yet): the **max_tokens real-response stress test** (truncation rate over many live completions, confirm auto-widen triggers above ~1-2%) and **thinking-effort validation against the held-out quality-check set** (with vs. without effort tuning, per task type) — Phase 4 shipped the structural/routing tests only, not this rigor bar, and that's flagged in ROADMAP.md as owed, not skipped. `display: "omitted"`'s multi-turn-continuation behavior also not yet tested. **Revised 2026-09-04**: that owed validation finally happened, for real, via `benchmark_runner.py usage` — and it found thinking effort's automatic-by-task-type default was making `code`/`conversational` requests 2-3x more expensive with no caller signal requesting that trade (see ROADMAP.md's "Full real-dollar usage benchmark" section for the full finding). Fixed by requiring an explicit `config["enable_thinking_effort"]=True` opt-in — the mechanism itself is unchanged and still works exactly as originally tested, it just no longer runs unless asked for | Run an extraction-shaped prompt with a matching tool registered, confirm `tool_choice`/system-prompt/`max_tokens` show the expected values in the per-stage log and `prepare()` output (thinking effort now needs `stage_config={"adaptive_budget": {"enable_thinking_effort": True}}` to see it fire); run a multi-turn exchange with the default (now-omitted-by-default) thinking display to confirm the second call's input tokens don't include the first call's reasoning trace |
| **5** ✅ done | Context scheduler (DP knapsack) | **Done**: per-turn classification for all 5 types; `decision` always pinned; `error` pinned unless a later *user* turn (not assistant, not "moved on") gives an explicit resolution signal; DP solver verified against brute-force on small synthetic cases + a zero-capacity edge case; greedy fallback triggers above an empirically-justified size threshold (documented timing data in-code) and logs it; degraded mode (last-N truncation) engages and logs when the stage is disabled, both called directly and through the orchestrator; budget source (caller-supplied vs. estimated context-window-derived fallback) tested; role-alternation safety net tested (merge, lone-leading-turn drop, fail-open-on-empty, and the merge-vs-drop logging distinction). **Deferred to Phase 9**: a rigorous latency benchmark across real request shapes (Phase 5 shipped an informal, empirically-grounded threshold justification, not the full benchmark-suite rigor) | Run the `long_history` scenario in `dev_demo.py`, confirm pinned/dropped counts, budget source, and the merged/valid message sequence in the per-stage log and `prepare()` output |
| **6** ✅ done | Caching: safety guard → reorder → pricing config → breakpoint optimizer | **Done**: safety guard standalone hard-raise test (system/tools change → `CacheSafetyError`, byte-identical → passes silently, no `previous_request` → skipped) plus the critical regression test that the orchestrator's fail-open framework does NOT swallow it. Reorder confirmed a no-op given today's stable-first shape. Pricing table sourced live (2026-07-27, minimum-cacheable-size addition re-verified 2026-08-02), not guessed. Breakpoint optimizer: TTL-tier choice verified against synthetic repeat patterns, including the corrected "cheapest tier that stays warm" model after catching a real bug in the first (window-total) formula. **Two more real bugs caught via `--measure-usage` against the real API (2026-08-02)**, both from the same `cache_hit` scenario showing `usage.cache_creation_input_tokens`/`cache_read_input_tokens` stuck at 0: (1) below each model's minimum cacheable prefix length (1024 tokens for `claude-sonnet-5`) the API silently never caches at all — fixed via `MIN_CACHEABLE_TOKENS`; (2) even after resizing past that minimum, still 0/0 — root cause was the anchor logic itself: Anthropic's cache hierarchy is `tools -> system -> messages`, so a breakpoint on `tools` caches ONLY the tools array, never `system` after it, which the stage had backwards (anchoring to `tools` whenever present, on the false assumption it also covered `system`). Fixed by anchoring to `system` whenever one exists. Both bugs and the live-doc confirmation are documented in `cache_breakpoint_optimizer.py`'s docstring. **Synthetic-traffic benchmark done** (`scripts/cache_benchmark.py`, no network, profiles resized above the real minimum) — ~28-31% cheaper than never caching, ~10-11% cheaper than a naive always-5m habit, ~80% hit rate, consistent across seeds (benchmark numbers unaffected by the anchor bug since its cost model was already anchor-independent). **Real end-to-end write-then-read confirmed against the live API (2026-08-02)** via `--all-scenarios --measure-usage`: `cache_hit`'s pipeline-enabled call showed a real `cache_creation_input_tokens=1724` write, and the immediate repeat call showed `cache_read_input_tokens=1724` on the same 1,724 tokens — the full cycle both fixes were meant to enable, measured, not just unit-tested. **A fourth stale-data bug caught later, during Phase 9 pricing research (2026-08-02)**: `BASE_INPUT_PRICE_PER_MTOK["claude-sonnet-5"]` was hardcoded to $3.00; the live-verified current price is $2.00 (introductory through 2026-08-31, stepping up 2026-09-01) — fixed, plus added `OUTPUT_PRICE_PER_MTOK` (previously missing entirely) sourced from the same live fetch. **A fifth bug caught much later still, via a real 8-turn multi-turn session benchmark (2026-09-05)**: TTL tier selection compared the *average* observed repeat gap against each tier's window, with no safety margin — a few fast gaps followed by one real gap over 5 minutes (ordinary generation/network latency between real calls) still averaged out to "comfortably under 5m," so the stage picked the 5m tier, which then genuinely expired and forced an unplanned second write once that slow gap recurred. Fixed to compare the *maximum* observed gap instead of the average — see `cache_breakpoint_optimizer.py`'s docstring for the full reasoning | Run `--scenario cache_hit`, confirm `cache_control` lands on `system` (not `tools`) in `prepare()` output; run `--all-scenarios --measure-usage` to see the write/read cycle in `cache_hit`'s output; run `--scenario cache_guard_violation` to see the hard-raise fire; run `scripts/benchmark_runner.py session --confirm-spend` to see the tier decision under real, non-uniform call cadence |
| **7** ✅ done | Structured delta compression + `apply_delta()` | **Done**: diff/patch computed correctly for a modified `tool_result` payload (JSON-patch for JSON content, line-patch otherwise); size-threshold fallback sends the full payload when the delta isn't meaningfully smaller; **round-trip tests** — `apply_delta(previous, delta)` reconstructs the original exactly (byte-exact for line-patch, semantically exact for json-patch, deliberately, see the module docstring); **deliberately-broken-plugin fail-open test** for the stage itself. Scoped to `tool_result` blocks keyed by `tool_use_id` (resolved 2026-08-02, see the brief's stage 6 amendment). **Deferred to Phase 9**: whether Claude actually interprets a delta-encoded tool_result correctly is a model-quality question, not a mechanical one — not assumed to just work | Run `--scenario delta_hit`, confirm the tool_result content in `prepare()` output is the small delta-encoded form (starts with `[tokenetics:delta v1]`), not the original full JSON |
| **8** ✅ done | Tier 0 assembled in fixed order, frozen API | **Done**: end-to-end integration test over a realistic multi-turn conversation, asserting the pipeline ran in the fixed order (via the logger's *runtime* stage trace, not just the static list) and produced a well-formed request; **regression test** that stages 4 and 5 never double-prune the same content, verified under a real combined run (both an irrelevant tool and a tight budget in play at once); a contract/signature test on `Tokenetics.__init__`/`prepare()`/`finalize()` so the now-frozen API can't silently drift — `tests/test_tier0_integration.py`. **Tier 0 API frozen 2026-08-02** | Full run against a real multi-turn, tool-using conversation; compare total token reduction against the Phase 0 baseline |
| **9** ✅ done | Benchmark suite — real measured numbers | **Fully closed (2026-08-10)**: runner infrastructure done and unit-tested (`tests/test_benchmark.py`, 28 tests). `benchmarks/corpus/` at 80 samples (20/category, the TOP of CLAUDE.md's 15-20+ floor), run for real 5 times total, cleanly every time except the very first (which found the real `schema_minification` bug this whole investigation traces back to). `benchmarks/quality_checks/` at 11 samples (2-3 per stage), run for real four times post-corpus-growth, each run finding and fixing one real gap: run 1 caught the `response_text_with_tool_inputs` grading gap (structured-output answers live in the `tool_use` block's `input`, not text). Run 2 caught `adaptive_budget_001`'s rubric rigidity (fixed via OR-group support in `check_required_elements`, since `judge_rubric` is confirmed never wired to an actual grading call, leaving exact-substring matching as the sole automated gate) and `adaptive_budget_002`'s classifier-bucket mismatch (fixed by adding a code fence so the sample actually lands in the `code` task type it's meant to test, not `conversational`). Run 3 confirmed both fixes live and caught `quality_context_scheduler_001`'s response-length variance on an open-ended "quick recap" question (fixed by requesting a one-sentence answer). **Run 4: fully clean, 11/11 passed with every stage enabled, 11/11 with every stage disabled** — every sample now has at least two consecutive clean real runs. 221 tests pass, ruff/mypy clean | None — Phase 9 is done. `uv run python scripts/benchmark_runner.py corpus` / `quality-check --confirm-spend` remain available for spot-checks as later phases add stages |
| **10** ✅ done | Tier 2 extras: `compress`, `semantic-cache`, TALE budget estimation, plus the deferred-from-Phase-9 mixed-workload corpus bucket | **Fully closed (2026-08-10)**. **10a (TALE)**: closed, including a real bug caught via a live `--tale` run and fixed (see ROADMAP.md). **10c (semantic-cache)**: closed including at-scale validation — `scripts/semantic_cache_benchmark.py` (free, local) run for real against a 12-entry/30-query held-out near-miss set: **0.0% false-positive rate (0/16)**, confirming the 0.90 threshold is genuinely safe; **64.3% recall (9/14)**, an honest real tradeoff (safe direction to be wrong) deliberately left untuned given the sample size. **10b (compress)**: closed — `scripts/compress_benchmark.py` (cost-ceiling-gated) run for real three times: run 1 caught a real rubric bug (ASCII/umlaut mismatch, same class as Phase 9's wording bugs); run 2 (with response-text-on-failure printing added) diagnosed and fixed it with an OR-group; run 3, at finer-grained ratios `[0.0, 0.4, 0.45, 0.5, 0.55, 0.6]` specifically to locate the cliff (prompted by the user asking whether to cap at 35-40% or find the real limit), showed **100% pass at 0.4, first failure at 0.45, 25% by 0.55-0.6** — root-caused to numeric figures/proper nouns being disproportionately dropped, while conceptual facts survive cleanly. **Result acted on, not just recorded**: added `RECOMMENDED_MAX_RATIO = 0.4`, clamped by default in `Compressor.compress()`/`compress_text()` (info-level log, `CompressResult.clamped`/`requested_ratio` report it), opt-out via `allow_above_recommended_max=True` — same safe-by-default idiom used elsewhere in the project, backed by the exact real benchmark data rather than a guess. 3 new tests. **Mixed-workload bucket**: `benchmarks/corpus/mixed_workload.json` (2 samples) reported as its own explicitly-labeled category by `corpus`, confirmed via the real pipeline to engage `near_dup` — its near-duplicate padding pairs needed iterating against the stage's actual MinHash estimator, since exact Jaccard alone proved a meaningfully looser proxy near the 0.95 boundary. `--compress-ratio`/`--semantic-cache` wired into `dev_demo.py` for hand-checking (not yet run against the real API, but ready). 259 tests total, ruff clean, mypy clean | `uv run python scripts/semantic_cache_benchmark.py` (free); `uv run python scripts/compress_benchmark.py --confirm-spend` (~$0.03) to reconfirm; `uv run python scripts/dev_demo.py --scenario code --tale` / `--compress-ratio 0.3` / `--semantic-cache` for hand-checking |
| **11** ✅ done | Dashboard (swapped ahead of distribution 2026-08-10 — see ROADMAP.md's "Dependencies to respect") | **Done (2026-08-12), including the Tier 2/cache gap closed same day**: `FileCostLogger` persists `CostLogger` entries to JSONL (previously nothing persisted them anywhere); `src/tokenetics/dashboard/` (`aggregate.py`/`render.py`/`server.py`) reads that log and serves a small local web view via stdlib `http.server` only — no new dependency. Decoupling requirement verified directly, not assumed: real loopback HTTP tests against an actually-running server, including one proving it re-reads the log fresh on every request (no caching). First pass shipped stage-level data only and explicitly flagged cache hit rate/Tier 2 cost-benefit as not shown (nothing logged that data anywhere). **Second pass same day**: added `CostLogger.log_event()` (a sibling to `log_stage()` for data that exists outside the pipeline — real cache usage from the caller's own `messages.create()`, TALE's round-trip cost, compress's achieved ratio, semantic-cache's hit/miss), extended `aggregate.py`/`render.py`/`server.py` to surface it (real numbers per-mechanism when logged, an honest "no data logged" placeholder when not — never a fabricated zero), and wired `log_event()` calls into all four `dev_demo.py` Tier 2 call sites. `--log-to` wired into `dev_demo.py`; `scripts/dashboard.py` CLI launcher smoke-tested end-to-end twice (stage-only data, then again with real Tier 2 event data) via a real background process + real `curl`. Second pass added 17 tests (5 in `test_logger.py`, 12 in `test_dashboard.py`). **Third pass, same day, caught via the user hand-running the dashboard against real accumulated data**: a real, confusing `-8.0%` overall "savings" number with otherwise-clean data (no crashed-run artifact) traced to a genuine cross-axis aggregation bug — `post_hoc_trim` (response-side, measures REPLY tokens) was getting mixed into the same before/after calculation as request-side stages (measure REQUEST tokens), so any run calling both `prepare()` and `finalize()` compared request-size-before against reply-length-after, a meaningless comparison that trends negative since replies are often longer than an optimized request. Fixed by tagging every `log_stage()` call with `phase="request"`/`phase="response"` in the orchestrator (lands in the existing `**extra` passthrough, no signature change) and restricting the dashboard's overall-savings calculation to request-side entries only; overall summary cards relabeled "Request tokens before/after/saved" to make the scope explicit going forward. Also fixed in this window (found via the same real hand-check, unrelated root cause): a `finalize()` crash on any reply whose visible text ends in trailing whitespace — the Messages API's real `count_tokens` endpoint rejects assistant content ending in trailing whitespace, and `finalize()`'s own stated contract ("text ready to store/re-inject as future context") was never actually satisfied for such text anyway. Fixed by stripping trailing whitespace at text-extraction time. **The first version of the aggregation fix, though it passed its own test, still failed on the user's real, already-accumulated `costs.jsonl`** when they restarted the dashboard and reported back the identical -8.0% — its missing-`phase` backward-compatibility fallback defaulted every untagged entry (including `post_hoc_trim`'s, since the user's real log predated the fix entirely) to `"request"`, silently undoing the fix for exactly the file it was meant to fix. Corrected same day to identify `post_hoc_trim` by stage name when untagged, not a blanket default; re-confirmed against the real CLI with the exact untagged-log shape the user actually hit. 297 tests total, ruff clean, mypy clean | `uv run python scripts/dev_demo.py --scenario code --log-to costs.jsonl` then `uv run python scripts/dashboard.py --log-file costs.jsonl` and open `http://127.0.0.1:8765/`; add `--measure-usage`/`--tale`/`--compress-ratio`/`--semantic-cache` to any `dev_demo.py --log-to` run to populate the Tier 2 section with real data |
| **12** (in progress) | Distribution: PyPI + MCP server | **MCP server real-client test done (2026-09-05)**: `src/tokenetics/mcp_server.py` (`MCPServer("tokenetics")` from the real `mcp==2.1.1` package, one `optimize_request` tool forwarding to `Tokenetics().prepare()`, malformed input caught and returned as `{"error": ...}` rather than crashing the stdio JSON-RPC stream). A real `mcp.client.stdio` session, run as an actual subprocess, confirmed `list_tools()` finds it and `call_tool()` round-trips — without a live API key in this environment it correctly surfaced Anthropic's real auth error as a clean tool result, not a server crash, confirming the fail-open-at-the-boundary contract. New `tokenetics[mcp]` extra + `tokenetics-mcp` console script. No dedicated pytest file (consistent with this project's `scripts/*.py`-style convention: validated via a real client smoke test, not synthetic unit tests, since the only logic here is a thin pass-through to the already-fully-tested `Tokenetics.prepare()`). MIT `LICENSE` added + PyPI metadata (`classifiers`/`keywords`/`[project.urls]`) filled in. 300 tests total (unchanged), ruff/mypy clean. **Not yet done**: a real run with a live API key to see genuine `optimize_request` output; `uv build` + clean-venv install verification; the actual PyPI publish (needs the user's own credentials) | Install from a built wheel (not the repo checkout) in a clean environment, run `dev_demo.py` against that install to catch packaging bugs; run `uv run tokenetics-mcp` and connect a real MCP client (e.g. Claude Desktop) with a live `ANTHROPIC_API_KEY` set to see real optimized output |
| **13** | Docs | Every published benchmark number traces back to an actual Phase 9 run; cross-links between README/CLAUDE.md/ROADMAP/architecture doc resolve; the "considered and rejected" section reflects real amendment history | Fresh read-through of the quickstart by someone (or a cold Claude session) who hasn't seen the code, to catch doc rot |

See [ROADMAP.md](ROADMAP.md) for the phase-by-phase build order and dates these tests map to.
