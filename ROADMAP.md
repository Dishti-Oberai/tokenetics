# Roadmap

This is the dated, actionable version of the build order in [CLAUDE.md](CLAUDE.md). CLAUDE.md is the source of truth for *how* each phase must be built (fail-open discipline, conservative defaults, testing requirements); [tokenetics-project-brief.md](tokenetics-project-brief.md) is the source of truth for *what* gets built and why. This file is only about sequencing, effort, and dates.

## Immediate next steps (this week)

Phase 0 is partially done already — `pyproject.toml`, pytest, and `src/tokenetics/__init__.py` (with a version string and a passing import test) exist. What's left to finish it:

- [x] Set up CI (run `ruff check`, `mypy`, and `pytest` on every push/PR) — `.github/workflows/ci.yml`
- [x] Define the internal request representation (the type every stage reads/writes — not raw Anthropic API JSON) — `src/tokenetics/core/request.py`
- [x] Define the plugin contract as an ABC/protocol: `plugin(request, config, logger) -> request` — `src/tokenetics/core/plugin.py` (`Stage` ABC), `src/tokenetics/core/logger.py` (`CostLogger` interface stub)
- [x] Build the orchestrator skeleton (runs plugins in the fixed pipeline order, nothing real inside yet) — `src/tokenetics/orchestrator.py` (`Tokenetics`)
- [x] Write a no-op plugin round-trip test (request goes in, comes out unchanged, through the full orchestrator) — `tests/test_orchestrator.py`, `tests/test_request.py`
- [x] Add a runnable dev demo script (e.g. `scripts/dev_demo.py`, outside pytest/CI) that hits the real Anthropic API through the current pipeline and prints token counts before/after, which stages fired, and the reply text — per [CLAUDE.md](CLAUDE.md)'s "Incremental runnability" convention. Keep it wired up as each new stage lands in later phases, not just Phase 0. — `scripts/dev_demo.py`

**Phase 0 complete.** Next up: Phase 1 (cost logger, fail-open framework, tokenizer, deliberately-broken-plugin test).

## Phase 1 (cost logger, fail-open framework, tokenizer)

- [x] Shared tokenizer utility, wrapping the real `count_tokens` API (never a local approximation) — `src/tokenetics/core/tokenizer.py`
- [x] Real cost logger (`InMemoryCostLogger`), recording tokens before/after, timing, on/off, and `measured`/`estimated` per stage — `src/tokenetics/core/logger.py`
- [x] Fail-open framework: every stage call wrapped so a raised exception is caught, the stage skipped, the request passed through unmodified, logged at error level — `src/tokenetics/orchestrator.py`
- [x] The deliberately-broken-plugin test — `tests/test_fail_open.py`
- [x] `scripts/dev_demo.py` updated to print real token counts and the per-stage cost-logger summary

**Phase 1 complete.** Next up: Phase 2 (dedup, near-dup, schema minification, post-hoc trim — the first real pipeline stages).

## Phase 2 (dedup, near-dup, schema minification, post-hoc trim)

- [x] `DedupStage` — exact-match, hash-based, keeps first occurrence — `src/tokenetics/stages/dedup.py`
- [x] `NearDupStage` — hand-rolled MinHash/shingling (no new dependency), dual thresholds, most-recent-turn exemption falls out of the algorithm rather than a special case — `src/tokenetics/stages/near_dup.py`
- [x] `SchemaMinificationStage` — structural-only (inert JSON-Schema keys + whitespace collapse); tool-*relevance* filtering deferred to Phase 4 since it needs the classifier — `src/tokenetics/stages/schema_minification.py`
- [x] `ResponseStage` — a parallel interface for response-side stages, since post-hoc trim operates on reply text, not a `TokeneticsRequest` — `src/tokenetics/core/response_stage.py`
- [x] `PostHocTrimStage` — end-anchored boilerplate stripping, applied iteratively for stacked sign-offs — `src/tokenetics/stages/post_hoc_trim.py`
- [x] `Tokenetics.finalize(response)` — new public method mirroring `prepare()`'s fail-open + cost-logger pattern, on response text
- [x] `Tokenetics()`'s default pipeline now runs the real stages automatically, in fixed order — `dedup → near_dup → schema_minification` — with fresh instances per call (not shared singletons, to avoid `--disable`-style mutation leaking across instances)
- [x] `scripts/dev_demo.py` updated with a sample conversation containing a real duplicate, plus a `finalize()` call demonstrating post-hoc trim

**Phase 2 complete.** Next up: Phase 3 (shared task classifier + a 30-50 example hand-labeled validation set — clears the accuracy gate before Phase 4 consumes it).

## Phase 3 (shared task classifier + hand-labeled validation set)

- [x] `TaskClassifierStage` — deterministic regex/keyword scoring over the latest user message + a structural scan for `tool_use`/`tool_result` content; emits `(task_type, confidence)` onto `request.meta` for stages 4 and 9 to read later — `src/tokenetics/stages/task_classifier.py`
- [x] Categories: `code`, `conversational`, `extraction`, `tool-heavy`, plus an explicit `None` ("unclassified") outcome below the confidence threshold, so downstream stages get an honest "don't know" instead of a guessed label
- [x] Slotted into the fixed pipeline order between `near_dup` and `schema_minification` — `src/tokenetics/orchestrator.py`
- [x] 30-50 example hand-labeled validation set (32 examples, covering all 4 categories plus 2 unclassified edge cases) — `tests/fixtures/classifier_validation_set.json`
- [x] Accuracy gate test: **85%** threshold (confirmed with the user) — `tests/test_task_classifier_accuracy.py`
- [x] Per-category unit tests — `tests/test_task_classifier.py`
- [x] `scripts/dev_demo.py` updated (classifier now fires by default; its `task_type`/`confidence` show up in the existing per-stage log via `self.note()`)

**Phase 3 complete.** Next up: Phase 4 (classifier-dependent stages: tool-relevance filtering, structured-output enforcement, brevity injector, adaptive generation budgets).

## Phase 4 (classifier-dependent stages)

- [x] Tool-relevance filtering added to `SchemaMinificationStage` (same pipeline slot as minification, not a new stage): drops only tools with zero lexical-word-overlap against the latest user message; keeps all tools untouched if there's no user text to compare against — `src/tokenetics/stages/schema_minification.py`
- [x] `StructuredOutputStage` (9a) — forces `tool_choice` onto an already-registered tool when the classifier flags `extraction` and a tool's name/description clears a lexical-match bar; never invents a schema, respects a caller-set `tool_choice`. Schema-source approach confirmed with the user (2026-07-26): reuse an existing matching tool, not a generic JSON constraint or a logged-only no-op — `src/tokenetics/stages/structured_output.py`
- [x] `BrevityInjectorStage` (9b) — `extraction` → MODERATE instruction appended to the system prompt. `code`/`conversational`/`tool-heavy` → AGGRESSIVE by default, but only when `meta.bounded_shape` is True (added 2026-09-06, see "Defaulting AGGRESSIVE..." below — the classifier's 4 categories alone don't reliably signal "verifiable short answer, minimal reasoning," `bounded_shape` narrows within them). Ambiguous shape or task type → OFF. `src/tokenetics/stages/brevity_injector.py`
- [x] `AdaptiveBudgetStage` (9c) — task-type-derived `max_tokens` (widen-only, never narrows below the caller's value) and task-type-derived thinking effort via `thinking: {type: "adaptive"}` + `output_config.effort`, gated by a new model-compatibility table; skips entirely (info-level log) on unsupported models or when the caller already set their own `thinking` config — `src/tokenetics/stages/adaptive_budget.py`
- [x] Resolved the max_tokens/stateless-core conflict flagged during Phase 4 planning: the truncation-rate stat that widens the margin further is caller-supplied per call (`config["truncation_stats"]`), not stage-tracked — see the project brief's Amendments log (2026-07-26)
- [x] New versioned model-compatibility table (`LAST_VERIFIED = "2026-07-26"`), unknown/unconfirmed models conservatively resolve to unsupported rather than guessed — `src/tokenetics/core/model_compatibility.py`
- [x] Shared `core/lexical.py` helper (`overlap_score`, `latest_user_text`, stopword-filtered) — used by both tool-relevance filtering and structured-output's tool matching; `task_classifier.py` refactored to use the same `latest_user_text` instead of its own copy
- [x] Slotted into the fixed pipeline order after `schema_minification`: `structured_output → brevity_injector → adaptive_budget` — `src/tokenetics/orchestrator.py`
- [x] Per-stage unit tests — `tests/test_structured_output.py`, `tests/test_brevity_injector.py`, `tests/test_adaptive_budget.py`, `tests/test_lexical.py`, `tests/test_model_compatibility.py`, plus new relevance-filtering tests in `tests/test_schema_minification.py`
- [x] `scripts/dev_demo.py` updated: the Phase-2-era blanket `thinking: {type: "disabled"}` default removed now that `adaptive_budget` owns that decision for classified requests on supported models; documented which stages the default sample does/doesn't exercise

**Caveat, flagged during Phase 4 planning and still true**: CLAUDE.md's testing bar for stage 9c calls for a real-response `max_tokens` stress test and a thinking-effort validation against the benchmark suite's held-out quality-check set — neither exists yet, since both need Phase 9's corpus. Phase 4's tests are structural (correct routing per task type, conservative fallback, fail-open on unsupported models), not the full quality/truncation-rate study. That's the acknowledged forward dependency, not a skipped requirement — the real validation is still owed at Phase 9.

**Phase 4 complete.** Next up: Phase 5 (context scheduler: per-turn classifier, pinning, DP knapsack, greedy fallback, degraded mode, latency benchmark).

## Phase 5 (context scheduler / DP knapsack)

- [x] Per-turn activity classifier (`code`/`error`/`decision`/`tool_result`/`small_talk`, priority-ordered, `small_talk` the catch-all) — separate from stage 3's request-level classifier — `src/tokenetics/stages/context_scheduler.py`
- [x] Retention-value scoring: type weight + a bounded recency bonus; heuristic defaults, not yet benchmarked (Phase 9's job)
- [x] Pinning: `decision` turns always pinned; `error` turns pinned unless a **later turn from the user** (not the assistant, not just "conversation moved on") contains an explicit positive resolution signal — resolved with the user 2026-07-26, see the project brief's Amendments log
- [x] Token budget: caller-supplied via `config["token_budget"]` when given; otherwise falls back to a context-window-derived default scaled by `task_type`, logged `estimated` — `src/tokenetics/core/context_window.py`
- [x] 0/1 knapsack via DP (budget discretized into 100-token buckets), verified against brute-force on small synthetic cases
- [x] Greedy value-per-token fallback above a threshold empirically justified with real timing data (documented in-code: DP stays under ~15ms through ~n=500/budget=500-bucket histories, crosses into hundreds of ms well beyond that) — informal justification per Phase 5's build-order requirement; a rigorous latency benchmark is still Phase 9 scope
- [x] Real degraded-mode fallback (last-N-turns truncation, N=20) — the first stage whose `degraded_fallback()` does something other than pass through unchanged
- [x] Closed a real gap found while wiring this up: `Tokenetics.prepare()` previously always passed an empty `{}` config to every stage, which meant `config["token_budget"]` (and Phase 4's `adaptive_budget` `truncation_stats`) was unreachable through the real public API, only testable by calling a stage's `run()` directly. Added `Tokenetics(..., stage_config={...})`, keyed by stage name — `src/tokenetics/orchestrator.py`
- [x] Repositioned in the fixed pipeline order: `... → schema_minification → context_scheduler → structured_output → ...` (stages 6-8 still don't exist, so the stage-9 trio still immediately follows)
- [x] Per-turn token costs for the knapsack's internal sizing use a fast local ~4-chars/token estimate, not the real tokenizer — stages have no client/network access (Tier 0 is deterministic). The orchestrator's own before/after counts remain the authoritative `measured` numbers for anything logged
- [x] Tests: turn classification, pinning/resolution (including that assistant-claimed resolution doesn't count), DP-vs-brute-force, greedy-fallback trigger, degraded mode (including through the orchestrator with the stage disabled), budget-source (caller-supplied vs. estimated fallback), `stage_config` threading — `tests/test_context_scheduler.py`, `tests/test_context_window.py`, additions to `tests/test_orchestrator.py`
- [x] `scripts/dev_demo.py` `long_history` scenario added

**Role-alternation limitation, flagged during implementation and resolved same-day (2026-07-26)**: unlike `near_dup`'s pair-aware dropping, `context_scheduler` drops individual turns independently based on score, so nothing inherently guaranteed the surviving subset still alternated user/assistant roles correctly for the real API. Fixed with a `_fix_alternation` post-processing pass, applied as the last step of both `run()` and `degraded_fallback()`: adjacent same-role survivors are merged into one message (nothing lost, just combined — logged distinctly from an actual drop), and a lone leading non-`user` survivor is dropped (the API requires the first message to be `user` — the one case where content is genuinely lost, noted as `alternation_fix_dropped_lead_turn`). If the fix would empty the result entirely, the stage fails open and returns the request unmodified. Caught a related logging bug while verifying this against `long_history`: a lossless merge was initially (incorrectly) logged as a "dropped lead turn" — fixed by having `_fix_alternation` report the two cases distinctly rather than inferring from a count difference.

**Phase 5 complete (structural).** Next up: Phase 6 (caching: safety guard → reorder → pricing config table → breakpoint optimizer + synthetic-traffic benchmark).

## Phase 6 (caching: safety guard → reorder → pricing table → breakpoint optimizer)

- [x] Cache pricing sourced live (not guessed) from Anthropic's prompt-caching docs on 2026-07-27: 5m write = 1.25x, 1h write = 2x, read = 0.1x base input price, universal multipliers stacked on each model's own base price — `src/tokenetics/core/cache_pricing.py`
- [x] `CacheSafetyError` — dedicated exception type (not a bare `Exception`), specifically catchable, with a message identifying exactly what changed — `src/tokenetics/core/errors.py`
- [x] Extended `TokeneticsRequest` with `cache_breakpoint: CacheBreakpoint | None` (anchor: `"system"`/`"tools"`, ttl: `"5m"`/`"1h"`); `from_api_kwargs`/`to_api_kwargs` round-trip real `cache_control` block placement, including parsing an already-cached `previous_request` back into a comparable internal shape — `src/tokenetics/core/request.py`
- [x] `CacheReorderGuardStage` (stage 7) — the guard diffs the current request's system+tools against a caller-supplied `config["previous_request"]` (raw API kwargs) and hard-raises `CacheSafetyError` if either changed; the reorder half is a normalize-and-verify no-op given today's already-stable-first request shape (per the 2026-07-27 scoping decision) — `src/tokenetics/stages/cache_reorder_guard.py`
- [x] **Special-cased `CacheSafetyError` in the orchestrator's fail-open framework** — a necessary fix caught while wiring this up: the generic `except Exception:` catch-all would otherwise have silently swallowed the guard's hard-raise exactly like every other stage's failures, defeating the entire point of having one. Added a regression test asserting it actually propagates out of `Tokenetics.prepare()` — `src/tokenetics/orchestrator.py`
- [x] `CacheBreakpointOptimizerStage` (stage 8) — anchors to `tools` if present else `system` (prefix-based caching means one marker at the end of the last stable block caches everything before it too); needs ≥2 matching `config["cache_usage_history"]` entries (`{"timestamp", "content_hash"}`, hashed via `core/cache_hash.py`) to estimate a repeat interval, conservative no-breakpoint otherwise — `src/tokenetics/stages/cache_breakpoint_optimizer.py`
- [x] **Caught and fixed a real modeling bug** while writing this stage's tests: the first version compared each TTL tier's *total* expected benefit independently over its own window, which meant the 1-hour tier's larger window always won mathematically (more window = more assumed reads), so the 5-minute tier could never actually be selected even at very short repeat gaps. Fixed by recognizing that a cache read refreshes its own TTL — once a tier's window covers the repeat gap, the cache stays continuously warm off one amortized write, so the cheapest viable tier should win. Redesigned to a simple "cheapest tier that stays warm" rule; the pricing multipliers are now used for an informational net-benefit estimate logged alongside the decision, not for the tier choice itself.
- [x] **Caught and fixed a second real gap, post-Phase-6, via `--measure-usage` against the real API (2026-08-02)**: `cache_hit`'s real `usage.cache_creation_input_tokens`/`cache_read_input_tokens` both came back 0 despite the stage logging `breakpoint_placed=True` — first traced to a per-model minimum cacheable prefix length (1024 tokens for `claude-sonnet-5`) that the real API enforces silently, with no error, just an ordinary uncached request. Added `MIN_CACHEABLE_TOKENS` (sourced live, same doc, re-verified 2026-08-02) to `core/cache_pricing.py`, and the stage declined to place a breakpoint below that minimum (`reason="prefix_below_minimum_cacheable_size"`). `cache_hit`'s demo system prompt and `cache_benchmark.py`'s three traffic profiles were resized above the threshold.
- [x] **Caught and fixed a third, more fundamental gap in the same investigation**: even after the resize, the numbers still came back 0/0. Root cause was the anchor-selection logic itself: Anthropic's cache hierarchy is `tools -> system -> messages`, and a breakpoint caches everything up to AND INCLUDING the block it's on, in that order — so a marker on the last `tool` caches **only the tools array**, never the `system` prompt after it. The stage had been anchoring to `"tools"` whenever tools were present, on the mistaken assumption (confirmed live against the docs, 2026-08-02) that this also covered `system` — it was confusing the JSON *key* order `to_api_kwargs` happens to emit (system, tools, messages) with Anthropic's actual cache-*hierarchy* order (tools, system, messages), which is the reverse for system/tools. For `cache_hit`, this meant only the ~30-token tools array was ever being offered to the cache, far below the minimum, regardless of how large the system prompt was. Fixed by anchoring to `"system"` whenever one exists (covers tools+system together) and only falling back to `"tools"` when there's no system to fold in — `src/tokenetics/stages/cache_breakpoint_optimizer.py`. `cache_reorder_guard.py`'s docstring (the likely source of the original confusion) was also corrected. **Confirmed end-to-end against the real API same day**: `--all-scenarios --measure-usage` showed `cache_hit`'s pipeline-enabled call writing a real `cache_creation_input_tokens=1724`, then an immediate repeat call reading those same 1,724 tokens back via `cache_read_input_tokens` — the full write-then-read cycle, measured, not just unit-tested.
- [x] Synthetic-traffic benchmark, per this phase's explicit build-order requirement (distinct from Phase 9's real-corpus benchmark suite) — `scripts/cache_benchmark.py`, no network calls, deterministic. Headline numbers at 1000/5000 simulated calls across three traffic profiles (hot/warm/cold, each sized above the real per-model minimum cacheable prefix — see the `cache_breakpoint_optimizer` minimum-size fix below): **~28-31% cheaper than never caching, ~10-11% cheaper than a naive always-5m-breakpoint habit**, ~80% cache hit rate
- [x] Repositioned in the fixed pipeline order: `... → context_scheduler → cache_reorder_guard → cache_breakpoint_optimizer → structured_output → ...`
- [x] Tests, including the standalone hard-raise test required before anything else in this phase per CLAUDE.md — `tests/test_cache_reorder_guard.py`, `tests/test_cache_breakpoint_optimizer.py`, `tests/test_cache_pricing.py`, `tests/test_cache_hash.py`, plus cache-control round-trip tests added to `tests/test_request.py`
- [x] `scripts/dev_demo.py`: `cache_hit` scenario (breakpoint optimizer placing a 5m breakpoint) and `cache_guard_violation` scenario (demonstrates the hard-raise, caught and printed rather than crashing `--all-scenarios`)

**Known stub-only artifact, not a real bug**: `dev_demo.py`'s offline (`StubAnthropicClient`) token counts show `cache_breakpoint_optimizer` "costing" tokens when it places a breakpoint — this is the stub's `len(str(system))` approximation reacting to `system` becoming a block-list once `cache_control` is attached, not a real cost. The real Anthropic API only counts actual text content, not JSON/cache_control structure; against the real API this stage should show ~0 request-token change.

**Phase 6 complete.** Next up: Phase 7 (structured delta compression + `apply_delta()` + round-trip tests).

## Phase 7 (structured delta compression + `apply_delta()`)

- [x] **Join-key design (resolved 2026-08-02)**: scoped to `tool_result` content blocks only, keyed by `tool_use_id` — `config["previous_payloads"]: dict[str, str]` maps a tool_result's `tool_use_id` (unique within the current request, already known to the caller when constructing that reply) to whatever they consider its previous version. General prose-text repeats stay near_dup's job. See the brief's stage 6 amendment for the full resolution.
- [x] `src/tokenetics/core/delta.py` — `compute_delta`/`apply_delta`, stdlib-only (`difflib` + `json`, no new core dependency). Two content shapes, auto-detected: JSON content (both previous/new parse as an object/array) gets a minimal hand-rolled structural patch (add/remove/replace by escaped JSON-pointer-style path, RFC 6902-flavored subset, not the full spec); everything else gets a line-level patch via `difflib.SequenceMatcher` opcodes, applied by directly replaying those opcodes against `previous` rather than re-parsing unified-diff hunk text (avoids real correctness risk for no benefit, since nothing outside the module ever reads a human-authored diff). `DELTA_SIZE_RATIO_THRESHOLD = 0.6` — a plain documented constant (not pricing/compat data, so no versioned table, same precedent as near_dup's similarity thresholds).
- [x] **"Exact" round-trip means two different things by design**: line-patch reconstruction is byte-exact; JSON-patch reconstruction is semantically exact (`json.loads(apply_delta(...)) == json.loads(new)`, re-serialized via `json.dumps(..., sort_keys=True)`) — tool-result payloads are data, not formatted prose, so byte-exact JSON formatting was never a real invariant worth preserving. Documented explicitly in the module docstring so it reads as a decision, not an oversight.
- [x] `DeltaCompressionStage` (stage 6) — `src/tokenetics/stages/delta_compression.py`. Only `tool_result` blocks whose `content` is a plain string are eligible (list-of-blocks content, e.g. an image result, is left untouched — conservative by default, not guessed at); falls back to full content when the encoded delta isn't meaningfully smaller than the threshold.
- [x] Repositioned in the fixed pipeline order: `... → context_scheduler → delta_compression → cache_reorder_guard → ...`
- [x] Tests: `tests/test_delta.py` (round-trip correctness for both shapes, no-change case, totally-different-content-returns-None, path-escaping, JSON top-level-type-change fallback, marker-rejection) and `tests/test_delta_compression.py` (stage-level matching/skipping behavior, multi-block requests, plus the deliberately-broken-plugin fail-open regression test required before any stage is considered done — a malformed content block crashes the stage internally, and the orchestrator must catch it and pass the request through unmodified). Pipeline-order assertion updated in `tests/test_orchestrator.py`.
- [x] `scripts/dev_demo.py`: `delta_hit` scenario — a tool_result whose JSON content differs from a caller-supplied previous version by one field out of many; offline-verified 1035 → 113 chars (single-op JSON patch), confirmed it survives the full pipeline unmodified through every later stage.
- [x] **Deliberately deferred, not resolved here**: whether Claude actually interprets a delta-encoded tool_result correctly (vs. just seeing the full payload) is a real model-quality question, not a mechanical one — belongs in Phase 9's held-out quality-check set, same treatment as brevity/thinking-effort tuning, not assumed to just work. Flagged in both the brief and the stage's own docstring.

**Phase 7 complete.** Next up: Phase 8 (assemble Tier 0 in fixed order, end-to-end integration test, freeze the Tier 0 API).

## Phase 8 (Tier 0 assembled in fixed order, end-to-end integration, frozen API)

This phase wasn't "build the pipeline" — all 10 request-side stages plus `PostHocTrimStage` were already wired into the orchestrator in the correct fixed order incrementally as each earlier phase landed. It's "prove the whole pipeline holds together and lock its public surface":

- [x] **End-to-end integration test** — `tests/test_tier0_integration.py::test_end_to_end_pipeline_runs_in_the_documented_fixed_order` runs a realistic multi-turn, tool-using conversation through the *entire* pipeline and asserts the logger's runtime stage trace matches CLAUDE.md's fixed order exactly — not just the static stage list (already covered by `test_orchestrator.py`), the actual execution order over a real request. A companion test confirms the output is well-formed enough for the real API: alternating roles, first message `user`, the relevant tool survived.
- [x] **Stage 4/5 double-prune regression test** — `test_stage_4_and_5_never_double_prune_the_same_content`. Schema minification (stage 4) only ever prunes `request.tools`; the context scheduler (stage 5) only ever prunes `request.messages` — disjoint fields by construction (confirmed by reading both stages' source, each returns `replace(request, tools=...)` / `replace(request, messages=...)` respectively, never the other). The test proves this holds under a real *combined* run, not just code inspection: a request with both an irrelevant tool and a tight token budget in play at once, asserting each stage's log only ever mentions its own kind of drop (`dropped_tools` vs. `dropped_turns`), never the other's.
- [x] **Contract/signature test on `Tokenetics.prepare()`/`finalize()`/`__init__`** — `test_prepare_signature_is_frozen`, `test_finalize_signature_is_frozen`, `test_tokenetics_init_signature_is_frozen`, via `inspect.signature`. A later phase changing these signatures now fails a test instead of silently drifting.
- [x] **Tier 0 API frozen** as of 2026-08-02. Per CLAUDE.md's "don't jump ahead" rule, Phase 9 (benchmarking) and everything after it should not have started before this milestone — it now has.
- [x] **A real crash in `finalize()` caught much later (2026-08-12), during a live Phase 11 dashboard hand-check, fixed same day**: a response whose visible text ended in a trailing newline crashed `Tokenetics.finalize()` with a 400 from the real API — `count_text_tokens()`'s own `count_tokens` call wraps the extracted text as a synthetic assistant message to measure it, and the Messages API rejects any assistant content ending in trailing whitespace, `invalid_request_error: final assistant content cannot end with trailing whitespace`. Not just a counting-endpoint quirk: `finalize()`'s own stated contract is "text ready to store/re-inject as future context" — text with trailing whitespace was never actually valid for that; re-injecting it as history on a later turn would have hit the identical 400 from a real completion call. Fixed by stripping trailing whitespace from the extracted text immediately, before any response-stage processing (including the token count that crashed) — `text = "".join(...).rstrip()` in `orchestrator.py`'s `finalize()`. This slightly changes `finalize()`'s behavior with an empty `response_stages` list (previously a strict no-op on the extracted text; now still strips trailing whitespace, since that's a real API validity requirement, not a stage's job) — `tests/test_finalize.py` updated to describe this precisely, plus a new regression test reproducing the exact crash case. This is a Tier 0 core fix (`orchestrator.py`, frozen-API territory, though only `finalize()`'s *signature* is frozen, not its internals) surfacing well after the freeze — flagged here rather than silently folded into a later phase's notes, since it affects every phase's response-side text, not just the phase where it was found.

**Phase 8 complete.** Next up: Phase 9 (benchmark suite — real measured numbers, before any Tier 2 extras are built).

## Phase 9 (benchmark suite — real measured numbers)

A different flavor from 0–8: not new pipeline stages, but running the now-frozen Tier 0 pipeline against real data and reporting honestly on what it does. First phase with sustained real spend, not just occasional dev_demo spot-checks.

- [x] **Quality-grading methodology (resolved 2026-08-02)**: a hand-written rubric's `required_elements` (facts/keywords that must appear in the response) is the authoritative, objective pass/fail gate for the held-out quality-check set. An optional LLM-judge score is a secondary signal only — grading Claude's output by asking Claude is reasonable as a cheap secondary check, but using it as sole authority in exactly the risk-bearing cases this set exists to catch would undercut the whole point.
- [x] **Cost control (resolved 2026-08-02)**: the corpus run (token savings) is entirely free — same `prepare()` + `count_tokens` model as `dev_demo.py`, no completions. Only the quality-check run spends real money, and it's gated: an upfront cost estimate is always shown; the runner refuses to proceed past `--cost-ceiling` (default $1.00) even with `--confirm-spend`, and refuses to run at all without `--confirm-spend` regardless of estimate size.
- [x] **Caught and fixed a real pricing bug while sourcing output prices for the cost estimator**: `core/cache_pricing.py`'s `BASE_INPUT_PRICE_PER_MTOK["claude-sonnet-5"]` was hardcoded to $3.00; live-verified current price (2026-08-02) is $2.00 (Sonnet 5's introductory pricing through 2026-08-31, stepping up 2026-09-01). Fixed, and added the previously-missing `OUTPUT_PRICE_PER_MTOK` table from the same live fetch, plus `claude-opus-5` (present in `MIN_CACHEABLE_TOKENS` already but missing from the price tables). `cache_benchmark.py`'s headline percentages are unaffected (ratio-based, price-scale-independent); its absolute "arbitrary units" numbers shifted down proportionally. Sourced from `https://platform.claude.com/docs/en/about-claude/pricing`, not the prompt-caching-specific page the rest of the table cites — both are now tracked as separate `SOURCE_PRICING`/`SOURCE_PROMPT_CACHING` constants rather than one shared `SOURCE`.
- [x] `src/tokenetics/core/benchmark.py` — corpus/quality-check JSON loading, `estimate_cost_usd` (pre-flight, explicitly `estimated` not `measured`), `aggregate_savings_by_category` (min-max range per category, never a flat percentage, per CLAUDE.md's honest-benchmarking discipline), `check_required_elements` (the rubric gate).
- [x] `scripts/benchmark_runner.py` — `corpus` subcommand (free) and `quality-check` subcommand (cost-ceiling-gated real completions, on vs. off per named stage). `corpus` also prints a per-sample per-stage trace by default (which stage(s) actually produced the token delta, `--quiet` to suppress) — added after a real run's category-range extremes (an 80% ceiling, a -5.7% floor) turned out to be untraceable back to a specific sample/stage from the aggregate output alone.
- [x] `benchmarks/corpus/` — **grown to 15 samples × 4 categories (code, conversational, extraction, tool-heavy) = 60 total (2026-08-02)**, clearing the low end of CLAUDE.md's 15-20+ per category floor. Each category has genuine variety (bug types, question shapes, extraction domains, tool-call patterns), not padding — e.g. `code` spans off-by-one bugs, async deadlocks, SQL optimization, closures, design patterns; `extraction` spans 12 distinct real-world extraction domains, each with its own matching tool schema.
- [x] **Caught and fixed a real corpus-design bug via `benchmark_runner.py corpus`'s new per-stage trace (2026-08-02)**: `conversational_002` was built to demonstrate `near_dup` (a user-turn paraphrase pair, one word different), but its two assistant replies were left byte-identical — since `dedup` runs before `near_dup` in the fixed pipeline, it consumed the exact-duplicate assistant turns first, leaving nothing near-duplicate-but-not-exact for `near_dup` to actually catch. Found by scanning all 60 samples offline for which stage produces a nonzero delta; confirmed by directly computing the real shingle-Jaccard similarity (`near_dup._shingles`) for several candidate edits, since word-level edits on short text (~200 chars) reliably score well below the 0.95 threshold even when word-overlap looks high (~0.86-0.89) — 4-character shingling is much stricter than word overlap. Fixed by lengthening both turns and editing only a single word at the very end (matching the pattern `dev_demo.py`'s own `near_dup` scenario already used successfully), verified offline to land at 0.9535/0.9655 Jaccard for the user/assistant pairs respectively, comfortably clearing 0.95. Offline-reverified: `near_dup` now merges the pair (`dedup` shows zero effect).
- [x] `benchmarks/quality_checks/` — **grown from 2 to 5 samples covering all 5 risk-bearing concerns CLAUDE.md names (2026-08-02)**: `brevity_injector` (extraction completeness under the MODERATE instruction), `adaptive_budget` × 2 (thinking-effort correctness on a real edge-case bug, AND a separate max_tokens-truncation sample — a deliberately tiny caller-supplied cap of 30 that widens to 360, verified offline), `context_scheduler` (a decision buried under small-talk padding, recalled correctly under a token_budget tight enough to force pruning), `delta_compression` (a JSON-patch-encoded tool_result correctly reporting the *current*, not stale, status — verified offline that the delta actually engages: a single-op patch against a padded previous payload). Extended `QualityCheckSample`/`load_quality_checks` with an optional `stage_config` field (needed for `context_scheduler`'s `token_budget` and `delta_compression`'s `previous_payloads` — neither is testable via `--stage` name alone) and threaded it through `benchmark_runner.py`'s on/off comparison.
- [x] **A 6th quality-check sample, `schema_minification`, added same day after a real `corpus` run surfaced a genuine risk the original 5 concerns didn't cover**: the corpus's per-stage trace showed `tool_heavy_003`/`tool_heavy_014` hitting ~80% "savings" because `schema_minification`'s tool-relevance filter dropped a tool that had *already been used* earlier in the conversation, purely because the latest follow-up turn didn't lexically overlap with that tool's name/description. Not necessarily wrong (many follow-ups genuinely don't need a repeat call) — but a real correctness risk if one *does* need it, which the corpus (token-count only) can't see. Required a genuine harness extension, not just a new sample: the existing quality-check mechanic only checked response *text* (`required_elements`), but the correct behavior here is a `tool_use` call with no meaningful text. Added `check_tool_calls`/`required_tool_calls` to `core/benchmark.py` (an independent, optional grading signal alongside `required_elements`, combined via a new `_grade()` helper in the runner) and a sample reproducing the exact real pattern (a follow-up needing the same tool on a different SKU) — offline-verified that with the filter enabled, `check_inventory` is removed from `tools` entirely (the model literally cannot call it), and stays present with the filter disabled.
- [x] **Confirmed via a real billed run, then fixed same day (2026-08-02)**: `quality-check --stage schema_minification --confirm-spend` measured `passed=False, missing=['check_inventory']` with the filter enabled vs. `passed=True` disabled — the model genuinely never attempted to re-check stock when the tool was unavailable, confirming this was a real regression, not a hypothetical. Fixed in `src/tokenetics/stages/schema_minification.py`: tools with a `tool_use` block anywhere earlier in `request.messages` are now exempt from the relevance drop entirely, regardless of lexical score against the latest turn — an extension of the stage's own existing "keep anything ambiguous" bias (a tool already reached for once is inherently less ambiguous than a never-used one), not a new behavior class. Trade-off accepted deliberately: a used tool stays exempt for the rest of the conversation even once genuinely no longer relevant, favoring correctness over savings. Offline-reverified against the exact real cases that surfaced this (`tool_heavy_003`, `tool_heavy_014`, the quality-check sample itself) — all three now correctly keep the already-used tool; the pre-existing never-used-irrelevant-tool case (`extraction_003`'s `send_email`) still drops correctly, unaffected. Two new regression tests added to `tests/test_schema_minification.py`. **Confirmed against the real API same day**: re-running `quality-check --stage schema_minification --confirm-spend` post-fix measured `passed=True, missing=[]` in both the with- and without-`schema_minification` conditions — the full loop (real bug found → traced → tested → fixed → reconfirmed against the live API) closed within Phase 9 itself, exactly the workflow this phase exists to enable.
- [x] **Revised the fix from permanent to windowed exemption (2026-08-09)**, after discussing the cost tradeoff: permanent exemption is unbounded — in a long multi-tool agentic session, every tool ever used accumulates and never gets pruned again, eventually defeating the point of the filter. Replaced with `_tools_recently_used(messages, window_turns)`: a tool stays exempt only while its most recent `tool_use` is within `_TOOL_REUSE_WINDOW_TURNS` (3) *genuine* user follow-ups — `tool_result` deliveries (mechanically `role="user"` but not a real question) don't consume the window. Window size reasoned, not guessed: 1 fails a two-step clarification ("what about a large?" then "in blue too?"); 5+ starts approximating permanent for most realistic tool-using threads (commonly under 10 turns total), so the marginal safety over 3 isn't worth the marginal cost given up. Caught and fixed a real off-by-one during implementation: an initial backward-walk version found a tool_use as still-exempt one follow-up later than intended, because the loop could reach the tool_use message before the turn-counter's own boundary check fired on it; replaced with a simpler forward scan (each `tool_use` resets that tool's counter to 0, each genuine user turn increments every active counter by 1, exempt if the final count is within the window) — verified against both boundary cases (`window_turns` exactly and `window_turns + 1`) and a multi-round-trip-tool-calls-before-a-real-followup case. Re-verified the fix still resolves the original real regression cases (`tool_heavy_003`, `tool_heavy_014`, the quality-check sample) unchanged, since all three are well within the window. Four tests total now cover this behavior in `tests/test_schema_minification.py`. **Confirmed against the real API same day**: re-running `quality-check --stage schema_minification --confirm-spend` against the windowed version measured `passed=True, missing=[]` both ways — unchanged from the permanent-exemption result, as expected, since that sample's follow-up is immediate and well within the 3-turn window. The revision from permanent to windowed changes long-conversation token economics, not this sample's outcome.
- [x] **Added `tool_heavy_018` (2026-08-09)** to demonstrate the window actually expiring in a real `corpus` sample, not just synthetic unit tests: `get_weather` used once, followed by 5 genuine unrelated follow-ups (past the 3-turn window) before the current query. Offline-verified `get_weather` is correctly dropped again (`dropped_tools=[{'tool': 'get_weather', 'score': 0.0}]`, `tools=[]`), reclaiming 12.0% (stub estimate; real API will differ) that a permanent exemption would have kept paying for indefinitely. `tool-heavy` is now 18 samples (63 total across the corpus). New test `test_real_corpus_sample_demonstrates_the_tool_reuse_window_expiring` in `tests/test_benchmark.py`. Added `--sample`/`--category` filters to `benchmark_runner.py corpus` (previously always ran the full corpus) so a single new sample can be checked without a 63-sample run. **Confirmed against the real API same day**: `corpus --sample tool_heavy_018` measured 775→350 tokens (54.8% real savings, `schema_minification saved 425`, `dropped_tools=[{'tool': 'get_weather', 'score': 0.0}]`) — the window-expiry revision verified end-to-end, not just offline.
- [x] **`corpus`'s post-fix re-run confirmed the fix at the aggregate level too**: `tool-heavy`'s range collapsed from the false `0.0% - 80.1%` down to a flat `0.0% - 0.0%` across all 15 original samples — correct, but revealed a second, honest finding: none of those samples supply `stage_config`, so `cache_breakpoint_optimizer`/`delta_compression` never fire (`insufficient_usage_history`/`no_previous_payloads_supplied` on every one), meaning the category couldn't demonstrate either stage's savings at all. Extended `CorpusSample`/`load_corpus` with the same optional `stage_config` field `QualityCheckSample` already has, and added two more samples: `tool_heavy_016` (delta compression, mirrors `dev_demo.py`'s `delta_hit`) — offline-verified real savings, 1581→660 tokens (58.3%); `tool_heavy_017` (cache breakpoint optimizer, mirrors `cache_hit`) — offline-confirmed `breakpoint_placed=True`, but **flagged in its own description that it will never move this report's percentage**, since caching changes price-per-token via `cache_control`, not token count, the same structural blind spot established repeatedly in Phase 6/9's cache work; its real effect only shows in `--measure-usage` or a cost-based metric, never a token-count-only report like `corpus`. `tool-heavy` is now 17 samples (62 total across the corpus). **Confirmed against the real API same day**: `tool_heavy_016` measured 1064→569 tokens (46.5% real savings, `delta_compression saved 495`); `tool_heavy_017` measured 1807→1807 (0.0%, `breakpoint_placed=True`) — the correct real-API result, not the stub artifact seen offline, matching the Phase 6 finding again. `tool-heavy`'s range is now `0.0%-46.5%` (n=17), honestly demonstrating real savings instead of a flat zero or a false 80% ceiling. Minor, non-blocking observation: `tool_heavy_017` has no actual `tool_use`/`tool_result` exchange, so `task_classifier` reads it as `conversational` at runtime despite being filed under the `tool-heavy` corpus category — doesn't affect what the sample tests (cache placement), left as-is.
- [x] Tests: `tests/test_benchmark.py` — cost-estimate math, range-aggregation math (including single-sample and empty-input edge cases), rubric pass/fail matching (including case-insensitivity and missing-elements reporting), `CorpusSample`/`QualityCheckSample` optional `stage_config` loading, plus tests that load the *real* `benchmarks/corpus`/`benchmarks/quality_checks` directories (including that `tool_heavy_016`/`017` actually engage their target stage) so a malformed or incomplete real file fails a test, not just silently loads wrong at runtime.
- [x] `--dry-run`/cost-ceiling refusal paths verified to make zero network calls (checked directly, no API key needed for those paths).

- [x] **Corpus grown from the floor to the top of CLAUDE.md's stated range (2026-08-09)**: 15 → 20 samples per category (80 total, up from 60). `code` added generators, decorators, git conflicts, recursion/stack-overflow, dependency injection; `conversational` added music, etiquette, dream-science, public-speaking, personality-psychology; `extraction` added 5 more real-world domains (job postings, reservations, warranty claims, utility bills, cancellations) each with a matching tool schema; `tool_heavy` added a failed-then-retried tool call and a 3-different-tools-in-sequence agentic chain, on top of the earlier `_016`/`_017`/`_018` additions (20 total in that category).
- [x] **Quality-check set grown from 1-2 samples per stage to 2-3 (2026-08-09)**, closing the "real statistical confidence wants more than one probe" gap: `brevity_injector` +1 (a 6-field extraction under brevity pressure, stress-testing whether a safety-relevant detail like an allergy note survives); `context_scheduler` +1 (the OTHER pinned-turn type — an unresolved *error*, not a decision, tested independently); `delta_compression` +1 (a text/line-patch delta — re-reading a config file after one line changed — covering the OTHER content-format branch from the existing JSON-patch sample, matching the brief's second worked example); `schema_minification` +2 (a negative control confirming a genuinely-irrelevant never-used tool still drops cleanly with zero quality impact, and a real-completion test placed exactly *at* the reuse-window boundary — 2 padding follow-ups then a 3rd genuine need, not past it like `tool_heavy_018`). 11 quality-check samples total now, all offline-verified to actually engage their target mechanism (`test_real_quality_check_samples_actually_engage_their_mechanism`), not just parse. Total estimated cost for a full `quality-check --confirm-spend` run: ~$0.04.

- [x] **The full 80-sample `corpus` run and the 11-sample `quality-check --confirm-spend` run both happened for real (2026-08-09)**. `corpus`: completely clean — all 7 new samples behaved exactly as predicted (new single-turn samples 0.0%, new extraction samples in the same -5.3% to -5.6% band, new tool-heavy samples 0.0% since neither supplies `stage_config`); ranges unchanged from before growth. `quality-check`: **caught a real, systematic bug in the grading harness itself, not the pipeline** — `quality_brevity_001`/`002` both failed *identically* with and without `brevity_injector` (impossible if the stage were the cause), because both are extraction-shaped and `structured_output` forces `tool_choice`, so the real answer lives in the `tool_use` block's `input` (structured JSON), not text — `check_required_elements` was only ever checking response text. Same family of gap `check_tool_calls` fixed for tool-use-only responses, just the mirror case ("answer's content lives inside a tool call" vs. "response IS a tool call"). Fixed with `response_text_with_tool_inputs()` in `core/benchmark.py` (text + JSON-serialized tool_use `input`, combined), wired into `_grade()`; 4 new unit tests. Also added response-text printing on any failure in `run_quality_check` — needed immediately, since `quality_adaptive_budget_001` failed on `missing=['fewer than two']` in BOTH conditions with no way to tell whether that's a real miss or just different phrasing (e.g. "when there's only one unique value") without seeing the actual response; this was previously unfalsifiable from the runner's output alone.
- [x] **Clean, positive results also confirmed for real**: `quality_adaptive_budget_002` (max_tokens truncation) — `with: passed=True`, `without: passed=False, missing=['split']`, exactly the intended effect; `context_scheduler` (both samples), `delta_compression` (both samples), `schema_minification` (all 3 samples) all passed both ways — none of those show the with/without pipeline actually differing in outcome for these specific samples, which for `schema_minification` matches the earlier standalone confirmation that the fix holds.

- [x] **Second real `quality-check --confirm-spend` re-run (2026-08-10), post-harness-fix**: confirmed the `response_text_with_tool_inputs` fix worked — `brevity_001`/`002` now `passed=True` both ways. `context_scheduler`/`delta_compression`/`schema_minification` (7 samples) stayed clean. Surfaced two new, real issues, both traced to root cause and fixed the same day:
  - **`adaptive_budget_001`** failed both ways on `missing=['fewer than two']`, but the newly-visible response text showed both responses *correctly* diagnosed the edge case, just phrased it differently ("only one unique value" vs. "unique has only 1 element") — no shared substring between the two genuinely-correct phrasings. Checked whether `judge_rubric` (the intended secondary signal for exactly this kind of natural-language nuance) was actually wired to a grading call: it is not — `judge_rubric` is loaded and stored but never sent to the API anywhere in the codebase, confirmed by grep. That means `required_elements`'s exact-substring match is, in practice, the *sole* automated gate, and too rigid for anything but a fixed technical term. Fixed at the mechanism level, not just this one sample: extended `check_required_elements` (`core/benchmark.py`) to accept OR-groups — a `required_elements` entry can now be a `list[str]` of alternate phrasings, passing if any one matches; plain-string entries are untouched, so no existing sample's behavior changed. `quality_adaptive_budget_001`'s rubric updated to an OR-group of the phrasings actually seen. 2 new unit tests (`test_check_required_elements_or_group_*`).
  - **`adaptive_budget_002`** newly failed both ways with visibly truncated responses (mid-sentence cutoffs) — looked like variance at first, but wasn't: the sample's prose-only B-tree prompt has no code fence, code keyword, or traceback, so `task_classifier`'s deterministic regex scoring (correctly, per its own documented rules) tags it `conversational` (0.4 confidence, 300-token base → 360 widened) rather than the `code` bucket the sample exists to exercise (800-token base → 960 widened) — confirmed offline by running the classifier directly against the exact sample text. 360 tokens was never going to be enough for a genuine 3-part technical answer; this was a benchmark-sample design mismatch, not a classifier bug or model quality issue. Fixed by adding a short code fence (a `BTreeNode` class definition — a natural thing for this question to reference) to the prompt; offline-reconfirmed it now classifies `code` at confidence 1.0.
  - 221 tests pass (was 219), ruff clean, mypy clean.

- [x] **Third real `quality-check --confirm-spend` re-run (2026-08-10), confirming both fixes above live**: `adaptive_budget_002` now shows exactly the intended contrast — `with: passed=True`, `without: passed=False, missing=['split', 'range quer']` — the OR-group and code-fence fixes both hold against a real completion, not just offline logic. `brevity_001`/`002` stayed clean (confirms the harness fix is stable across runs, not a one-off). Surfaced one more issue, diagnosed and fixed the same day: `quality_context_scheduler_001` failed with `missing=['async']` and a visibly truncated response (cut off mid-word). Distinguished from a structural bug by checking history: this exact sample+config passed cleanly both ways on the *prior* real run — same prompt, same `token_budget`, different real output length, which points to LLM response-length variance, not a regression. Root cause: the sample's final turn ("quick recap for me") is open-ended enough that Claude sometimes answers with a full technical breakdown instead of a short recap, especially once `context_scheduler` prunes the surrounding small talk — and the `conversational` category's widened max_tokens (360) isn't always enough headroom for that discretionary elaboration. This conflated "does context_scheduler retain the pinned decision" (what the sample is meant to test) with "how verbose does the LLM choose to be" (a response-length judgment call that's `adaptive_budget`'s territory, not this stage's). Fixed by rephrasing the final question to explicitly request a one-sentence answer, which removes the ambiguity driving the variance without weakening what the sample actually tests. 221 tests pass, ruff/mypy clean.
- [x] **Fourth real `quality-check --confirm-spend` re-run (2026-08-10) — fully clean: 11/11 passed with every stage enabled, 11/11 passed with every stage disabled.** The `context_scheduler_001` rephrase held against a live completion; every other sample stayed clean across this and the prior run, giving each at least two consecutive clean real runs. No unfixed gap remains — the grading-harness bugs found across four real runs (`response_text_with_tool_inputs`, `required_elements` OR-groups, the `adaptive_budget_002` classifier-bucket mismatch, the `context_scheduler_001` phrasing) are all fixed, unit-tested, and reconfirmed live.

**Phase 9 complete (2026-08-10).** `benchmarks/corpus/`: 80 samples (20/category, the top of CLAUDE.md's stated floor), confirmed clean against the real API. `benchmarks/quality_checks/`: 11 samples (2-3/stage, all 5 risk-bearing concerns CLAUDE.md names), confirmed 11/11 both ways against the real API across two consecutive runs. Every real bug this phase surfaced — one genuine pipeline regression (`schema_minification`'s tool-reuse filter) and four grading-harness gaps — was found via a real run, root-caused, fixed, unit-tested, and reconfirmed live before being called done. Next up: Phase 10 (Tier 2 extras), gated on Phase 9 having produced real numbers, which it now has.

## Phase 10 (Tier 2 extras)

Three independent, opt-in plugins layered on the frozen, benchmarked Tier 0 core — per the brief, there's no fixed *ordering* requirement between them the way Tier 0's 11 stages have (CLAUDE.md's pipeline-order lock is a Tier 0 constraint), but they still need to be built one at a time. Build order below is a sequencing choice, not a brief requirement — starting with the smallest, dependency-free one first to validate the whole "opt-in extra" pattern (new `pyproject.toml` extras group, fail-open on unavailability, its own benchmark separate from Phase 9's suite) before taking on the two that need heavy new dependencies.

**Non-negotiables carried over from CLAUDE.md, apply to all three:**
- Tier 2 dependencies (`torch`, `transformers`, embedding models, vector stores) live behind their own extras group — never a base-install dependency, never imported at module load time outside that extra's own code path.
- Each is fail-open to plain Tier 0 behavior (or a normal, uncompressed/uncached API call) if its dependency isn't installed or its runtime fails — same fail-open discipline as every Tier 0 stage, not a lighter bar just because it's optional.
- Each ships its *own* benchmark, separate from Phase 9's corpus/quality-check suite (which stays Tier-0-only) — Phase 9's suite is not extended to cover these; a new, extra-specific measurement each time.
- Tested only under their own extras install (`pip install tokenetics[compress]` etc.) — never pulled into the core `pytest` run, so a base install's CI never needs `torch`.

### 10a. TALE-style budget estimation — DONE (2026-08-10)
- [x] `src/tokenetics/extras/tale.py` — `estimate_token_budget(client, messages, model, system, estimation_model)`: a small, cheap side-call (defaults to `claude-haiku-4-5`, cheaper than the target model) asks for a single-integer output-token estimate for the specific upcoming request. Deliberately lives outside Tier 0 (a plain function the caller runs themselves before `prepare()`) rather than as a pipeline stage — Tier 0's `prepare()` must stay network-free (CLAUDE.md), so the actual LLM call can't live inside a stage's `run()`. Returns `TaleEstimate(budget_tokens, estimation_input_tokens, estimation_output_tokens)`, the latter two tagged `measured` (from real `response.usage`) so the round-trip cost can be honestly weighed against what the tighter budget saves, per CLAUDE.md's measured/estimated discipline.
- [x] `src/tokenetics/stages/adaptive_budget.py` extended: `config["external_budget_estimate"]` (an int from TALE, or anywhere else) now feeds into the estimate source when present — same safety margin and `truncation_stats` widening still apply on top of it. Same caller-supplied-input shape as `truncation_stats`/`cache_usage_history`/`previous_payloads` already use, so this required no change to the stage's fundamental contract. Notably, an external estimate works even when `task_type` is unclassified (unlike the heuristic, which requires a known task type).
- [x] Fail-open (per CLAUDE.md's "hard error" flavor): `estimate_token_budget()` catches any exception from the API call and returns `None` rather than raising, and separately returns `None` if the reply has no parseable integer — either way the caller falls back to `adaptive_budget`'s existing free heuristic untouched. Tested directly: API failure, unparseable reply, and a reply with extra prose around the number (`"Approximately 400 tokens."` still parses to 400) all covered.
- [x] **Real bug caught via the user's own live `--tale` run (2026-08-10), fixed same day**: the original design let `external_budget_estimate` fully *replace* the heuristic. A real run against a `code`-classified request had TALE estimate 150 tokens for the visible answer (-> 180 after the safety margin) — but `adaptive_budget` also turns on high-effort adaptive thinking for that same classified task type, and thinking tokens draw from the *same* `max_tokens` budget, which TALE's estimation prompt has no way to know about. The real reply came back as a bare `ThinkingBlock` with **no visible text at all** — the 180-token cap was entirely consumed by thinking, leaving zero room for the answer. The free heuristic's 960-token code estimate (used in the same-scenario, no-`--tale` comparison run right after) had headroom for both and produced a full 913-token answer. This directly violated CLAUDE.md's conservative default ("uncertain output size -> widen max_tokens, not narrow it") — an opt-in estimate source made the *known-safe* heuristic path actively unreachable when it undershot. **Fixed**: when a classified task type also has a heuristic value available, `_apply_max_tokens` now takes the wider of the external estimate and the heuristic, never a blind replacement — an external estimate can still widen further than the heuristic when it's genuinely larger, but it can no longer narrow below what the heuristic alone would have provided. `max_tokens_widen_reason` now reports whichever source actually won (`"external_estimate"` or `"task_type_estimate"`).
- [x] Tests: `tests/test_tale.py` (7 tests, offline via a fake client — clean parse, prose-wrapped parse, unparseable fail-open, API-failure fail-open, cheaper-estimation-model-by-default, prompt correctly includes the real model/conversation/system prompt) and `tests/test_adaptive_budget.py` (7 tests covering the external-estimate path, including two regression tests for the real bug above: a too-small external estimate for a classified task type is capped at the heuristic's value, and a genuinely larger external estimate still wins over the heuristic).
- [x] Wired into `scripts/dev_demo.py` for hand-checkable, incremental runnability per CLAUDE.md: new `--tale` flag (single-scenario only) runs the real TALE side-call, prints its estimate and measured cost, and feeds it into `stage_config["adaptive_budget"]`. The existing per-stage log already prints `max_tokens_widened_to`/`max_tokens_widen_reason`, so running the same scenario with and without `--tale` gives the head-to-head comparison by hand (`uv run python scripts/dev_demo.py --scenario code --tale`) — this exact comparison is what surfaced the bug above.
- [x] 253 tests pass (was 221 before Phase 10), ruff clean, mypy clean.
- [ ] **Not yet done**: the *rigorous* head-to-head benchmark (systematic, corpus-style, across many real requests, comparing `max_tokens` tightness and truncation rate at scale) — `--tale` gives a real, hand-checkable single-request comparison, but a proper benchmark write-up needs a dedicated real-API run across a sample set, deferred until 10b/10c's scope is also decided so all three extras' benchmarks can be planned together.

### 10b. LLMLingua-style compression — DONE (2026-08-10)
- [x] `pyproject.toml`: new `tokenetics[compress]` extras group (`torch>=2.0`, `transformers>=4.40`) — confirmed absent from the base install (both imports are deferred to inside function bodies in `extras/compress.py`, never at module load time; `uv sync` without `--extra compress` never touches them).
- [x] `src/tokenetics/extras/compress.py` — `Compressor` (caller-constructed, caches its loaded model across calls, since loading a causal LM per call would be far too slow) and a one-shot `compress_text()` convenience wrapper. Default model `distilgpt2`.
- [x] **Mechanism, word-granularity, not raw sub-token dropping**: each token is scored by its own negative log-likelihood given prior context (high loss = surprising = informative = keep; low loss = predictable = redundant = safe to drop) via one real forward pass through the causal LM. Scores are aggregated per whitespace-delimited word (mean of that word's sub-token losses), not dropped at the raw BPE sub-token level — sub-token-level removal doesn't reassemble into readable text (a word's fragments don't survive losing one piece). Top `(1 - ratio)` fraction of words by score are kept, **in original order** (a selection, not a reordering) — verified directly (`test_kept_words_preserve_original_relative_order`, a subsequence check against the original text).
- [x] Lossy by design, documented honestly in the module docstring as trading fluency for token count — this is NOT a literal LLMLingua reimplementation (no iterative budget controller, no bidirectional context re-scoring), just the same underlying "perplexity-based importance + budget-based dropping" mechanism the brief describes, at a scope proportionate to Phase 10.
- [x] Fail-open: empty text, an out-of-range ratio (must be in `(0.0, 1.0)`), a failed model load, and a failed inference/scoring pass all return `CompressResult(success=False, compressed_text==original_text_unchanged)` — never raises, logged at error level. Tested directly against a real nonexistent-model-name failure (not mocked).
- [x] Tests: `tests/test_compress.py`, 8 tests, run for real against `distilgpt2` (not mocked) — real compression achieves the requested ratio within tolerance, kept words are provably a subsequence of the original (order preserved), at-least-one-word-always-kept on an extreme ratio, all four fail-open paths (empty text, ratio=0.0, ratio=1.0, unknown model name), and that a `Compressor` instance genuinely reuses its loaded model across two `.compress()` calls.
- [x] Tested only under `tokenetics[compress]` installed — guarded by `pytest.importorskip("torch")`/`("transformers")`, so a base install's test run skips these cleanly rather than failing.
- [ ] **Not yet done, flagged explicitly**: the brief's required quality-degradation benchmark (reduction-vs-accuracy curve at several ratios, real completions, held-out set) — this needs billed API calls under a cost ceiling, same shape as Phase 9's quality-check set; deferred as its own follow-up rather than folded into "implement the mechanism," consistent with how 10a's rigorous head-to-head benchmark was also deferred.

### 10c. Semantic caching — DONE (2026-08-10)
Dependency choice resolved with the user: **`sentence-transformers`** (`all-MiniLM-L6-v2`) for embeddings, plus a **minimal in-memory cosine-similarity store** — not chromadb/faiss — per the brief's "lightweight local vector store" language and to keep the fail-open surface to one dependency, not two.
- [x] `pyproject.toml`: new `tokenetics[semantic-cache]` extras group (`sentence-transformers>=3.0`) — confirmed absent from the base install (deferred import inside `extras/semantic_cache.py`, same pattern as `compress.py`).
- [x] `src/tokenetics/extras/semantic_cache.py` — `SemanticCache`, a caller-constructed, caller-held object (Tier 2's one genuinely *stateful* piece — a request/response cache has to persist between calls by definition, which is why it lives here rather than contradicting Tier 0's statelessness). Methods: `lookup(query) -> CacheLookupResult`, `store(query, response)`, `invalidate(query) -> int`, `clear()`, `__len__()`.
- [x] Mechanism: embeds the query text via `sentence-transformers`; cosine-similarity lookup against the in-memory store of previous (query, response) pairs; above the similarity threshold, returns the cached response and reports the real match score.
- [x] **Threshold empirically measured, not guessed (0.90 cosine)**: ran the real `all-MiniLM-L6-v2` model against hand-built same-intent paraphrase pairs ("What's the capital of France?" / "What is the capital city of France?") vs. genuinely-different near-miss pairs on related topics ("How do I reset my password?" / "How do I reset my email password?" — a different question). Same-intent pairs scored 0.92-0.99; the worst near-miss scored 0.87 — a clean gap, with 0.90 sitting in the middle with margin both directions. Small hand-built sample, not a rigorous benchmark yet — flagged below as the next thing to validate at scale.
- [x] TTL expiry (`ttl_seconds`, lazily swept on `lookup()`/`__len__()`) + `invalidate()`/`clear()`.
- [x] Match-quality (the real similarity score) reported on every lookup, hit or miss — a near-miss that stayed just below threshold is exactly the case worth auditing later, not just confirmed hits.
- [x] Fail-open: embedding-model load failure, embedding-call failure, and similarity-computation failure all return a clean miss from `lookup()` and a silent no-op from `store()` — never raises. Tested directly against a real nonexistent-model-name (not mocked).
- [x] Tests: `tests/test_semantic_cache.py`, 10 tests, run for real against `all-MiniLM-L6-v2` (not mocked) — empty-cache miss, genuine-paraphrase hit, **the exact near-miss case the threshold was tuned against correctly misses** (the single most important test in this file — proves the false-positive risk this whole module exists to manage is actually being managed), unrelated-query miss, best-match selection among multiple stored entries, `invalidate()`/`clear()`/TTL expiry, and the model-load-failure fail-open path.
- [x] Tested only under `tokenetics[semantic-cache]` installed — guarded by `pytest.importorskip("sentence_transformers")`.

**Both installed together (2026-08-10)** as one deliberate dependency decision, per the plan: `uv sync --extra compress --extra semantic-cache` — 32 packages, confirmed the base `pytest` run (232 tests, pre-existing) is unaffected and extras tests (18 more) pass for real against the actual models, not mocks. mypy's `python_version` raised from 3.10 to 3.12 project-wide to work around a known numpy-stub/mypy syntax incompatibility (PEP 695 `type` aliases in numpy 2.x's bundled stubs) — this only changes what typing syntax mypy assumes is available while checking; the project's real supported-runtime floor stays `requires-python = ">=3.10"`, enforced at install time, unaffected by this setting. 250 tests passed at this point, ruff clean, mypy clean — later grew to 253 after the real TALE bug (below) added 3 regression tests.

### 10c's at-scale threshold validation — DONE (2026-08-10)
- [x] `benchmarks/semantic_cache/near_miss_set.json`: 12 stored FAQ-style entries across distinct domains (password reset, business hours, refunds, shipping, subscriptions, geography, biology, Python data structures, weather, WiFi setup, flu symptoms) + 30 test queries (14 genuine paraphrases with a labeled correct-match id, 16 near-miss/unrelated queries expected to miss) — much larger and more realistic than the original 4-pair hand-built sample (a shared multi-topic cache, so this also exercises cross-domain confusion, not just isolated pair similarity).
- [x] `scripts/semantic_cache_benchmark.py` — free, entirely local (no network calls, no cost gating needed unlike `compress_benchmark.py`/`quality-check`), loads every stored entry into one shared `SemanticCache`, runs every test query, and reports recall (correct hits / genuine paraphrases) and false-positive rate (false hits / should-miss queries) separately, plus every individual miss/false-positive/wrong-match printed for inspection.
- [x] **Real result, run for real against `all-MiniLM-L6-v2` (2026-08-10)**: **0.0% false-positive rate (0/16)** — the 0.90 threshold is genuinely safe at this scale, no should-miss query incorrectly hit, including several structurally close near-misses ("pause subscription" vs. "cancel subscription" at 0.861, "reset password" vs. "change password" at 0.874). **64.3% recall (9/14)** — five genuine paraphrases were missed, mostly ones with very different surface structure from the stored query despite matching intent (e.g. "When are you open?" vs. "What are your business hours?" scored only 0.508). This is an honest, real tradeoff, not a bug: the threshold is safe in the correct direction (never wrong-answers a user) at the cost of some real missed cache hits. **Deliberately left unchanged**: the data hints a marginal lift is available (moving the threshold to ~0.88 would correctly hit one more paraphrase, "How do I stop my subscription?" at 0.884, without crossing the highest near-miss score of 0.874) — not applied, since a 30-query sample is too small to safely tune a threshold whose whole job is avoiding false positives, and CLAUDE.md's conservative-by-default rule favors staying put over squeezing out marginal recall from limited data. A real deployment with its own traffic is the right place to make that call, not this benchmark.
- [x] `tests/test_semantic_cache.py`: added a data-integrity test for the near-miss set (no duplicate stored-entry ids, every `expected_hit_id` resolves to a real stored entry) — catches a malformed dataset at test time, not mid-benchmark-run.

### 10b's quality-degradation benchmark — DONE, including a default safety cap (2026-08-10)
- [x] `benchmarks/compress_quality/passages.json`: 4 real passage+question samples across distinct domains (an earnings report, a biology comparison, a historical treaty, a software changelog), each with `required_elements` (specific facts — dollar figures, cell structures, a date and two city names, config values) that must survive compression to answer correctly. One sample's required element (`"2-second"` vs. `"2 second"`) uses the OR-group `required_elements` support added during Phase 9's `adaptive_budget_001` fix, reused here rather than reinvented.
- [x] `scripts/compress_benchmark.py` — cost-ceiling-gated real-completion benchmark, same `--dry-run`/`--confirm-spend`/`--cost-ceiling` contract as `benchmark_runner.py quality-check`. For each sample, compresses the passage locally (free) at each ratio in `_RATIOS`, sends one real completion per (sample, ratio) with the passage + question, and grades the response with Phase 9's `check_required_elements`/`response_text_with_tool_inputs` (the same authoritative gate, a different corpus). Reports the reduction-vs-accuracy curve: pass rate per ratio, alongside the REAL achieved word-reduction at that ratio.
- [x] **Run 1 (2026-08-10, $0.0199, ratios `[0.0, 0.2, 0.4, 0.6]`) caught a real rubric bug, same class as Phase 9's**: `compress_quality_003` failed at EVERY ratio, including `0.0` (the uncompressed baseline) — impossible to be a compression-quality issue if the baseline itself fails.
- [x] **Run 2 (same ratios, after adding response-text-and-passage-on-failure printing to the script)** diagnosed it: Claude correctly answered with the historically accurate umlauted spelling ("Osnabrück", "Münster") regardless of the plain-ASCII spelling in the fed passage, while `required_elements` only checked the ASCII form. Also confirmed, from the same run: the earnings-report and changelog samples genuinely lost their required numeric facts from the compressed text at ratio 0.6 (`present in passage sent: False`), and the model *honestly* reported the fact wasn't present rather than hallucinating — real content loss, not a rubric issue. Fixed the rubric with an OR-group (`["Osnabruck", "Osnabrück"]`, `["Munster", "Münster"]`), verified directly against the already-captured real response strings before spending money again.
- [x] **User asked (2026-08-10) whether the tool should cap around 35-40%, or find the real limit first** — chose to find it: re-ran with `_RATIOS` narrowed to `[0.0, 0.4, 0.45, 0.5, 0.55, 0.6]` (0.4/0.6 as the known "fine"/"broken" endpoints from runs 1-2, three new points between them), at $0.0298. **Run 3 is the final, trustworthy result, and also confirms the rubric fix holds**: 100% pass at 0.4, **first crack at 0.45 (75%)**, 50% by 0.5, down to 25% by 0.55-0.6. The cliff starts almost immediately past 0.4, not gradually — 0.4 is the last ratio with a clean 100% pass rate in this held-out set.
- [x] **Implemented a default safety cap based on this exact result**: `RECOMMENDED_MAX_RATIO = 0.4` in `compress.py`. `Compressor.compress()`/`compress_text()` now clamp any requested ratio above it down to 0.4 by default (info-level log), matching CLAUDE.md's conservative-default doctrine ("wherever a stage has a choice between an aggressive and a safe option, default to safe") — the same idiom already used elsewhere in the project (structured_output respects a caller's own `tool_choice`, adaptive_budget respects a caller's own `thinking` config): safe by default, explicit caller opt-out available via `allow_above_recommended_max=True`. `CompressResult` gained `requested_ratio`/`clamped` fields so callers (and `dev_demo.py --compress-ratio`, updated to print when clamping occurs) can see when this happened. `compress_benchmark.py` itself passes `allow_above_recommended_max=True`, since its entire job is testing past the cap. 3 new tests in `test_compress.py` (clamped by default above the cap, not clamped at/below it, override opts out) — all run for real against `distilgpt2`.
- [x] The full finding (cliff location, root cause, and the cap this led to) is documented directly in `compress.py`'s own module docstring and `RECOMMENDED_MAX_RATIO`'s comment, not just here, per CLAUDE.md's "document where a stage can go net-negative" rule.
- [x] Ruff/mypy clean, 259 tests pass throughout.

### Mixed-workload benchmark scenario — DONE (2026-08-10, deferred from Phase 9)
- [x] `benchmarks/corpus/mixed_workload.json`: 2 samples, each a single realistic multi-turn session spanning several task types in one conversation (small talk → code debugging → a tool call → near-duplicate small-talk padding → an extraction request, and a differently-ordered second sample: extraction → code → near-duplicate padding → a tool call) — deliberately not a single task-classifier category.
- [x] Reported as its own explicitly-labeled `mixed_workload` bucket by `benchmark_runner.py corpus`, never blended into the 4 categorized ranges: `aggregate_savings_by_category` already groups generically by category string, so this required no core logic change — just excluding `mixed_workload` from the "clears CLAUDE.md's 15-20+/category floor" claim (which is specifically about the 4 real task-type categories) and printing it as a separate, explicitly-caveated line instead. `--category mixed_workload` also works as a corpus filter now.
- [x] The near-duplicate padding pairs in both samples were empirically verified to actually trigger `near_dup` through the real pipeline (not just an isolated exact-Jaccard check, which turned out to be a meaningfully looser proxy than the stage's real 32-hash MinHash estimate — several candidate sentence pairs that cleared 0.95 exact Jaccard still failed to merge for real, so the final text was iterated against the actual `_minhash_signature`/`_estimate_similarity` functions until it reliably cleared). Confirmed: `mixed_workload_001` 2315→1787 tokens (22.8%, 2 merges), `mixed_workload_002` 2383→1855 tokens (22.2%, 2 merges) via `near_dup`, with `dedup` correctly doing nothing (the pairs are near-duplicates, not exact).
- [x] `tests/test_benchmark.py`: `test_the_real_corpus_directory_parses_and_covers_all_four_categories` changed from exact category-set equality to a subset check (mixed_workload is a legitimate 5th category, not one to break the 4-category assertion); new `test_the_real_corpus_directory_has_a_mixed_workload_bucket` and `test_mixed_workload_samples_actually_engage_near_dup`.

### Both extras wired into `dev_demo.py` and confirmed against the real API (2026-08-10)
- [x] `--compress-ratio RATIO`: locally compresses the latest user turn's text (free, no network) before the real completion, prints original/compressed text and the real achieved ratio; prints a note when the `RECOMMENDED_MAX_RATIO` clamp fires.
- [x] `--semantic-cache`: demonstrates the full `store()`/`lookup()` round trip within one run — a first lookup against an empty cache (expected miss), the real completion, `store()` of the real reply, then a second lookup with the same query (expected hit) — self-contained since the script has no cross-run persistence to demo a hit against a previous invocation.
- [x] **Confirmed for real by the user (2026-08-10)**: `--scenario code --compress-ratio 0.7` correctly clamped to 0.4 (achieved 0.38) and printed the clamp note; `--scenario long_history --semantic-cache` correctly missed on the empty cache, then hit with similarity ≈1.0 after `store()`. Every Phase 10 extra (10a/10b/10c) is now confirmed against the real API, not just unit-tested.
- [x] **`mixed_workload` added to `dev_demo.py`'s `SCENARIOS` (2026-08-10)**, prompted by the user asking whether a mixed scenario existed to test — it did, but only in `benchmarks/corpus/` (free, `prepare()`-only, no real reply). Added a `dev_demo.py` scenario using the exact same content as `mixed_workload_001` (not a new sample -- reusing already-validated text means one less thing to independently verify), so `--scenario mixed_workload` now shows the real completion the corpus version can't. Offline-confirmed identical `near_dup` merge behavior (`2315 -> 1787` tokens, same 2 merges) to the corpus sample via a stub client, before handing off for the real-API hand-check (this sandbox has no `ANTHROPIC_API_KEY`).
- [x] **Real hand-check revealed a genuine "why so short" moment, not a bug**: `mixed_workload`'s real reply was a terse 47-token JSON blob, because its FINAL turn is a narrow single-purpose extraction request -- the model correctly answers only the newest unanswered turn, same as any multi-turn chat, not a re-synthesis of the whole conversation. The user then asked for a variant that forces genuine engagement across the whole conversation. **Added `mixed_workload_003`** (corpus) and its matching `dev_demo.py` scenario **`mixed_workload_recap`**: same tool/code/extraction ingredients as `mixed_workload_001`, plus a genuine conversational exchange (a weekend-activity question, not just check-in filler) and the previously-final extraction request now answered mid-conversation, ending instead on an open-ended "recap everything we covered" question — forcing the real reply to synthesize across small talk, code, a tool result, and the extraction, rather than answer one isolated task. Offline-verified `near_dup` still catches the same padding pair even repositioned mid-conversation (`2966 -> 2438` tokens, same 2 merges), and that the corpus sample and `dev_demo.py` scenario are byte-identical in content. `test_mixed_workload_samples_actually_engage_near_dup` extended to cover all 3 samples; `mixed_workload` bucket is now 3 samples.
- [x] **Confirmed for real by the user (2026-08-10)**: `--scenario mixed_workload_recap` produced a genuine synthesis reply correctly recalling all 4 topics (weekend suggestion, bug fix with the actual code quoted, weather, order extraction JSON) despite the `near_dup` merge having removed one copy of the padding from history — nothing dropped, nothing hallucinated. Also incidentally the first real confirmation of `post_hoc_trim` actually firing on a hand-checked scenario (`241 -> 229 tokens, boilerplate_removed: 1`, stripping a "Let me know if you need anything else!" sign-off before storage) — none of the other `dev_demo.py` scenarios' replies had previously triggered it.

259 tests pass (was 253), ruff clean, mypy clean.

## Phase 11 (dashboard) — DONE (2026-08-12)

Per the brief: "read-only consumer of the cost logger's structured log, fully decoupled from the request path (can crash without affecting requests)... v1 form: start with the simpler of CLI table or small local web view." User chose the web view. Swapped ahead of Phase 12 (distribution) on 2026-08-10 — see the "Dependencies to respect" note below for why.

- [x] **Gap found before building anything**: nothing in the project persisted `CostLogger` entries anywhere — `InMemoryCostLogger` only lives for the lifetime of one `Tokenetics()` instance. The brief's "structured log" for a dashboard to read didn't exist yet, so this phase is really two pieces, not one.
- [x] **`FileCostLogger`** (`src/tokenetics/core/logger.py`) — a new `CostLogger` implementation (same protocol, zero changes to the frozen `Tokenetics.__init__`/`prepare()`/`finalize()` API) that appends each `log_stage()` call as one JSON line to a file. Every entry from one instance shares a `run_id` (a UUID generated once at construction) — since the project's own convention is one fresh `Tokenetics()` per request, this groups a request's stage entries together for later aggregation without needing any orchestrator changes.
- [x] **`src/tokenetics/dashboard/`** (new package, three modules, deliberately separated so each is independently testable):
  - `aggregate.py` — `load_entries()` (reads the JSONL file, skips malformed lines rather than raising, matching the "must stay decoupled" requirement) and `aggregate()` (groups by `run_id` for per-request overall before/after, plus a flat per-stage rollup: times seen/enabled/errored, token totals, avg timing). Pure functions, no I/O beyond the read, no network.
  - `render.py` — renders `DashboardStats` as one self-contained HTML page (inline CSS, no JS framework, no charting library) — summary cards + a per-stage table with an inline-CSS savings bar. Log path is HTML-escaped. An empty-state view prints the exact `dev_demo.py --log-to` command to generate data if none exists yet.
  - `server.py` — stdlib `http.server.ThreadingHTTPServer` only, no new dependency (deliberately not Flask/FastAPI, matching how carefully this project scopes dependencies everywhere else). Re-reads and re-aggregates the log file fresh on every request — no cached/shared mutable state between requests, so a second process appending to the same log file shows up on the next page load.
- [x] **`scripts/dashboard.py`** — CLI launcher (`--log-file`, `--port`, `--host`). Unlike `dev_demo.py`/the benchmark scripts, this makes zero network calls of its own (pure local file read + local HTTP serve), but stays a script rather than pytest since it's a long-running server.
- [x] **`--log-to PATH` wired into `dev_demo.py`** for incremental runnability — swaps in a `FileCostLogger` instead of the default in-memory one for a single-scenario run, prints the `run_id` so it's traceable. This is how the dashboard actually gets fed real data.
- [x] **First-pass honest scope note (superseded below)**: v1 surfaced per-stage token savings, timing, and enabled/error state, but explicitly did NOT show cache hit rate or Tier 2 cost-benefit, since neither was logged through `CostLogger` anywhere. Flagged in the rendered page's own footer rather than silently omitted.
- [x] Tests (first pass): `tests/test_logger.py` extended (4 new) and `tests/test_dashboard.py` (14 new), including **two real loopback HTTP round-trip tests** against an actual running server.
- [x] Manually smoke-tested the actual `scripts/dashboard.py` CLI end-to-end (not just the underlying modules) — real background process, real `curl` against it, confirmed rendered content.

### Second pass (2026-08-12): closed the cache hit rate / Tier 2 cost-benefit gap

User asked directly how to fix it, then said to go ahead. Root cause: `log_stage()` only fires *inside* `Tokenetics.prepare()`/`finalize()`, but the missing data (real `usage.cache_read_input_tokens`, TALE's round-trip cost, compress's achieved ratio, semantic-cache's hit/miss) all happens OUTSIDE the pipeline entirely — in the caller's own `messages.create()` call, or in a separate Tier 2 extras call. Nothing reported it back anywhere. Fixed with a second write path into the same log file, not a pipeline change:

- [x] **`CostLogger.log_event(event_type, **fields)`** added to the Protocol and all three implementations (`InMemoryCostLogger` stores an `events` list; `FileCostLogger` appends a `"type": "event"` JSONL record tagged with the same `run_id` as its `log_stage()` calls; `NullLogger` no-ops). `log_stage()` records now also carry `"type": "stage"` explicitly (records with no `"type"` at all, i.e. any pre-existing log file, are still treated as `"stage"` for backward compatibility).
- [x] **`aggregate.py`**: `aggregate()` now explicitly filters to `"type": "stage"` records (previously relied on stage-only fields being implicitly absent from other record shapes — still correct, but now explicit rather than incidental). New `aggregate_events()` + `Tier2Stats`/`CacheUsageStats`/`TaleStats`/`CompressStats`/`SemanticCacheStats` dataclasses compute: cache hit rate (`cache_read / (cache_read + cache_creation)`), TALE's avg budget estimate and avg round-trip cost, compress's clamp rate and avg achieved ratio, semantic-cache's real hit rate. Unknown `event_type` values are silently ignored (forward-compatible), matching `load_entries()`'s existing "never crash the dashboard" contract.
- [x] **`render.py`**: new Tier 2 section, one sub-card group per mechanism, each independently showing real numbers or an honest "No X data logged (needs --flag)" placeholder — never a fabricated zero passed off as a real measurement. `render_html()` gained an optional `tier2: Tier2Stats | None` parameter; the empty-state check now also considers Tier 2 data, so a log file with only `log_event()` calls (no stage entries) doesn't wrongly show "no runs logged yet."
- [x] **`server.py`**: now calls both `aggregate()` and `aggregate_events()` per request and passes both to `render_html()`.
- [x] **`dev_demo.py`**: `logger` construction moved earlier (was created right before `Tokenetics()`, now created right after `request_kwargs`) so the Tier 2 blocks — `--compress-ratio`/`--semantic-cache`/`--tale`, which all run *before* the pipeline — can also call `log_event()` against the same `run_id`. Wired at all four sites: `--compress-ratio` (`requested_ratio`/`ratio_achieved`/`clamped`), `--semantic-cache` (only the FIRST lookup, before `store()` — the realistic "would this have skipped the API call" signal; the second demo lookup after `store()` is intentionally not logged to stats, since it only proves the mechanism works, not a real hit-rate data point), `--tale` (`budget_tokens`/`estimation_input_tokens`/`estimation_output_tokens`), and the real completion's `usage` object (`cache_read_input_tokens`/`cache_creation_input_tokens`, logged whenever `--log-to` is set and a response comes back — including honest zeros when no caching occurred, not just non-zero hits).
- [x] Tests: 5 new in `tests/test_logger.py` (`log_event()` on all three logger types, `"type"` tagging, shared `run_id` between `log_stage()`/`log_event()` calls) and 12 new in `tests/test_dashboard.py` (`aggregate_events()`'s math for all four mechanisms, unknown-event-type and stage-record-ignored edge cases, `Tier2Stats.has_any_data`, `render_html()`'s real-numbers-vs-placeholder rendering and the events-only non-empty-state edge case, and a real loopback HTTP test confirming Tier 2 data actually renders through an running server).
- [x] Manually smoke-tested the real `scripts/dashboard.py` CLI end-to-end with real Tier 2 event data (not just stage data) — background process, real `curl`, confirmed cache hit rate and semantic-cache stats rendered correctly while untouched mechanisms (TALE, compress) correctly showed "no data logged."
- [x] 293 tests total, ruff clean, mypy clean.

**Not yet done**: nobody has run this against the real API yet with `--log-to` plus `--measure-usage`/`--tale`/`--compress-ratio`/`--semantic-cache` together to see genuine Tier 2 numbers on the dashboard (only smoke-tested with synthetic event data). See the handed-back commands below.

### Third pass (2026-08-12): fixed a real negative-savings bug caught via a live dashboard run

User ran the handed-back commands for real, got a genuinely confusing result: `-8.0%` overall "savings" with clean data (no crashed-run contamination — `post_hoc_trim`'s run count matched every other stage's, ruling out the artifact from the previous round). Root cause, diagnosed from the actual numbers rather than guessed: `post_hoc_trim` is the one response-side stage — it measures the **reply's** token count via `count_text_tokens`, a completely different axis from every other (request-side) stage, which measures the **request's** token count via `count_tokens`. `aggregate()`'s per-run "overall savings" picked "first stage's `tokens_before` -> last stage's `tokens_after`" without distinguishing the two — and since `post_hoc_trim` runs last (in `finalize()`, after `prepare()`'s 11 request-side stages), any run that called both ended up comparing **request size before optimization** against **reply length**, not request-before vs. request-after. Real replies are routinely longer in tokens than an optimized request, which is exactly why this trended negative — it was never measuring real token reduction at all.

- [x] **`orchestrator.py`**: `_run_stage`'s two `log_stage()` calls now tag `phase="request"`; `_run_response_stage`'s two calls tag `phase="response"`. Lands in `extra` (the existing `**extra` kwarg passthrough), no signature change to any logger method, no change to the frozen `Tokenetics.__init__`/`prepare()`/`finalize()` API.
- [x] **`aggregate.py`**: `aggregate()`'s per-run overall-savings calculation now only considers request-side entries. The per-stage rollup table (`stats.stages`) is untouched by this fix and was never actually wrong — each stage's own before/after was already self-consistent on its own axis; only the cross-stage summary was mixing axes.
- [x] **`render.py`**: the three overall summary cards relabeled "Request tokens before/after/saved" (was "Tokens before/after/Saved") — matching `dev_demo.py`'s own established "(request/input side, measured)" convention, specifically to stop this exact confusion from recurring now that the underlying math is correct.
- [x] Tests: `test_aggregate_overall_savings_ignores_response_side_stages` (reproduces the exact bug shape — a well-optimized request paired with a long, untrimmed reply — and asserts the overall numbers reflect only the request side). 296 tests total, ruff clean, mypy clean.
- [x] Hand-verified against synthetic data — but **the user then restarted the dashboard against their real, already-accumulated `costs.jsonl` and still saw the identical -8.0%**, disproving the fix on real data. Root cause of the fix itself failing: the first version defaulted ANY entry with no `phase` tag to `"request"` (for backward compatibility with log files predating this fix) — but the user's real `costs.jsonl` predated the fix ENTIRELY, so `post_hoc_trim`'s own entries had no `phase` tag either, and got defaulted straight back into `"request"`, silently undoing the fix for exactly the file it was meant to fix. A blanket default was the wrong kind of backward compatibility.
- [x] **Corrected same day**: the missing-`phase` fallback now identifies `post_hoc_trim` by **stage name** (`_KNOWN_RESPONSE_SIDE_STAGES = {"post_hoc_trim"}` — the only response-side stage that has ever existed in this project, so this is exact, not a guess) rather than defaulting everything to `"request"`. New regression test `test_aggregate_excludes_post_hoc_trim_even_when_untagged` reproduces the user's exact real scenario (an untagged log file). 297 tests total, ruff clean, mypy clean.
- [x] Re-confirmed against the real CLI **with the exact untagged-log-file shape the user actually hit** (not just tagged synthetic data this time): correctly reports the request-side numbers only.

**Phase 11 is now genuinely done** — all three real bugs this phase's actual real-world usage surfaced (the `finalize()` trailing-whitespace crash, the cross-axis aggregation bug, and the flawed backward-compatibility fallback that initially failed to fix it on real data) were caught via the user hand-running the tool against real data and reporting back exactly what they saw, not found in review or synthetic tests alone — including one case where a fix that passed its own test and looked correct on synthetic data still didn't work on the real file it was meant to fix, until the user re-ran it for real and reported the unchanged result. That's exactly why the "verify against real data, not just synthetic tests" discipline matters.
- [x] 276 tests total, ruff clean, mypy clean.

## Full real-dollar usage benchmark (`benchmark_runner.py usage`) — built 2026-08-12

Prompted by a real finding from `dev_demo.py --all-scenarios --measure-usage`: input tokens saved (2874) but output tokens cost more (2220, priced ~5x higher for Sonnet 5), and the combined net across that batch was a real **-$0.014 (a net cost, not a saving)**. But `dev_demo.py`'s scenario set is ~11 stage-demonstration fixtures deliberately biased toward triggering every mechanism at once (several are specifically built to force `adaptive_budget`'s high-thinking-effort path) — not a representative sample, so that result couldn't be trusted as *the* answer, only as evidence a proper measurement was needed.

- [x] **New `usage` subcommand added to `scripts/benchmark_runner.py`**, reusing the already-vetted 82+3-sample corpus (organized for "genuine variety, not padding" since Phase 9) instead of `dev_demo.py`'s biased fixture set — a much better representative base for this measurement.
- [x] For each sample: a real baseline completion (raw request) and a real pipeline-enabled completion, `response.usage`-measured, run through `_full_cost_usd()` — the piece that never existed before: input, output, AND cache tokens each weighted by their own real per-token price from `core.cache_pricing`'s live-verified table, combined into one true dollar figure per call. (`dev_demo.py`'s own cost verdict only ever priced the cache portion in isolation, never combined with the regular input/output cost — insufficient to answer "did this call get cheaper or more expensive overall.")
- [x] For any sample where the pipeline call actually wrote to cache, one additional (cheap) repeat call captures the real steady-state cache-READ cost — reported separately from the main totals, not blended in, since real traffic is mostly repeat calls, not perpetual first-calls, and blending would misrepresent both numbers.
- [x] Reports real $ savings as a **min-max range per category** (never a single flat percentage, same discipline as `corpus`), plus an explicitly-labeled overall total that states its own weighting assumption ("equal per-sample weighting -- NOT a claim about real traffic proportions") rather than presenting one number as universally representative.
- [x] Always prints the raw input/output token deltas alongside the dollar verdict, specifically so the "a token-count view and a dollar view can disagree" finding stays visible per run, not just in this one write-up.
- [x] Cost-ceiling-gated identically to `quality-check` (`--dry-run`/`--confirm-spend`/`--cost-ceiling`, refuses to run without `--confirm-spend` regardless of estimate size). Dry-run confirmed: **$0.31 for the full 83-sample corpus** (166 completions), comfortably under the default $1.00 ceiling — no need to raise it.
- [x] `_full_cost_usd()`'s arithmetic verified by hand against 3 manually-computed cases (no-cache, cache-write, cache-read) before spending real money on it — all three matched exactly. No pytest coverage added for the script itself, consistent with this project's established convention that `scripts/*.py` files are validated via `--dry-run`/manual smoke-testing, not direct unit tests (the underlying pricing logic it calls, `core.cache_pricing`, already has full test coverage from Phase 6/9).
- [x] Ruff/mypy clean; full 297-test suite unaffected (no source module changes, only a new script).

- [x] **Real run happened (2026-09-04, run by the user)** — and it landed on a genuinely important finding, not just a number:

| Category | Real $ range | Net |
|---|---|---|
| **code** | -137.1% to +25.7% | **$0.082 → $0.131 (≈59% MORE expensive)** |
| **conversational** | -19.7% to -9.8% | **$0.062 → $0.073 (≈19% MORE expensive)** — all 20/20 samples lost money |
| extraction | -6.9% to +14.8% | $0.0416 → $0.0427 (≈3% more expensive, roughly neutral) |
| **tool-heavy** | -20.5% to +42.0% | **$0.046 → $0.042 (≈9% cheaper)** ✅ |
| mixed_workload | +10.1% to +13.0% | $0.0101 → $0.0089 (≈12% cheaper) ✅ |
| **TOTAL (83 samples, equal weighting)** | | **$0.242 → $0.298, net -23.0% (MORE EXPENSIVE overall)** |

Context printed alongside: input tokens saved 333 total (negligible) vs. output tokens cost 5,203 total (priced ~5x input for Sonnet 5) — that ratio is the whole story.

**Root cause, identified from the pattern in the per-sample data, not guessed**: 7 of the 20 `code` samples clustered tightly around -130% to -137% (not random variance — a systematic on/off signature), while 6 others sat at exactly +0.0% (pipeline untouched). Same story in `conversational`: 18 of 20 samples landed within 0.3 points of -19.6%. That signature pointed straight at `adaptive_budget`'s `_EFFORT_BY_TASK_TYPE` mapping (`code: "high"`, `conversational: "medium"`) — every request of those types got forced into higher-effort thinking automatically, and the extra output tokens that costs consistently outweighed everything else the pipeline saved. `tool-heavy`/`extraction` (both `"low"` effort) never showed this pattern, and `tool-heavy` came out net positive, driven by real structural savings (schema_minification, delta_compression) plus confirmed cache amortization (`tool_heavy_017`'s steady-state repeat call: $0.005065 → $0.001103, a real 78% drop on the second hit).

**Decision (discussed with the user, not unilateral)**: two options were on the table — lower the effort levels (e.g. `"high"` → `"medium"`), or stop auto-setting thinking effort entirely and require an explicit opt-in. Chose **opt-in**, for two reasons: (1) lowering the level is a guess that would need its own real-money re-test to validate, with no guarantee `"medium"` isn't *also* net-negative; (2) opt-in is the *consistent* choice, not a bigger departure — every other cost/benefit tradeoff in this project already defaults safe with explicit opt-in for aggressive behavior (`tale.py` is entirely opt-in; `compress`'s `RECOMMENDED_MAX_RATIO` clamps by default, needs `allow_above_recommended_max=True` to exceed). Automatic thinking-effort was the one place that didn't follow that pattern, and the real data is exactly why it should.

- [x] **Implemented (2026-09-04)**: `adaptive_budget.py`'s `_apply_thinking_effort` now requires `config["enable_thinking_effort"] = True` before it does anything at all — every existing skip condition (caller's own `thinking` config, unsupported model, unclassified task type) is unchanged, they just no longer fire without the flag either. Skip reason `"opt_in_not_enabled"` logged when it's off (the new default). Module docstring rewritten with the full finding + reasoning, not just the mechanics.
- [x] `dev_demo.py`'s `caller_sets_thinking` scenario updated to explicitly opt in via `stage_config` (it exists specifically to test "opted in, but caller's own config still wins" — without the opt-in flag that corner case wouldn't even reach the interesting code path). `code` scenario's description corrected to stop claiming thinking effort fires by default.
- [x] `benchmarks/quality_checks/adaptive_budget.json`'s `quality_adaptive_budget_001` (built specifically to test thinking effort's quality benefit) given `stage_config: {"adaptive_budget": {"enable_thinking_effort": true}}` — without it, the sample would silently stop testing what it exists to test. Offline-reconfirmed it still triggers `thinking_effort: "high"` with the flag set. New assertion in `test_real_quality_check_samples_actually_engage_their_mechanism` locks this in.
- [x] Tests: `tests/test_adaptive_budget.py` — every thinking-effort test updated to opt in explicitly (so each still tests what it claims to, isolated from the new opt-in gate), plus 2 new tests for the core new behavior (`test_thinking_effort_not_set_by_default_without_opt_in`, `test_notes_skip_reason_when_opt_in_not_enabled`). 299 tests total, ruff clean, mypy clean.

**What this means for the numbers above**: the -23.0% overall and the `code`/`conversational` losses were measured *before* this fix, against the old automatic-effort behavior. The thinking-effort fix itself remains justified on independent grounds (matches this project's established safe-by-default pattern), but it turned out NOT to be the reason for those numbers.

### Post-fix re-run (2026-09-04) — same result, revealing a second, more precise root cause

Re-ran `usage --confirm-spend` after shipping the opt-in fix. **Result: essentially unchanged (-25.5% overall, `code`/`conversational` losses nearly identical sample-for-sample to the pre-fix run)** — several samples matched to within 0.1 percentage points (`code_001`: -136.0% both times, `code_014`: -136.1% both times, `code_017`: -137.1% both times). That level of reproducibility across separately-run real completions is itself a clue: it doesn't look like content variance, it looks like something hitting the exact same ceiling both times.

**Verified directly, not guessed, before writing any more code**: offline-checked `code_001` and confirmed `thinking_effort_skipped: 'opt_in_not_enabled'` and `thinking: None` in the prepared request — the fix was working correctly. Yet the cost pattern persisted. So thinking effort was never the (sole) cause; the earlier diagnosis had picked the wrong variable. A small, cheap, targeted diagnostic (5 samples, ~$0.05, `stop_reason` captured on every call) confirmed the real cause: **every single completion in both conditions — baseline AND pipeline — showed `stop_reason="max_tokens"`.** Every one was truncated. `code_001`/`code_014` baseline cut off at 400 tokens, pipeline cut off at 960 (still truncated, just later); `code_005`/`code_007` (the ones showing exactly +0.0%) turned out to never get widened at all (`prepared max_tokens=400`, unchanged from the caller's own value — very likely a task-classifier mismatch, the same pattern already seen once before with `tool_heavy_017`).

**The actual root cause**: `benchmarks/corpus/`'s `max_tokens` values (400 for `code`, 300 for `conversational`) were deliberately set tiny for Phase 9's own purpose — proving `adaptive_budget`'s widening mechanism correctly fires under a tight cap — not to represent what a realistic caller would set. Reusing that corpus unmodified for a *dollar-cost* benchmark meant every comparison was "truncated garbage at 400 tokens" vs. "still-truncated-but-less-garbage at 960 tokens" — neither a complete, useful answer, and the wider one costs more purely because it wrote more before also getting cut off. Not a fair test of real efficiency.

- [x] **Fixed in `benchmark_runner.py`, not by touching the corpus**: `_REALISTIC_MAX_TOKENS_FLOOR = 2048` (chosen from real observed reply lengths in this project's own `dev_demo.py` runs — thorough code/explanation answers commonly ran 700-960 output tokens — generous headroom, not a guess) applied via `_with_realistic_max_tokens()` to BOTH the baseline and pipeline calls identically, in both the cost estimator and the real run. The corpus itself is untouched — Phase 9's widening-correctness tests still need the original tight caps and still have them.
- [x] With the floor in place, `adaptive_budget`'s own widening will often no-op (the caller's cap already exceeds its heuristic) — correct, and it isolates whether the OTHER mechanisms (dedup, near_dup, schema_minification, context_scheduler, delta_compression, caching) save money once truncation stops confounding the result.
- [x] Dry-run cost re-estimated: **$1.75** for the full 83-sample corpus at the new floor (up from $0.31 — expected, since allowing genuinely longer, complete replies costs more than truncated ones, by design). Exceeds the default $1.00 `--cost-ceiling`, correctly refused when tested; needs an explicit override to run.
- [x] Ruff/mypy clean; 299-test suite unaffected (script-only change, no source module touched).

- [x] **Corrected real run happened (2026-09-04/05)**: net **-2.1%** overall ($0.414 → $0.423), a world away from the truncation-confounded -25.5%. `input tokens saved 333 total; output tokens cost 517 total` — both numbers now small and plausible, not the earlier 5,000+-token blowout. No more suspicious identical-percentage clustering across categories either (real per-call variance now, not a systematic artifact). `conversational` flipped from a -19% loss to net positive (~+2.5%); `mixed_workload` stayed solidly positive (~+8%); `code`/`extraction`/`tool-heavy` each landed slightly negative (~-4% to -6%) but scattered with real positive and negative samples in every category, consistent with normal single-call variance rather than a structural bias. `tool_heavy_017`'s cache repeat remained the standout real number: $0.005065 → $0.001233 (~76% cheaper on the second hit).
- [x] **Honest reading of that -2.1%, discussed with the user**: at one call per sample with no averaging, a result this close to zero is inside normal noise — not strong evidence of a real bias either way. The more important structural point: `usage`'s own design (83 *isolated, one-shot* samples) is close to the worst-case scenario for the mechanism with the largest real payoff (caching only pays off on repeat calls) and indifferent to the mechanisms that need accumulated history to matter (dedup/near_dup/context_scheduler need real redundancy to prune, which a short independent sample rarely has). It answers "does this help on someone's first-ever message," not "does this help across a real session" — those are different questions with different honest answers.

## Real multi-turn session benchmark (`benchmark_runner.py session`) — built 2026-09-05

Built because the honest answer to "why no net savings" pointed straight at `usage`'s own structural blind spot: it can't see caching amortize past a single write, and it can't see redundancy accumulate the way a real conversation does. `session` tests that directly, with real completions.

- [x] **`benchmarks/session/realistic_session.json`**: one realistic 8-turn conversation (code question → real bug fix → near-duplicate padding → a genuine decision → more padding → a tangent → an extraction request → a final recap that requires remembering the earlier decision), with the SAME system prompt reused across every turn (so caching gets a real chance to amortize past the first write, unlike a one-shot corpus sample) and genuine accumulated redundancy for `dedup`/`near_dup`/`context_scheduler` to actually prune. `max_tokens=800` (realistic, well above what these turns naturally need — the truncation lesson from `usage` applied here from the start, not re-discovered the hard way again).
- [x] **Two parallel real threads, run turn by turn**: `baseline` (raw request/reply every turn, no Tokenetics, never cached — what a caller who never adopted Tokenetics would actually pay) and `pipeline` (`prepare()`/`finalize()` every turn, with a real, incrementally-grown `cache_usage_history` fed forward exactly as a real caller tracking their own repeat pattern would build it). The two threads' histories diverge in content turn to turn (separate real completions) — expected and correct, since this measures "what would my bill look like across this session if I'd used Tokenetics from turn one," not a replay of identical content.
- [x] **A real bug caught and fixed before spending money, via an offline structural dry-run (stub client, fake replies, zero cost)**: the session originally registered one never-used tool (to also exercise `schema_minification`), but `schema_minification` runs earlier in the fixed pipeline than `cache_breakpoint_optimizer` — and one turn's text ("what's a good weekend activity if the **weather's** nice") coincidentally lexically matched the tool's name, making it unpredictably survive on that one turn only. That changed the actual stable prefix the cache stage sees turn to turn, breaking the repeat-pattern match against this benchmark's own precomputed hash — confirmed directly (`cache_breakpoint_optimizer` fired on only 1 of 8 turns) before it ever became a real-money mistake. Fixed by removing the tool entirely (schema_minification is already covered elsewhere; it's not this benchmark's concern, and having it silently perturb the cache prefix defeated the point). Re-verified offline: the cache breakpoint now fires consistently from turn 3 onward (turns 1-2 correctly show `insufficient_usage_history` — not enough evidence yet, conservative and correct), `near_dup` catches the duplicate padding from turn 5, `context_scheduler` starts pruning once there's real accumulated content.
- [x] Same cost-ceiling gating as `usage`/`quality-check` (`--dry-run`/`--confirm-spend`/`--cost-ceiling`). Dry-run: **$0.1556** for 8 turns × 2 threads (16 completions).
- [x] Ruff/mypy clean; 299-test suite unaffected (script + data only, no source module touched).

- [x] **Real run happened (2026-09-05): net +16.0% cheaper** ($0.052282 → $0.043923) — the first genuinely positive, substantial verdict in this whole benchmarking arc. Turn-by-turn: turns 1-2 slightly more expensive (no cache activity yet, still building evidence), turn 3 a real cache **write** (-14.8%, the one-time premium), turns 4/5/6 real cache **reads** off that write (+38.9%/+42.1%/+29.3%), turn 7 a **second** write (-14.6% — the turn-3 write had expired), turn 8 a read off the fresh write (+38.2%). A clean, textbook demonstration of the write-then-amortize pattern `cache_breakpoint_optimizer` is built around, confirmed with real money.
- [x] **The turn-7 re-write led directly to a real bug fix, not just an observation**: `cache_breakpoint_optimizer`'s `_choose_ttl` picked the 5m tier based on the *average* observed repeat gap, with no safety margin — a few fast early gaps (turns 1→2→3) pulled the average comfortably under 5 minutes, but real generation/network latency between turns 3 and 7 pushed that specific gap past the window, expiring the cache and forcing an unplanned second write. Root cause: averaging hides exactly the gap that will recur and cost money; nothing in the decision asked "what's the worst gap we've actually seen," only "what's typical."
- [x] **Fixed same day**: `_choose_ttl` now decides based on the **maximum** observed gap, not the average — if any past gap already exceeded a tier's window, that tier is no longer eligible, even if other gaps were fast. Trades a bit more upfront write cost (1h's 2x premium vs. 5m's 1.25x) for avoiding a strictly worse outcome (a surprise re-write that also forfeits whatever reads the shorter tier might have delivered) — matching this project's conservative-by-default principle used everywhere else (`adaptive_budget` widens rather than narrows, `schema_minification` keeps ambiguous tools). The informational net-benefit estimate logged alongside the decision still uses the average (a genuine "how much do we expect to save" question, not a safety margin) — only the tier-selection safety check changed.
- [x] New regression test (`test_a_few_fast_gaps_then_one_slow_gap_does_not_pick_the_5m_tier`) reproduces the exact gap shape from the real session (60s, 60s, 60s, 400s -- average 145s looks safely under the 5m window, but the 400s max isn't) and confirms it now correctly lands on the 1h tier instead. All 9 pre-existing tests passed unchanged (every one of them used uniform gap spacing, where average and maximum are numerically identical, so this fix was invisible to them — exactly the kind of case that needed a dedicated non-uniform test to catch at all). 300 tests total, ruff/mypy clean.

**Net result of the whole investigation, start to finish**: isolated one-shot requests are roughly cost-neutral (`usage`, -2.1%, within normal variance); a real, repeated-traffic multi-turn session is genuinely and substantially cheaper (`session`, +16.0%), and the one real inefficiency that session run surfaced (a cache tier picked without a safety margin) is now fixed and regression-tested. This is the complete, honest, evidence-based answer to "what does Tokenetics actually save" — not a single flat percentage, exactly as CLAUDE.md's benchmarking discipline requires.

### Three more real `session` re-runs (2026-09-05) — confirmed repeatable, and the tier fix's actual role clarified

Per-turn `ttl`/`avg_gap`/`max_gap` visibility added to `session`'s output (no behavior change) specifically to settle whether the tier fix above was actually preventing turn-7-style re-writes, rather than assuming from code review alone.

- [x] Three further real runs, all net positive: **+4.4%**, **+12.0%**, **+13.7%** — alongside the original +16.0%, four consecutive real runs, all cheaper, none negative.
- [x] **The turn-7 re-write recurred in every run** — but the added visibility showed `max_gap` at turn 7 was consistently ~14-15 seconds, nowhere near even the 5-minute window. The tier-selection fix was choosing `5m` correctly every time, given what it actually observed; there was never a hidden slow gap for the max-vs-average fix to catch in this data.
- [x] **Corrected conclusion**: the turn-7 re-write is best explained as ordinary best-effort cache variance on Anthropic's side (real distributed caches aren't a guaranteed hit within a nominal TTL window, even when the caller's own timing and tier choice are both correct) — not a defect the tier-selection logic could have prevented. The max-vs-average fix remains independently justified (a real, different failure mode it *would* catch), but it does not explain this specific recurring pattern, and it would have been wrong to claim it did without this instrumentation.
- [x] **Four-for-four repeatability is the actual headline finding**: a realistic multi-turn session is consistently cheaper with Tokenetics even with this recurring, unavoidable cache blip included in every run — the positive result is not a one-off.

## Phase 12 (Distribution: PyPI publish, MCP server + real-client test) — in progress (started 2026-09-05)

- [x] **License chosen (discussed with the user, not assumed)**: MIT — no prior LICENSE file existed; needed before PyPI publish. `LICENSE` added; `pyproject.toml` gained `license = "MIT"` + `license-files = ["LICENSE"]`, plus PyPI-required metadata that was previously missing (`classifiers`, `keywords`, `[project.urls]` pointing at the GitHub repo).
- [x] **MCP server tool shape decided (discussed with the user)**: a single `optimize_request` tool, taking raw Anthropic Messages API kwargs (as a JSON object) plus optional per-stage `stage_config`, returning the optimized kwargs from `Tokenetics().prepare()` — never calling the real Anthropic API itself. Two alternatives considered and rejected: two separate tools (`optimize_request` + `finalize_response`, more complete but a needless second round-trip for most callers) and a single tool that also calls the Anthropic API server-side (more convenient but adds API-key handling and a billed-call surface this project's "thin layer" framing doesn't call for).
- [x] **`src/tokenetics/mcp_server.py`** — the entire MCP layer: `MCPServer("tokenetics")` (`mcp.server.MCPServer`, verified against the installed `mcp==2.1.1` package directly, not just docs recall) with one `@mcp.tool()`-decorated `optimize_request` function. Malformed input is caught and returned as `{"error": "..."}` rather than crashing the server or the stdio JSON-RPC stream. `main()` runs `mcp.run(transport="stdio")`; nothing in this module writes to stdout (the stdio transport's hard requirement).
- [x] New `tokenetics[mcp]` extra (`mcp[cli]>=2.0.0`) — kept out of the base install, same discipline as the `compress`/`semantic-cache` extras. New `tokenetics-mcp` console script entry point.
- [x] **Real-client test performed, not just unit-tested**: a real `mcp.client.stdio` session was spun up against the server as an actual subprocess, called `list_tools()` (confirmed `optimize_request` is discoverable) and `call_tool()` — confirmed the server responds correctly over real stdio JSON-RPC. Without a live `ANTHROPIC_API_KEY` in this environment, the call correctly surfaced Anthropic's own authentication error as a clean `{"error": "..."}` tool result rather than crashing the server — proving the fail-open-at-the-boundary contract holds, though the actual token-optimization output still needs one real run with a live key to see genuine before/after numbers (handed back to the user to run).
- [x] Ruff/mypy clean; full 300-test suite unaffected (new module only, no source module touched).

- [x] **`uv build` + clean-venv install verified (2026-09-05)**: built wheel/sdist contain only `tokenetics/*` plus metadata (no stray dev files, `benchmarks/`/`tests/`/`scripts/` correctly excluded); `pip install` from the built wheel alone into a fresh, isolated venv pulled in only `anthropic` and its own deps -- zero Tier 2 leakage (no torch/transformers/sentence-transformers/mcp), confirming the extras boundary holds in a real install, not just in `pyproject.toml`'s declared structure. `import tokenetics`, `Tokenetics()` instantiation, and the full 11-stage + 1-response-stage list all worked correctly from that clean install, run entirely from `/tmp`, away from the repo checkout.
- [x] **A real CI bug caught and fixed the same day, unrelated to packaging itself but found while re-verifying this phase's work**: `uv run mypy src` failed in CI (`numpy` import-not-found in `extras/semantic_cache.py`) but passed locally -- root cause was CI's plain `uv sync` never installing the Tier 2 extras that `numpy` is a transitive dependency of, while the local dev venv happened to have them installed already. Confirmed by actually reproducing CI's exact environment (`UV_PROJECT_ENVIRONMENT=... uv sync` with no extras, then running mypy against that interpreter) rather than trusting a fix from code review alone. Fixed by adding `numpy`/`numpy.*` to the existing `ignore_missing_imports` override list, alongside the same-class gap for the new `mcp` extra (`mcp_server.py` itself wasn't covered either) -- which uncovered a second, subtler issue: `mcp` genuinely ships full type stubs, so a dev venv WITH it installed sees a fully-typed `@mcp.tool()` decorator while CI (without it) sees `Any`, and `strict`'s `disallow_untyped_decorators` only complains in the latter case. A line-level `# type: ignore` would be correct in one environment and a stale "unused ignore" error in the other, so this was fixed instead with a per-module override (`disallow_untyped_decorators = false` for just `tokenetics.mcp_server`) -- environment-independent, unlike a line comment. Re-verified clean in both a simulated bare-CI environment and the full local one.
- [x] **Real MCP client round-trip confirmed with a live API key (2026-09-05)**: `scripts/mcp_client_smoketest.py` (a real `mcp.client.stdio` session, spawning the actual server subprocess) correctly called `optimize_request` and got back genuine pipeline output -- `max_tokens` widened from the caller's `100` to `360` (adaptive_budget's conservative-widen behavior on a short, unclassified message), and a ~700-token repeated system prompt correctly left uncached (below `claude-sonnet-5`'s 1024-token minimum cacheable prefix -- the cache breakpoint optimizer declining rather than claiming a benefit that wouldn't materialize). One real bug caught and fixed getting here: `mcp.client.stdio`'s subprocess spawning only inherits a small env allowlist by default (`HOME`/`LOGNAME`/`PATH`/`SHELL`/`TERM`/`USER`), so `ANTHROPIC_API_KEY` never reached the server subprocess until the smoke-test script explicitly passed `env=dict(os.environ)` to `StdioServerParameters` -- the same thing a real MCP host's own server config has to do.
- [x] **Savings visibility added (2026-09-05), prompted by the user asking why the tool didn't report any savings percentage**: the original `optimize_request` returned only the optimized kwargs, discarding the same per-stage `count_tokens()` data that fires on every call (visible in the earlier real run's logs) once the function returned. Fixed to return `{"request": <optimized kwargs>, "stats": {"tokens_before", "tokens_after", "tokens_saved", "saved_pct"}}`, computed from that call's own `InMemoryCostLogger` entries using the identical "first stage's tokens_before -> last stage's tokens_after" convention `dashboard/aggregate.py`'s `aggregate()` already established (not a new formula). Also added optional persistence: if `TOKENETICS_MCP_LOG_FILE` is set, the same call's entries are replayed into a `FileCostLogger` at that path, so MCP-driven runs show up on the same dashboard as `dev_demo.py --log-to` runs -- off by default, matching this project's opt-in-for-persistence pattern rather than a stdio server silently writing an unannounced file. This is a breaking change to the tool's return shape (callers now read `result["request"]` instead of `result` directly) -- acceptable since the tool has never been published or used by anyone outside this session. Ruff/mypy clean in both a real CI-equivalent environment and the full local one; 300 tests unaffected (script/module-only change). **Confirmed with a real re-run (2026-09-05)**: `{'tokens_before': 1010, 'tokens_after': 1010, 'tokens_saved': 0, 'saved_pct': 0.0}` -- a genuine, honest zero, not a bug: this specific test input (one short user turn, a repeated-but-non-duplicate system prompt sitting right at the model's minimum-cacheable size, no tools, no history) has nothing for any stage to find. Confirms the stats path reports real measured numbers rather than fabricating a nonzero result.

### A real bug found via the MCP demo itself (2026-09-06): stage 4's zero-overlap filter had a false-negative hole

Prompted by the user asking to see a real, nonzero savings case through MCP. Built a second, realistic demo request (`scripts/mcp_client_smoketest.py`): an accidentally double-sent user turn (a common real frontend-retry bug) asking about weather, with 5 registered tools where only `get_weather` is actually relevant. An offline structural check (no API key needed -- just running `DedupStage`/`SchemaMinificationStage` directly) caught a real bug before it ever reached a real API call: `send_email` -- genuinely unrelated to a weather question -- survived stage 4's "drop only on literally zero word overlap" filter.

**Root cause, traced directly**: `core/lexical.py`'s `_words()` splits text into tokens on `[a-z0-9]+`, which fragments contractions on the apostrophe -- `"what's"`/`"it's"` in the question and `"user's"` in the tool's own description all produce the same meaningless remnant token, `"s"`. That single stray token was the ONLY word the two texts had in common (`overlap_score` returned `0.029`, not `0.0`), which was enough to dodge the zero-overlap drop threshold entirely -- a real false negative in an already-existing, previously-shipped mechanism, not something new about this demo.

- [x] **Fixed in `core/lexical.py`**: `_words()` now also excludes single-character tokens (in addition to the existing stopword list) -- no legitimate single letter/digit carries real topical signal for this heuristic, and excluding them removes the contraction-remnant false positive without weakening any genuine match.
- [x] New regression test (`test_overlap_score_ignores_contraction_remnants`) reproduces the exact real scenario (a `send_email` tool description vs. a weather question, both containing unrelated contractions) and asserts `overlap_score(...) == 0.0`. All prior `test_lexical.py` tests passed unchanged (none exercised single-character tokens). 301 tests total, ruff/mypy clean.
- [x] **Re-verified offline post-fix**: `send_email`/`search_web`/`calculate`/`get_stock_price` are now all correctly dropped, `get_weather` correctly kept; the duplicate user turn is correctly deduped to 1 message.
- [x] **Confirmed with a real MCP round-trip (2026-09-05)**: `{'tokens_before': 907, 'tokens_after': 506, 'tokens_saved': 401, 'saved_pct': 44.2}`, `tools kept: ['get_weather']`, `messages kept: 1` -- the first real, substantial, nonzero savings number seen through the MCP layer, and a clean demonstration of dedup + schema_minification's relevance filter both doing genuine, correct work on a realistic pattern (an accidental duplicate turn, an oversized tool registry) rather than a synthetic best-case.

This is also a good example of this project's broader testing discipline paying off outside a dedicated benchmark run: a real, if narrow, correctness bug in a stage that had already shipped and passed its own test suite was caught by simply trying a new, realistic input through the MCP layer -- not by a scheduled benchmark pass.

**Not yet done**: the actual `pypi.org` publish step (needs the user's own PyPI credentials) -- everything else in this phase is now real, measured, and confirmed.

## Assumptions

- **Original pace assumption (2026-07-19, superseded below)**: solo, ~4 hours/day, ~5–6 days/week (~20–24 hrs/week) — a human-solo-dev estimate. This turned out to badly understate actual velocity once implementation started (see "Re-forecast" below) and is kept here only for context on how the original per-phase effort weights were derived.
- **Confidence**: estimates for phases 0–8 (Tier 0) are moderately confident. Phases 9–13 (benchmark suite, Tier 2 extras, distribution, dashboard, docs) are rougher and should be re-forecast again once Tier 0 actually ships — actual velocity through 0–8 is better evidence than either the original guess or this first re-forecast.
- **Scope**: full v1 as specified in the brief — nothing cut. This was a deliberate choice: the alternative (a minimal Tier 0 slice, or thin-rigor stages) was offered and rejected in favor of keeping the brief's rigor requirements (measured-not-estimated benchmarks, hand-labeled classifier validation set, fail-open test coverage per stage, quality-degradation benchmarks for lossy compression) intact.

### Re-forecast (2026-07-26, based on actual Phase 0–3 velocity)

Phases 0–3 — originally estimated at ~4.5 weeks (31.5 days) combined — were actually completed in **7 days** (2026-07-19 to 2026-07-26). That's roughly a **4.5x** speedup over the original human-solo-dev pacing model, which makes sense: this project is being implemented in AI-paired sessions, not solo part-time hours.

The remaining phases don't all scale the same way, though, so this re-forecast applies two different multipliers instead of a flat 4.5x everywhere:

- **Phases 4–8** (Tier 0 build-out — tool-relevance filtering, generation config, context scheduler, caching, delta compression, integration): same *kind* of work as phases 0–3 — design + implement + test a deterministic stage. Scaled at the observed **4.5x**.
- **Phases 9–13** (benchmark suite, Tier 2 extras, distribution, dashboard, docs): partly bound by things that don't compress with coding speed — real, paid API calls across a 15–20+ sample corpus, new heavy dependencies (`torch`/`transformers` for `compress`), external approval/publish steps (PyPI, an MCP client test), and honest human-in-the-loop quality review. Scaled at a more conservative **2x**.

Both multipliers are still guesses extrapolated from four phases of one kind of work — **re-check this again once Tier 0 (phase 8) actually freezes**, the same way the original roadmap already called for.

## Timeline

| Phase | Covers | Effort | Target window |
|---|---|---|---|
| 0 | Finish scaffolding: CI, internal request type, plugin ABC, orchestrator skeleton, no-op round-trip test | done | Jul 19 – Jul 26 (actual) |
| 1 | Cost logger, fail-open framework, tokenizer, deliberately-broken-plugin test | done | Jul 19 – Jul 26 (actual) |
| 2 | Foundation stages: dedup, near-dup (MinHash), schema minification, post-hoc trim | done | Jul 19 – Jul 26 (actual) |
| 3 | Task classifier + 30–50 example hand-labeled validation set, accuracy gate | done | Jul 19 – Jul 26 (actual) |
| 4 | Classifier-dependent stages: tool-relevance filtering, structured-output enforcement, brevity injector, adaptive generation budgets — max_tokens (real-response stress tests) + thinking effort level (validated against the quality-check set, not truncation rate; needs the model-compatibility table) | done | Jul 19 – Jul 26 (actual) |
| 5 | Context scheduler: per-turn classifier, pinning, DP knapsack, greedy fallback, degraded mode, latency benchmark | done | Jul 19 – Jul 26 (actual) |
| 6 | Caching: safety guard (hard-raise tested) → reorder → pricing config table → breakpoint optimizer + synthetic-traffic benchmark | done | Jul 19 – Jul 27 (actual) |
| 7 | Delta compression + `apply_delta()` + round-trip tests | ~1 day | Aug 4 |
| 8 | Assemble Tier 0 in fixed order, end-to-end integration test, stage 4/5 double-prune regression test, **freeze Tier 0 API** | ~2 days | Aug 5 – Aug 6 |
| 9 | Benchmark suite: corpus (15–20+ samples × 4 task-type categories) + held-out quality-check sets, real measured numbers | ~1 week | Aug 7 – Aug 13 |
| 10 | Tier 2 extras: `compress`, `semantic-cache`, TALE budget estimation (each independent, each benchmarked) | ~1.5 weeks | Aug 14 – Aug 23 |
| 11 | Dashboard (CLI table or small local web view, read-only) | ~3 days | Aug 24 – Aug 26 |
| 12 | Distribution: PyPI publish, MCP server + real-client test | ~3 days | Aug 27 – Aug 29 |
| 13 | Docs: architecture doc, benchmark write-up, "considered and rejected" section | ~2 days | Aug 30 – Aug 31 |

**Original total: ~19 weeks (~4.5 months)** at the superseded human-solo-dev pace. **Re-forecast total: ~6 weeks** (2026-07-19 – 2026-08-31) at observed AI-paired velocity, with the phase 9–13 half of that number weighted more conservatively per above.

## Milestones

- **Jul 26 (actual)** — Phases 0–3 done: orchestrator, cross-cutting systems (logger, fail-open framework, tokenizer), all four foundation stages, and the task classifier (accuracy-gated at 85%) all exist.
- **Aug 6 — Tier 0 frozen (re-forecast).** The deterministic core is feature-complete, integration-tested, and API-stable. This is the most load-bearing milestone: per CLAUDE.md, nothing in phases 9–13 should start before it. Re-forecast the rest of the timeline against the *actual* freeze date, not this estimate, once it happens.
- **Aug 13 (re-forecast)** — Benchmark suite produces the first real measured numbers.
- **Aug 31 (re-forecast) — v1 shippable.** PyPI package, MCP server, dashboard, and docs all done.

## Dependencies to respect

These are already implied by CLAUDE.md's build order, but easy to miss in a flat table:

- Phase 4 cannot start until phase 3's task classifier clears its accuracy gate.
- Phase 9 (benchmarking) must not start before phase 8 (Tier 0 freeze).
- Phase 10 (Tier 2 extras) must not start before phase 9 has produced real measured numbers.
- **Phases 11/12 swapped (2026-08-10, both this file and CLAUDE.md's own build-order list updated for consistency)**: dashboard now comes before distribution, reversing the brief's original order. Neither phase depends on the other in either direction (the dashboard just reads the existing `CostLogger` structured log; distribution is PyPI packaging + an MCP server) — CLAUDE.md's "don't reshuffle" build-order rule exists to stop skipping real dependencies (classifier before consumers, Tier 0 frozen before Tier 2), not to lock in an order between two independent phases. Rationale: the dashboard directly helps our own remaining work (including the pending final savings-benchmark pass, noted separately) instead of reading raw terminal output by hand, while distribution doesn't unlock anything else still owed internally — so there's no cost to doing it second.

## Full codebase review (2026-09-06)

User asked for a full review of the entire codebase "to ensure the app works properly" -- a systematic read through every source file (core, all 11+1 stages, Tier 2 extras, dashboard, MCP server), not tied to a specific feature or benchmark run. Found and fixed three real bugs, none previously caught by the test suite or any prior benchmark run:

- [x] **README.md's own usage example was broken.** It passed `previous_request` directly as a `tk.prepare()` kwarg; the cache-safety guard only ever reads it from `config["previous_request"]` via `Tokenetics(stage_config=...)` at construction. Confirmed directly: following the example verbatim produces a `prepare()` output containing a `previous_request` key, which the real `client.messages.create(**result)` call (the very next line in the same example) would reject with `TypeError: unexpected keyword argument`. Fixed by correcting the usage example and adding a second example showing the actual `stage_config`-based mechanism.
- [x] **A real fail-open gap in the orchestrator.** `_run_stage`/`_run_response_stage` measured a stage's result (`count_tokens`/`count_text_tokens`) OUTSIDE the fail-open try/except -- so if a stage succeeded but produced a result the real count-tokens endpoint couldn't measure (e.g. `post_hoc_trim` reducing an all-boilerplate reply to an empty string, which the real API very likely rejects the same way it already rejects trailing whitespace), the exception would escape `prepare()`/`finalize()` entirely instead of falling back like every other stage failure. Fixed two ways: (1) root cause in `post_hoc_trim.py` -- if trimming would leave nothing at all, keep the original text instead of ever emitting empty content; (2) defense in depth in `orchestrator.py` -- the measurement call now sits inside the same try/except as the stage's own execution, covering any current or future stage that hits this general class of issue, not just this one. Two new regression tests in `test_fail_open.py` use a stub client that raises when asked to measure an empty/unmeasurable result, confirmed to genuinely fail without the fix (reverted the orchestrator change, watched both tests fail with the real uncaught exception, restored it).
- [x] **`context_scheduler`'s `dropped_turns` metric overcounted.** It conflated turns genuinely excluded by pinning/knapsack with turns merely MERGED (not lost) by the alternation-fix safety net. Reproduced directly: 3 messages, only 1 turn actually excluded by the budget, but two pinned same-role turns surviving selection then merging into one message inflated the logged count to `dropped_turns=2, pinned_turns=2` -- self-contradictory (both pinned turns "dropped"?) and double the real exclusion count. This directly undermines CLAUDE.md's "every logged number is measured, never blurred" discipline for anyone reading the dashboard or a benchmark run. Fixed by computing real exclusions (`n - len(kept_messages)`, before the alternation fix runs) plus the one genuine content-loss case the alternation fix itself can cause (a dropped lead turn), instead of inferring drop count from a message-count delta that merging also shrinks. New regression test confirmed to genuinely fail without the fix (same revert-and-watch-it-fail method).
- [x] Also reviewed without finding further issues: `dedup`, `near_dup`, `task_classifier`, `schema_minification`, `delta_compression`/`core/delta.py`, `cache_reorder_guard`, `cache_breakpoint_optimizer`, `structured_output`, `brevity_injector`, `adaptive_budget`, `core/request.py`, `core/tokenizer.py`, `core/cache_hash.py`, `core/context_window.py`, all three Tier 2 extras, and the dashboard's `render.py`/`server.py`.
- [x] 305 tests total (301 + 4 new), ruff/mypy clean.

## Dashboard enhancements + a real pricing re-verification (2026-09-06)

A series of real, hand-run dashboard sessions (the user actually using it, not just described) surfaced several honest presentation gaps and one real stale-data catch:

- [x] **Five stages showing a bare, misleading `0.0%`** (`task_classifier`, `cache_reorder_guard`, `structured_output`, `adaptive_budget`, `cache_breakpoint_optimizer`) -- their real effect (annotation, verification, output shape, generation cost, cache price-per-token) was never expressible as a request-token-count change, but the dashboard showed `0.0%` exactly like a stage that genuinely did nothing. User asked directly whether this was misleading -- yes. Considered and rejected removing the row entirely (would lose real `Enabled`/`Errors`/`Avg time` diagnostic value for those stages); fixed instead with a per-stage inline note replacing just the Saved/%Saved cells (`render.py`'s `_NO_TOKEN_IMPACT_NOTES`).
- [x] **`brevity_injector`/`post_hoc_trim`'s real numbers still needed context.** Unlike the five above, these ARE real, correct measurements on the right axis -- `brevity_injector`'s negative % is genuine instruction-overhead cost (only on extraction-shaped requests), `post_hoc_trim`'s zero depends on whether a given batch of real replies happened to contain a recognized sign-off. Explicitly did NOT hide/replace `brevity_injector`'s negative number when asked "why keep it at all" -- CLAUDE.md's own honest-benchmarking rule names this exact case ("document where a stage can go net-negative... not buried"), so a small contextual note was added ALONGSIDE the real bar/percentage instead (`_CONTEXTUAL_NOTES`), never replacing it.
- [x] **Min/median/max savings added**, per the user asking directly whether the dashboard showed anything beyond one blended average -- it didn't, which is inconsistent with `benchmark_runner.py`'s own corpus/usage reports already following CLAUDE.md's "report as ranges" rule. `StageStats`/`DashboardStats` (`aggregate.py`) now track a per-occurrence/per-run list of %-saved values, with `min_pct_saved`/`median_pct_saved`/`max_pct_saved` properties; rendered as three new summary cards (overall) and a new table column (per-stage). Real numbers on the user's own accumulated log immediately showed a -5.3% to +78.7% spread hiding inside a 21.2% blended average.
- [x] **Cosmetic charts added, at the user's explicit request and explicit choice of tradeoff.** Recommended pure CSS/SVG (matches this project's "no new dependency" scoping used everywhere else); user chose a real JS charting library instead, after being told the tradeoff (an external CDN request, unlike everything else in this project). Implemented with Chart.js loaded from a CDN: a pie chart of tokens-saved-by-stage (positive contributors only -- pie slices can't represent a negative share, so `brevity_injector` is correctly excluded there while still appearing in the bar chart below it), a bar chart of %-saved-by-stage (handles negatives correctly), and a cache-hit-vs-write donut. The tradeoff is stated on the page itself (in the footer, not a top-of-page warning) rather than left to fail silently offline -- every other number on the page, including the charts' own underlying data, is still a fully local, offline read of the log file.
- [x] **A full visual/cosmetic pass** ("make it cosmetically appealing for executives"): a hero card for the headline saved-%/$ figure (color-coded green/red by sign, with a NET SAVINGS/NET COST badge), an inline SVG logo mark, a refined dark palette, table zebra-striping, and consistent card/section styling throughout. Presentation-only -- all 317 tests (unchanged data-layer logic) passed without modification.
- [x] **A real stale-pricing catch, prompted by the user asking to convert a dashboard %-saved figure into a dollar figure.** `cache_pricing.py`'s own docstring had flagged itself as needing re-verification past 2026-09-01 (the documented date Sonnet 5's introductory pricing was scheduled to step up to $3.00/$15.00) -- today being 2026-09-06 and the table still showing the old $2.00/$10.00 figures, per CLAUDE.md's "if the table looks stale, flag it rather than silently trusting it" rule, this needed a live check before answering. **Re-verified live**: the step-up was cancelled -- Anthropic's pricing page now states outright that $2.00/$10.00 "is now the standard price" and the scheduled increase "will not occur." The numeric values in the table were therefore already correct; only the surrounding docstring/comments (which described them as temporary/introductory) were stale. Fixed: `LAST_VERIFIED` updated to 2026-09-06, docstring and both inline comments corrected to state the price is standard and permanent, not introductory. `tests/test_cache_pricing.py`'s comment updated to match (the assertion itself was already correct and unchanged).
- [x] 317 tests total, ruff/mypy clean throughout this whole sequence.

### Output/thinking token savings added to the dashboard (2026-09-06)

Prompted directly: the dashboard's 21% headline was confirmed to be request/input tokens only, and the user asked whether output tokens were being saved at all. Honest answer at the time: no measured evidence existed anywhere in the project's own logging -- `brevity_injector`/`structured_output` are both DESIGNED to reduce output tokens, but nothing had ever captured a real before/after comparison of `usage.output_tokens`, since `Tokenetics.prepare()`/`finalize()` never make the actual completion call themselves.

- [x] **Reused existing infrastructure rather than building new measurement machinery**: `dev_demo.py`'s `_measure_usage()` (used by `--all-scenarios --measure-usage`) already runs a real baseline-vs-optimized A/B of two real completions and already computed an `output_tokens` diff in its `usage_diff` dict -- it just never got logged anywhere. Wired a new `log_event("generation_usage", ...)` call into it instead of writing a new comparison path.
- [x] **A real architectural gap found while implementing**: `--all-scenarios` returned before `logger = FileCostLogger(...)` was ever constructed, so `--log-to` was silently a no-op in that mode entirely, for every event type, not just this new one. Fixed by moving the logger construction earlier in `main()`, shared by both code paths.
- [x] **The thinking-token estimation caveat was made structural, not just a docstring note**: Anthropic's `usage` object has no separate line item for thinking tokens (confirmed via the real completion earlier this session whose `ThinkingBlock` had empty visible text yet a real, nonzero gap between `output_tokens` and the visible reply's own token count) -- so `GenerationUsageStats.estimated_thinking_tokens_saved` is named and documented as an estimate everywhere it appears (the dataclass docstring, the dashboard's inline note, and the footer scope-note), derived from two real measured numbers (`output_tokens` minus visible-text tokens, each side) rather than read directly. `output_tokens_saved`/`output_pct_saved` alongside it ARE real, measured savings -- the two are visually and textually distinguished, never blurred together.
- [x] New `GenerationUsageStats` dataclass (`aggregate.py`), wired into `Tier2Stats`/`aggregate_events()`/`has_any_data`; new dashboard card block in `render.py` following the same real-data-or-honest-placeholder pattern as every other Tier 2 mechanism.
- [x] 319 tests total (2 new: real output-tokens-saved math, and the render-layer real-vs-placeholder check), ruff/mypy clean. No pytest coverage added for the `dev_demo.py` changes themselves, consistent with this project's established `scripts/*.py` convention (real-money smoke-tested by hand, not unit-tested) -- verified offline instead (module imports cleanly, `--list-scenarios` still runs).
- [x] **First real run (2026-09-06) found two real problems, not just data.**

  1. **A real crash bug in this feature's own code**, caught by the user actually running it: `_measure_usage()`'s new `count_text_tokens(_visible_text(without_response), ...)` call didn't strip trailing whitespace before counting -- the exact same class of bug already found and fixed once in `Tokenetics.finalize()`, re-introduced here by not applying the same fix to this new code path. Crashed mid-run (`long_history` completed, then a real `400 invalid_request_error: final assistant content cannot end with trailing whitespace` killed the process before the remaining scenarios ran), leaving the run's `--log-to` data incomplete. Fixed: `_visible_text()` now `.rstrip()`s, matching `finalize()`'s existing fix. Confirmed offline (stub client, deliberately trailing-whitespace text) that this no longer crashes.
  2. **A real methodological confound, caught before drawing a wrong conclusion from the partial data that DID complete**: every scenario showing a large output-token "cost" lined up exactly with a large `adaptive_budget` max_tokens widen (`code`: 60→960 tokens widened, cost 900; `delta_hit`: →480, cost 427; `long_history`: →480, cost 330) -- the same shape as the truncation confound `benchmark_runner.py usage` already hit once and fixed with `_REALISTIC_MAX_TOKENS_FLOOR`. `dev_demo.py`'s scenarios deliberately use tiny caller-set `max_tokens` to demonstrate the widening mechanism firing at all, not to represent a realistic caller's own cap -- so the BASELINE call (still capped at the tiny original value) was a real truncation risk, and a "cost" here could mean "baseline is an incomplete fragment vs. a complete optimized answer," not real waste. This was flagged and investigated BEFORE concluding output tokens were being wasted, not after.
  - [x] Added `stop_reason` visibility: printed unconditionally in `_measure_usage()`'s output (free -- already-fetched response objects, no new API calls), and `baseline_truncated`/`optimized_truncated` booleans (derived from `stop_reason == "max_tokens"`) logged into the `generation_usage` event.
  - [x] `GenerationUsageStats` gained `baseline_truncated_count`/`optimized_truncated_count`; the dashboard's "Output tokens saved" card now shows a caveat ("N/M baseline replies were truncated...") whenever any baseline call was genuinely truncated, so a negative number there can't be misread as pure waste without that context visible.
  - [x] 4 new tests (2 aggregate, 2 render), 322 total, ruff/mypy clean.
  - [x] **Clean re-run happened (2026-09-06), confirming the truncation-confound diagnosis exactly.** Full run completed without crashing (the `.rstrip()` fix held). `stop_reason` broke the total cleanly into two groups: 4 scenarios (`code`, `long_history`, `caller_sets_thinking`, `near_dup`) hit `stop_reason="max_tokens"` on BOTH sides and accounted for -2050 of the total -2337 output-token "cost" (~88%) -- the baseline cut off almost immediately at its tiny original cap, the optimized side cut off later after writing more, not a fair comparison. Excluding those four, the clean comparisons landed close to neutral (-287 net), and the mechanisms actually designed to save output tokens showed real, positive savings once the confound was removed (`dedup` +36, `irrelevant_tool` +19, `extraction` +12 -- structured_output beat even a truncated free-text baseline). The one clean, non-truncation-confounded loss (`delta_hit`, -337, both sides `end_turn`) is a genuine, if minor, real finding: given far more headroom, the model sometimes writes a longer, complete answer even without being forced to -- a real quality-for-cost tradeoff, not a bug.
  - [x] **Fixed at the root, not just diagnosed**: added `_REALISTIC_MAX_TOKENS_FLOOR = 2048` + `_with_realistic_max_tokens()` to `dev_demo.py`, mirroring `benchmark_runner.py`'s already-established fix for the identical confound. Applied only inside `_measure_usage()` (the real-completion A/B path) to BOTH the baseline and pipeline calls -- scoped narrowly, not touching `scenario.kwargs` or the plain (non-`--measure-usage`) demo path, which still needs the real tiny caller-set value to demonstrate `adaptive_budget`'s widening mechanism firing at all. Confirmed offline: `code` scenario's `max_tokens` floors from 60 to 2048 inside `_measure_usage()`, while `SCENARIOS["code"].kwargs["max_tokens"]` itself stays 60.
  - [x] 322 tests total, ruff/mypy clean.
  - [x] **Clean re-run with the floor applied, against a fresh log file (2026-09-06): net -324 (-7.7%) across 12 samples** -- a world away from the confounded -150%/-175.8% seen before the fix, confirming the diagnosis directly rather than just arguing it from the old data. This is the honest answer to "are we saving output tokens": roughly neutral, with a small real residual cost consistent with the one genuine (non-truncation) finding already identified (`delta_hit`-style: more headroom occasionally leads to a longer, complete real answer even without being forced to -- a real quality-for-cost tradeoff, not a bug, and not the dramatic loss the confounded numbers implied).
  - [x] Noted, then fixed same day: `--all-scenarios` never logged `log_stage()` stage-level entries to `--log-to` (that path uses its own internal in-memory logger per scenario) -- only `generation_usage` events from `--measure-usage` flowed into the file in that mode. User asked directly why the dashboard's input-token stage table was empty.
- [x] **Fixed `_run_prepare`/`_persist_stage_entries` to replay stage entries into the file logger** (the same pattern `mcp_server.py`'s `_persist_if_configured` already established), wired at both `--all-scenarios` and single-scenario `--prepare-only` call sites.
- [x] **A real bug in that first fix, caught immediately by the user actually looking at the dashboard**: passed the SAME shared `FileCostLogger` instance across every scenario in the `--all-scenarios` loop. `FileCostLogger`'s `run_id` is fixed at construction and shared by every `log_stage()` call through it (by design -- "one fresh `Tokenetics()`, and therefore one fresh logger, per request"), so every scenario's replayed entries got tagged with the SAME `run_id`, and `aggregate()`'s per-run grouping treated a dozen unrelated scenarios as one giant "run" -- comparing the first stage's `tokens_before` from whichever scenario logged first against the last stage's `tokens_after` from a totally different scenario. Real, confirmed symptom: a dashboard showing `30 -> 503 tokens (saved -473, -1576.7%)`.
- [x] **Fixed**: construct a fresh `FileCostLogger(args.log_to)` per scenario inside the `--all-scenarios` loop (own `run_id` each), instead of reusing the one outer instance -- each scenario is conceptually a separate request, so each needs its own run_id, matching the project's own established convention rather than fighting it. Confirmed offline: 2 scenarios now produce 2 distinct `run_id`s in the log file.
- [x] 322 tests total (unchanged -- this bug and its fix were both only reachable via the real `--all-scenarios --log-to` path, not covered by existing dashboard/aggregate unit tests, which already assume correct run_id grouping as an input rather than testing dev_demo.py's own construction of it), ruff/mypy clean.
- [x] **Also beautified the output-tokens section** ("beautify the output tokens section similar to input tokens section"): added a `.sub-hero` CSS class (smaller sibling of the main `.hero`, same color-coded/badge visual language) and moved `generation_usage`'s "Output tokens saved" card into it -- consistent visual prominence with the main input-tokens hero, instead of sitting as a plain, easy-to-miss card among the other Tier 2 mechanisms.

## Opt-in AGGRESSIVE brevity (`brevity_injector`) -- built 2026-09-06

Prompted by the user asking, after seeing output tokens land close to neutral, whether the already-defined-but-dormant `AGGRESSIVE_INSTRUCTION` bucket (Chain-of-Draft style, never auto-selected since the classifier's categories are too broad to trust generally) could be tried for real savings. Explicitly scoped as a careful experiment, not a blind rollout, per the user's own choice when asked.

- [x] **`brevity_injector.py`**: new opt-in `config["aggressive_for_task_types"]` (a caller-supplied list of task types that get AGGRESSIVE instead of the default bucket) -- empty/off by default, same conservative-default-plus-explicit-opt-in pattern as `adaptive_budget`'s `enable_thinking_effort`. Default behavior (MODERATE on `extraction`, nothing elsewhere) completely unchanged.
- [x] New `dev_demo.py` scenario (`aggressive_brevity`) and a new quality-check sample (`quality_brevity_aggressive_001`), both opting AGGRESSIVE in for a genuinely simple, single-fact `code` question -- deliberately the narrow, low-risk case the module's own docstring says was never validated.
- [x] **A real bug caught immediately, before spending any money**: the first version of both the scenario and the quality-check sample used a prose-only question with no code fence/keyword -- `task_classifier` correctly (per its own deterministic rules) tagged it `conversational`, not `code`, so `aggressive_for_task_types: ["code"]` never matched and AGGRESSIVE silently never fired. The exact same classifier-bucket lesson `quality_adaptive_budget_002` already taught this project, caught again in a new context. Fixed by adding a real code fence to both; confirmed offline (stub client, no API key needed) that the sample now classifies `code` and the prepared `system` is the AGGRESSIVE instruction.
- [x] New test coverage: 5 new stage-level tests in `test_brevity_injector.py` (off by default even for `code`, fires when opted in, doesn't leak to other task types, correctly overrides the default MODERATE bucket, notes `brevity_bucket="aggressive"`), plus a new assertion in `test_real_quality_check_samples_actually_engage_their_mechanism` confirming the new sample actually engages AGGRESSIVE via `stage_config`, mirroring the existing convention for every other stage_config-gated sample.
- [x] 327 tests total, ruff/mypy clean.
- [x] **Real quality-check pass (2026-09-06): 3/3 passed with `brevity_injector` enabled, 3/3 with it disabled** (`quality-check --stage brevity_injector`), including the new `quality_brevity_aggressive_001` sample -- the real completion correctly and concisely distinguished `==` (value equality) from `is` (identity) even under AGGRESSIVE's ~5-word-reasoning-step pressure: `"==` compares values -> True (same contents)\n`is` compares identity -> False (different objects in memory)"`. Quality holds up on this case.
- [x] **A second real gap caught getting the output-token A/B**: `--measure-usage` was only ever checked inside the `--all-scenarios` loop -- combined with a single `--scenario`, the flag was silently a no-op (accepted by argparse, read nowhere else). The user ran `--scenario aggressive_brevity --measure-usage` expecting the real with-vs-without diff and got a single plain completion instead, no way to test one scenario's real effect without paying for the entire `--all-scenarios` battery. Fixed: single-scenario mode now calls `_measure_usage()` directly when `--measure-usage` is set (mirroring how `--all-scenarios` already uses it), scoped as an alternative to the other demo flags (`--compress-ratio`/`--semantic-cache`/`--tale`/`--show-thinking`) rather than combined with them, the same scope `--all-scenarios` itself already has. Confirmed offline (mocked client) that the real with-vs-without diff section now appears in single-scenario output. 327 tests total, ruff/mypy clean.
- [x] **Real output-token A/B happened (2026-09-06): a strong, clean positive result on the first sample.** `output_tokens`: 468 -> 68 (saved 400, 85.5%), with `stop_reason="end_turn"` on BOTH sides -- not the truncation confound, a genuinely complete answer on both ends. Input side cost +43 tokens (the instruction itself), a clear net win once output's ~5x-higher price is factored in. n=1 at this point -- explicitly not yet broad evidence.
- [x] **Broadened to n=3, per the user asking for more quality checks before trusting one sample.** Added `quality_brevity_aggressive_002` (mutable default argument gotcha) and `quality_brevity_aggressive_003` (shallow vs. deep copy) -- different simple, single-fact/two-sided code questions, each with a real code fence (same classifier-bucket lesson applied proactively this time, not caught after the fact) and confirmed offline, before any real spend, that both classify `code` and correctly produce the AGGRESSIVE system prompt. New table-driven assertion in `test_real_quality_check_samples_actually_engage_their_mechanism` covers all three samples. 327 tests total, ruff/mypy clean.
- [x] **Real quality-check run across all 5 samples (2026-09-06): 5/5 passed with the stage enabled, 5/5 with it disabled.** Correctness held across all three AGGRESSIVE cases (`==`/`is`, mutable default arguments, shallow vs. deep copy), not just the first one that already worked.

**Where this experiment lands after n=3**: strong, real, repeated evidence (3/3 quality, 1/1 output-token measurement at 85.5% savings) for the deliberately narrow shape this was scoped to -- a single, genuinely simple, one-fact-or-one-distinction code question. This is NOT yet evidence for the broader `code` category as a whole: none of the three samples approach the "one-line fix vs. multi-file design question" range `brevity_injector.py`'s own docstring warns `code` actually spans, which is exactly why AGGRESSIVE was never auto-selected for the whole category in the first place. The honest scope of what's been validated is "simple single-fact code questions," not "code questions."

### Testing the actual risk case: harder code questions (2026-09-06)

Per the user asking to test "on code now" -- i.e. the broader, harder end of the category the experiment above deliberately hadn't touched yet, not more easy wins.

- [x] **`quality_brevity_aggressive_004`**: a real bug-fix with a genuine edge case (same shape as `quality_adaptive_budget_001` -- `second_largest()`'s off-by-one on lists with fewer than 2 unique values), scoped to `brevity_injector` this time. Tests whether AGGRESSIVE's terseness pressure causes it to miss the edge case or give a broken fix.
- [x] **`quality_brevity_aggressive_005`**: a genuine two-part complexity/design question (naive recursive `fib()` -- why it's exponential-time AND how to fix it), the closer-to-real-design-question shape the docstring specifically warns about.
- [x] Both confirmed offline (no spend) to classify `code` and correctly produce the AGGRESSIVE system prompt before being added to the real suite. `test_real_quality_check_samples_actually_engage_their_mechanism` extended to cover both. 327 tests total, ruff/mypy clean.
- [x] **Real run happened (2026-09-06): 5/7 passed with the stage enabled, 7/7 without -- both failures traced to a grading-rubric gap, not a real quality regression, verified against the exact real response text before concluding either way.**
  - `quality_brevity_aggressive_001`'s real response (fully correct): `"== compares values, is compares identity"` -- failed only because `required_elements` expected "equal"/"equality"/"same value" and got "compares values" instead.
  - `quality_brevity_aggressive_004`'s real response (fully correct, working fix included): `"Crashes if list has <2 unique values (IndexError)"` -- failed only because it wrote the symbol `"<2"` instead of the words `"fewer than 2"`.
  - **A real, generalizable pattern, not just two isolated wording misses**: both failures are AGGRESSIVE's own terse style favoring compact/symbolic phrasing ("compares values", "<2") over the fuller wording a rubric written for normal-verbosity answers expects -- the same class of gap `quality_adaptive_budget_001` already taught this project once, now confirmed to recur specifically WHEN a stage is deliberately compressing output, worth remembering for any future AGGRESSIVE-adjacent sample.
  - Fixed by broadening both `required_elements` OR-groups (adding `"compares value"` to _001; `"<2"`/`"< 2"` to _004) -- confirmed offline, against the EXACT real response text pasted by the user, that both now pass, before asking for another real re-run rather than guessing the fix worked. 327 tests total (unchanged -- rubric wording only), ruff/mypy clean.
- [x] **Re-confirmed, real run: 6/7 with the stage enabled, 7/7 without -- both previously-fixed samples (`_001`, `_004`) now pass, INCLUDING the two harder cases this whole test was for.** One NEW failure appeared: `quality_brevity_aggressive_003` (real response, fully correct: `"b shares inner lists with a; c fully independent"`) failed for the exact same reason class, a third time -- rubric expected "shared inner"/"share the inner", real answer used present-tense "shares inner" instead.
- [x] **This is now a named, generalizable pattern, not three coincidences**: exact-substring grading against AGGRESSIVE's terse, grammatically-compressed output keeps missing correct answers phrased slightly differently than anticipated (`"compares values"` vs "equal"; `"<2"` vs "fewer than 2"; `"shares inner"` vs "shared inner"). Fixed `_003`'s `required_elements` more generously this time (added `"shares inner"`, `"share inner"`, `"alias"`, not just the one phrase observed) rather than continuing to chase single phrases reactively -- confirmed offline against the exact real response text before any further spend. 327 tests total, ruff/mypy clean.
- [x] **Clean run achieved (2026-09-06): 7/7 passed with the stage enabled, 7/7 without.** All three real failures this investigation surfaced were grading-rubric gaps, never an actual wrong answer, and all three are now confirmed fixed against fresh, independently-generated completions (not just the specific responses that originally exposed each gap).

**Final status of this experiment (as of the clean 7/7 quality run)**: quality holds across the full range tested. Real output-token measurement was still n=1 at that point -- only `quality_brevity_aggressive_001`'s underlying scenario had been measured for real.

### Closing the output-token measurement gap for the harder cases (2026-09-06)

Per the user asking for proper testing before considering expanding this further. Added two new `dev_demo.py` scenarios, `aggressive_brevity_hard_bugfix` and `aggressive_brevity_hard_complexity`, reusing the exact same content as `quality_brevity_aggressive_004`/`_005` (both already quality-confirmed correct twice for real) so the real output-token A/B can finally be measured on the harder cases, not just the simple `==`/`is` one.

- [x] Both confirmed offline (no spend) to classify `code` and correctly produce the AGGRESSIVE system prompt before being added.
- [x] 327 tests total (unchanged -- new scenarios, no source module touched), ruff/mypy clean.
- [x] **Not yet run for real** -- handed back to the user:
  - `uv run python scripts/dev_demo.py --scenario aggressive_brevity_hard_bugfix --measure-usage --log-to costs.jsonl`
  - `uv run python scripts/dev_demo.py --scenario aggressive_brevity_hard_complexity --measure-usage --log-to costs.jsonl`

  This is the real answer to whether the 85.5% output-token savings seen on the simple case holds up, shrinks, or reverses once the answer genuinely needs more content -- the last open question before this experiment could honestly be called complete.

- [x] **Real run happened (2026-09-06): output-token savings hold up, and the ABSOLUTE savings are larger on the harder questions than the simple one.**

  | Scenario | output tokens | saved | stop_reason (both sides) |
  |---|---|---|---|
  | Simple (`==`/`is`) | 468 -> 68 | 400 (85.5%) | `end_turn` |
  | Hard bug-fix | 636 -> 145 | 491 (77.2%) | `end_turn` |
  | Hard complexity | 1041 -> 282 | 759 (72.9%) | `end_turn` |

  All three clean (`stop_reason="end_turn"` on both sides every time -- never the truncation confound). The pattern makes sense on inspection: harder questions produce more verbose BASELINE answers (636/1041 raw tokens vs. 468 for the simple case), and AGGRESSIVE reliably compresses each down to a complete, correct answer regardless of difficulty. Input cost stays small and roughly constant (~43 tokens, the instruction itself) across all three -- completely swamped by output savings, and further favorable once output's ~5x-higher per-token price for Sonnet 5 is factored in.

**Final status of this whole experiment**: fully validated across everything it was scoped to test -- 5 real quality-check samples (3 simple, 2 genuinely harder) passing correctness with the stage both on and off across two independent real runs, PLUS 3 real output-token measurements (n=3, not n=1) all showing large, clean, non-truncation-confounded savings that hold up (and even grow in absolute terms) on harder questions. `aggressive_for_task_types` remains a caller-supplied opt-in, not a new default -- that would be a separate, bigger decision the user hasn't asked for, and this validation was explicitly scoped to "does the opt-in work correctly and pay off when used," not "should it be on by default."

## Extending AGGRESSIVE brevity to a new task type: `conversational` (2026-09-06)

Per the user asking to "expand its implementation now" -- given three real, distinct expansion paths (finer-grained shape detection to safely default it for a validated slice of `code`; testing + opt-in for more task types; or just defaulting it on for all of `code`), the user chose the middle path: extend the same rigorous validation to a new task type, staying opt-in throughout. Chose `conversational` first (very common category, natural "explain X" shape, same kind of terseness opportunity already validated for `code`).

- [x] Confirmed offline first: `"Why is the sky blue?"` and `"What's the actual difference between weather and climate?"` both classify `conversational` (0.4 confidence, the classifier's weak catch-all default) with zero special engineering needed -- unlike `code`, no code fence was required here.
- [x] Two new quality-check samples (`quality_brevity_aggressive_006`/`_007`): a simple single-fact question (why the sky is blue -- Rayleigh scattering) and a harder two-sided distinction (weather vs. climate, short-term vs. long-term), mirroring the exact simple+hard methodology already validated for `code`. `required_elements` written broadly from the start this time (not reactively patched after a real failure), having now seen the AGGRESSIVE-terse-phrasing-breaks-exact-substring-match pattern three times on the code samples.
- [x] Two new `dev_demo.py` scenarios (`aggressive_brevity_conversational_simple`/`_hard`) for the real output-token A/B, reusing the same content as the quality-check samples.
- [x] All four confirmed offline (no spend) to classify `conversational` and correctly produce the AGGRESSIVE system prompt before any real money is spent.
- [x] `test_real_quality_check_samples_actually_engage_their_mechanism` extended to cover both new samples. 327 tests total, ruff/mypy clean.
- [x] **Not yet run for real** -- handed back to the user:
  - `uv run python scripts/benchmark_runner.py quality-check --stage brevity_injector --confirm-spend` (now 9 samples: 2 extraction/MODERATE, 5 code/AGGRESSIVE, 2 conversational/AGGRESSIVE)
  - `uv run python scripts/dev_demo.py --scenario aggressive_brevity_conversational_simple --measure-usage --log-to costs.jsonl`
  - `uv run python scripts/dev_demo.py --scenario aggressive_brevity_conversational_hard --measure-usage --log-to costs.jsonl`

  `tool-heavy` is the remaining untested task type if this continues to hold up.

- [x] **Real run happened (2026-09-06): clean sweep, 9/9 passed both with and without the stage enabled** -- both new conversational samples correct on the first real run, no rubric gap this time (broadened proactively rather than reactively).
- [x] **Real output-token A/B, both clean (`stop_reason="end_turn"` on every side, every time)**: simple (why is the sky blue) 532 -> 98, saved 434 (81.6%); hard (weather vs. climate) 645 -> 184, saved 461 (71.5%). Same shape as the `code` results -- simple questions save a slightly higher percentage, harder questions save more in absolute tokens.

**Status after two task types**: `code` and `conversational` now both have the full validation (quality both on/off across simple+hard cases, real output-token measurement on 3+2 real scenarios respectively) -- 5 real output-token measurements total, all clean, all strongly positive, same consistent shape throughout. `tool-heavy` remains the one classifier category never tested; `extraction` already has its own default (MODERATE), never tried against AGGRESSIVE specifically. `aggressive_for_task_types` remains opt-in for both validated types.

## Extending AGGRESSIVE brevity to `tool-heavy` (2026-09-06)

The last untested major classifier category. Needed a genuinely realistic setup: a real `tool_use`/`tool_result` exchange already in message history (so `task_classifier`'s structural scan fires at 0.9 confidence, not the weaker text-based signal), ending in a real follow-up question that requires interpreting the tool result rather than just repeating it.

- [x] Designed a 5-turn stock-price-lookup conversation (`get_stock_price` tool, a real `tool_use`/`tool_result` pair already in history), confirmed offline to classify `tool-heavy` at 0.9 confidence.
- [x] Two quality-check samples: `quality_brevity_aggressive_008` (simple -- "is $181.50 higher or lower than $165?", answer: "higher") and `_009` (harder -- "what percentage increase is that?", answer: exactly 10%, clean numbers chosen specifically to avoid rounding-precision grading ambiguity).
- [x] Two matching `dev_demo.py` scenarios (`aggressive_brevity_tool_heavy_simple`/`_hard`) for the real output-token A/B, reusing the same conversation content.
- [x] All four confirmed offline (no spend) to classify `tool-heavy` and correctly produce the AGGRESSIVE system prompt.
- [x] `test_real_quality_check_samples_actually_engage_their_mechanism` extended to cover both. 327 tests total, ruff/mypy clean.
- [x] **Not yet run for real** -- handed back to the user:
  - `uv run python scripts/benchmark_runner.py quality-check --stage brevity_injector --confirm-spend` (now 11 samples across all 4 categories: extraction, code, conversational, tool-heavy)
  - `uv run python scripts/dev_demo.py --scenario aggressive_brevity_tool_heavy_simple --measure-usage --log-to costs.jsonl`
  - `uv run python scripts/dev_demo.py --scenario aggressive_brevity_tool_heavy_hard --measure-usage --log-to costs.jsonl`

  If this holds up, all 4 of `task_classifier`'s categories will have real evidence behind the AGGRESSIVE opt-in -- `extraction` (tested against AGGRESSIVE specifically, not just MODERATE) would be the only remaining gap.

- [x] **Blocked, not failed**: all three real-run attempts (2026-09-06) hit `anthropic.BadRequestError: "Your credit balance is too low to access the Anthropic API"` -- a real account-billing condition, not a code or test bug. All `tool-heavy` work (both quality-check samples, both `dev_demo.py` scenarios) is fully built and already confirmed offline; nothing needs to change once credits are added. Paused here pending the user topping up their Anthropic account.
- [x] **User topped up; real run completed (2026-09-06): 11/11 with the stage, 10/11 without.**
  - [x] **A real grading bug, but a genuinely different one this time**: the ONE failure was on the WITHOUT-`brevity_injector` baseline (`quality_brevity_aggressive_007`), not an AGGRESSIVE response -- the real reply said `"day to day"` (spaces), the rubric expected `"day-to-day"` (hyphenated). Fixed by adding the unhyphenated form. **Correction to the earlier working theory**: this exact-substring-grading fragility is not an AGGRESSIVE-specific artifact (four of the five prior instances happened to be AGGRESSIVE responses, but this one wasn't) -- it's a general weakness of the grading method itself, which can trip on any real response regardless of verbosity. Worth remembering as the honest, corrected version of the pattern named earlier in this document.
  - [x] **A genuinely new, real, structural finding on `tool-heavy`'s output-token savings -- smaller than `code`/`conversational`, but real and net-positive**: simple 71 -> 40 (saved 31, 43.7%); hard 85 -> 53 (saved 32, 37.6%). Both clean (`stop_reason="end_turn"` both sides). Compare to `code`/`conversational`'s 71.5-85.5% savings on baselines of 468-1041 tokens. The likely cause, not just a guess: `tool-heavy` follow-ups here are short factual questions after a tool result already provided the data -- the unprompted baseline reply is ALREADY fairly terse in that conversational shape (71/85 tokens), unlike an open "explain X" question that invites a long, discursive baseline (468-1041 tokens). AGGRESSIVE can only save what the baseline would have spent on filler, and there's much less filler here to begin with. Real dollar math (Sonnet 5, $2/$10 per MTok): net savings ~$0.0002-0.0003 per call here, vs. ~$0.004-0.008 on `code`/`conversational` -- still positive (output's 5x-higher price still outweighs the fixed ~43-token input cost), but a much thinner margin.

**Final status across all task types tested**: `code`, `conversational`, and `tool-heavy` all show real, clean, NET-POSITIVE output-token savings for the AGGRESSIVE opt-in -- but the SIZE of that benefit varies a lot by how verbose the untouched baseline naturally is for that conversational shape, not just by task-type label. `extraction` (tested only against MODERATE, never AGGRESSIVE specifically) remains the one true gap if this is extended further.

## Extending AGGRESSIVE brevity to `extraction` (2026-09-06) -- the last untested task type

Per the user asking to keep going. A real design nuance flagged before building: extraction requests typically have a matching tool registered, which makes `structured_output` force a JSON `tool_choice` reply -- already compact structured data, not prose. Whether AGGRESSIVE adds anything on top of that (or is largely redundant with it) is exactly what this measures, not assumed either way.

- [x] Two new questions confirmed offline to classify `extraction` at 0.8 confidence: a simple 2-field extraction (name/email) and a harder multi-item extraction (two action items, each with owner + due date).
- [x] Two quality-check samples (`quality_brevity_aggressive_010`/`_011`) and two matching `dev_demo.py` scenarios (`aggressive_brevity_extraction_simple`/`_hard`), each with a realistic registered tool so `structured_output` also fires alongside AGGRESSIVE -- the same combined-mechanism shape a real extraction call would actually have.
- [x] All four confirmed offline (no spend) to classify `extraction`, engage AGGRESSIVE, AND confirm `structured_output`'s `tool_choice_forced` also fires -- the intended combined setup, not accidentally testing AGGRESSIVE in isolation.
- [x] `test_real_quality_check_samples_actually_engage_their_mechanism` extended to cover both. 327 tests total, ruff/mypy clean.
- [x] **Not yet run for real** -- handed back to the user:
  - `uv run python scripts/benchmark_runner.py quality-check --stage brevity_injector --confirm-spend` (now 13 samples -- all 4 task-classifier categories represented)
  - `uv run python scripts/dev_demo.py --scenario aggressive_brevity_extraction_simple --measure-usage --log-to costs.jsonl`
  - `uv run python scripts/dev_demo.py --scenario aggressive_brevity_extraction_hard --measure-usage --log-to costs.jsonl`

  This completes real-evidence coverage across every task-classifier category, closing out this entire AGGRESSIVE-brevity validation arc.

- [x] **Real run happened (2026-09-06): 12/13 quality-check passed both ways; extraction's real output-token economics look different from every other category, and investigating why led to a real, confirmed bug in the core tokenizer utility -- bigger than this experiment.**
  - [x] One more wording-only grading gap (fixed): a WITHOUT-`brevity_injector` baseline correctly said `"10.0%"` where the rubric only had `"10%"`. Same general exact-substring fragility already named, not stage-specific -- confirmed for a second time on a baseline response, not an AGGRESSIVE one.
  - [x] **A real, confirmed bug in `core/tokenizer.py`, found via extraction's anomalous real economics, not assumed**: both extraction A/B scenarios showed real `usage.input_tokens` jumping by ~171-172 tokens, while this project's own before/after counting showed only ~43 (the brevity instruction alone) -- a consistent ~128-129 token gap across two independent samples. Root cause confirmed against Anthropic's real API reference for `POST /v1/messages/count_tokens`: `tool_choice` (which `structured_output` sets) is a documented, accepted, count-affecting parameter, and `count_tokens()`'s `_COUNT_TOKENS_KEYS` excluded it on the wrong assumption it was "generation-only like max_tokens." The magnitude matches Anthropic's own documented tool-choice system-prompt overhead difference for Sonnet 5. **This is not a new bug from this experiment -- it's a measurement gap that predates AGGRESSIVE entirely, first exercised by a real completion-based A/B here because this is the first time in this whole investigation that `structured_output` was active during one.** Every past request-side token count this project has ever logged for a `structured_output`-forced call (dashboards, corpus/usage benchmark runs, quality-check cost estimates) has under-counted real input tokens to some degree; historical numbers are not being retroactively corrected, but should be read with this caveat.
  - [x] **Fixed thoroughly, not narrowly**: added `tool_choice` AND `thinking`/`output_config` (also documented, accepted `count_tokens` parameters -- the latter two not individually confirmed empirically the way `tool_choice` was, included on the same "forward what the real endpoint documents as count-affecting" principle rather than only patching the one observed case). 2 new regression tests in `test_tokenizer.py`. 329 tests total, ruff/mypy clean. No existing test broke -- purely additive (only forwards a field when the request actually sets it).
  - [x] **Extraction's real economics, now correctly measurable**: simple case net a real, tiny loss (input +172 tok / $0.000344, output -32 tok saved / $0.00032 -- net -$0.000024); hard case a modest real gain (+171 tok / $0.000342 input, -65 tok saved / $0.00065 output -- net +$0.000308). The first category tested where the economics aren't cleanly positive -- but this reflects `structured_output`'s own real, now-accurately-measured cost, not a flaw in AGGRESSIVE brevity itself.
  - [x] **The empty-response finding on `quality_brevity_aggressive_003` remains open** -- added `stop_reason` visibility to `benchmark_runner.py`'s quality-check output (it had none before) specifically so a re-run can diagnose this with real evidence instead of guessing. Not yet re-run.
- [x] **Re-run (2026-09-06): clean sweep, 13/13 passed both ways, including `quality_brevity_aggressive_003`.** The empty response did not reproduce -- one occurrence in 4 real runs of that sample, no `stop_reason` data from the original failure (it predates the diagnostic being added), no other sample has ever shown this. Honest read: a rare, non-deterministic generation flake, not a systematic AGGRESSIVE issue -- noted rather than either dismissed or treated as unresolved, and the diagnostic is now in place if it recurs.
- [x] **Tokenizer fix confirmed for real, exactly as predicted**: re-ran both extraction `_measure_usage()` scenarios -- Tokenetics' own request-token accounting now EXACTLY matches real `usage.input_tokens` (simple: 490->662 both ways; hard: 596->767 both ways), with `structured_output`'s real ~128-129 token cost now correctly attributed in the per-stage log instead of being invisible. Real dollar net for extraction, now fully accurate: simple -$0.000034, hard -$0.000142 -- both essentially break-even to slightly negative, confirming the structural read that `structured_output`'s own forced-compact reply leaves little room for AGGRESSIVE to add net value on top of it, unlike every other category tested.

## Final status of the entire AGGRESSIVE-brevity validation arc (2026-09-06)

All 4 task-classifier categories now have real evidence behind the opt-in, plus one confirmed, project-wide core-infrastructure fix along the way:

| Category | Quality (real, both ways) | Real output-token savings | Net economics |
|---|---|---|---|
| `code` | 5/5 samples, clean | 72.9-85.5%, 3 real measurements | Strongly positive |
| `conversational` | 2/2 samples, clean | 71.5-81.6%, 2 real measurements | Strongly positive |
| `tool-heavy` | 2/2 samples, clean | 37.6-43.7%, 2 real measurements | Positive, thinner margin (baseline already terse) |
| `extraction` | 2/2 samples, clean | 20-32 tokens (variable), 2 real measurements | Break-even to slightly negative (structured_output already compact) |

`aggressive_for_task_types` remains a caller-supplied opt-in throughout -- no defaults changed. The real, unplanned bonus of this investigation: a confirmed, fixed gap in `core/tokenizer.py` (`tool_choice`/`thinking`/`output_config` were never forwarded to the real `count_tokens` endpoint) that improves measurement accuracy project-wide, not just for this feature -- found only because this was the first real completion-based A/B in the whole project to have `structured_output` active.

## Defaulting AGGRESSIVE for `code`/`conversational`/`tool-heavy` (2026-09-06)

The user asked to default AGGRESSIVE on for the three categories that came out net-positive above (explicitly excluding `extraction`, which was break-even/negative). Flagged one remaining gap before doing it: every sample validated above, even the "hard" ones (`_004`/`_005`), was still a short, single, self-contained question -- none tested the genuinely open-ended end of any category (a multi-file design question, an "explain the history of X" conversational prompt). Defaulting for the whole category as asked would apply AGGRESSIVE to that untested territory too. Given the choice between defaulting broadly anyway or narrowing the default to just the validated shape first, the user chose to narrow first.

- [x] **New signal: `request.meta.bounded_shape`**, computed by `task_classifier` (`stages/task_classifier.py`, `_is_bounded_shape`) alongside `task_type` -- deterministic, regex/word-count based (strips fenced code blocks first, then checks for open-ended markers like "design"/"architecture"/"walk me through", more than one real question, or prose over 40 words). True only for the short/self-contained shape actually validated above; ambiguous or ambiguous-adjacent input conservatively resolves to False, same posture as an unclassified `task_type`.
- [x] **`brevity_injector.py`**: `_DEFAULT_AGGRESSIVE_TASK_TYPES = {"code", "conversational", "tool-heavy"}` now gets AGGRESSIVE automatically, gated on `bounded_shape` being True for that specific request -- not a blanket category default. `extraction` deliberately stays out of the default set (its break-even economics), but `aggressive_for_task_types` remains available as an explicit opt-in for it (or for extending the default set) -- and the `bounded_shape` gate applies to that opt-in too, so an opted-in `extraction` request only gets AGGRESSIVE when it's also short/bounded (verified with `quality_brevity_aggressive_011`, the deliberately harder multi-item extraction sample -- it now falls back to MODERATE even with the flag set, since its prose is >40 words).
- [x] New test coverage: `test_task_classifier.py` (+7 tests for `_is_bounded_shape` -- short question, code-fenced question, open-ended marker, multiple questions, long prose, empty message, cost-logger note), `test_brevity_injector.py` (rewritten default/opt-in tests -- default fires per category only when bounded, stays off when shape is ambiguous or explicitly unbounded, opt-in still requires bounded_shape). `test_benchmark.py`'s mechanism-engagement check split: samples _001-_010 now assert AGGRESSIVE fires from the default (no config needed for code/conversational/tool-heavy), `_011` asserts it correctly falls back to MODERATE. 341 tests total, ruff/mypy clean.
- [x] Existing `quality_brevity_aggressive_*` samples' `stage_config` opt-ins are now redundant for code/conversational/tool-heavy (already covered by the default) but left in place -- harmless, and still meaningful for the two extraction samples. No changes needed to `dev_demo.py`'s scenarios for the same reason.

## Real, measured thinking-token tracking on the dashboard (2026-09-06)

The user asked to record thinking tokens on the dashboard. The dashboard already had `estimated_thinking_tokens_saved` (from the earlier "output/thinking token savings" work above) -- but its own docstring said Anthropic's `usage` object has no separate line item for thinking tokens, so it was only ever a derived estimate (output tokens minus visible-text tokens). Checked that claim against the currently-installed `anthropic` SDK (0.117.0) before building anything on top of it, rather than trusting the old note -- it turned out to be wrong: `usage.output_tokens_details.thinking_tokens` is a real, documented field ("Computed by re-tokenizing the raw reasoning text, so it may differ from the model's exact generation count by a small number of tokens" -- close to exact, not a guess). This is exactly CLAUDE.md's stage 9c line item 1 ("configured effort level vs. actual thinking tokens consumed at generation time"), previously unimplemented as a real measurement.

- [x] **`scripts/dev_demo.py`**: new `_thinking_tokens(response)` helper reads `usage.output_tokens_details.thinking_tokens` (returns `None`, not `0`, when a side never engaged thinking at all -- so "used 0 thinking tokens" stays distinguishable from "thinking wasn't on"). `_measure_usage()` now captures it for both sides, prints it unconditionally when either side has real data, and logs `baseline_thinking_tokens`/`optimized_thinking_tokens` as two more fields on the existing `generation_usage` event (not a new event type -- same real A/B, additional real fields, matching the pattern `baseline_truncated`/`optimized_truncated` already established).
- [x] **`dashboard/aggregate.py`**: `GenerationUsageStats` gained `thinking_sample_count`/`total_baseline_thinking_tokens`/`total_optimized_thinking_tokens` (real) and a `thinking_tokens_consumed` property (`optimized - baseline`, framed as "consumed" not "saved" since the baseline side almost always has no thinking config at all -- this is usually a real cost of opting into the effort knob, though it can go negative/net-positive in the rarer case where the caller's own baseline already had thinking on). Kept separate from `output_tokens_saved`/`estimated_thinking_tokens_saved` per CLAUDE.md's explicit "must not be conflated" rule for this exact pair of cost dimensions.
- [x] **`dashboard/render.py`**: new "Thinking tokens consumed (real, measured)" card, shown only once `thinking_sample_count > 0` -- falls back to the old estimated card otherwise, so a log with no real thinking samples yet doesn't silently show a `0` that looks like "measured zero cost." Scope note at the bottom of the page updated to describe the real field and when the fallback still applies.
- [x] New test coverage: `test_dashboard.py` (+4 -- real thinking-tokens aggregation, a negative-consumption case, and both render branches: real card present/estimate-card absent when data exists, estimate card present/real card absent when it doesn't). 344 tests total, ruff/mypy clean.

**Scope note, not yet built**: CLAUDE.md names a SECOND, distinct stage 9c line item -- tokens saved by `display: "omitted"` not being re-billed when history with a thinking block is echoed back on a later turn. That's a genuinely different measurement (a multi-turn input-token A/B: `display:"omitted"` vs `display:"summarized"` on the follow-up call), not a byproduct of this change. Not built here since it wasn't what was asked; worth doing as its own follow-up.

## A real gap found via the user's own dashboard hand-check: `_measure_usage()` never logged `cache_usage` (2026-09-06)

The user ran `--scenario cache_hit --measure-usage --log-to costs.jsonl` specifically to get a real cache read onto the dashboard (per the earlier "why not cache as well?" question) and the dashboard's cache section didn't move at all -- "Cache lookups logged" stayed at 23. Traced, not guessed: `_measure_usage()` (the only code path that actually makes a real cache-read repeat call, when the pipeline-enabled call paid a write) never called `logger.log_event("cache_usage", ...)` at all -- that call only existed in the plain, non---measure-usage single-scenario path further down the file. So every real cache write AND the real cache read `_measure_usage()` demonstrates were printed to stdout and immediately lost, never reaching the dashboard's `CacheUsageStats` aggregation.

- [x] **`scripts/dev_demo.py`**: `_measure_usage()` now logs a `cache_usage` event for the pipeline-enabled call's own usage (write, if any) right alongside its existing `generation_usage` log, and a second `cache_usage` event for the repeat call's usage (the real read) when that repeat call fires -- same event shape (`cache_read_input_tokens`/`cache_creation_input_tokens`/`input_tokens`) the single-scenario path already used, so `aggregate_events()` needed no changes at all.
- [x] Docstring updated to name the gap and the fix explicitly, matching this project's convention of documenting a real bug where it was found rather than silently patching it.
- [x] No new test added -- `dev_demo.py` has no direct pytest coverage by project convention (validated by hand-running against the real API, same as every other `_measure_usage()` change this session). 344 tests still pass, ruff/mypy clean.

## Closing the second stage 9c gap: thinking re-injection savings on the dashboard (2026-09-06)

The earlier "Real, measured thinking-token tracking" work explicitly flagged CLAUDE.md's SECOND stage 9c line item as not yet built: tokens saved by `display: "omitted"` not being re-billed when a thinking block is echoed back as history on a later turn. The user asked for it directly ("is there no way we could show the thinking tokens saved on dashboard too").

- [x] **`scripts/dev_demo.py`**: new standalone `--measure-thinking-reinjection` flag (ignores `--scenario`, uses a fixed built-in code-question prompt) running `_measure_thinking_reinjection()` -- a real 2-turn, 2-thread A/B. Both threads send the IDENTICAL first-turn prompt and follow-up, differing only in `thinking.display` (`"omitted"` vs `"summarized"`); turn 2's real `usage.input_tokens` on each thread is the measured comparison. `response.content` is passed straight back as the next message's `content` -- confirmed via the installed SDK's own `MessageParam` type that `ContentBlock` (a real response's own blocks) is a directly accepted type, no re-serialization needed. Costs 4 real completions, logs a `thinking_reinjection` event.
- [x] **`dashboard/aggregate.py`**: new `ThinkingReinjectionStats` dataclass (`tokens_saved`/`pct_saved` properties), wired into `Tier2Stats`/`aggregate_events`/`has_any_data`. Kept as its own dataclass and event type, not folded into `GenerationUsageStats`, per CLAUDE.md's explicit rule that this and the generation-time thinking-tokens number are different cost dimensions that "must not be conflated."
- [x] **`dashboard/render.py`**: new "Thinking re-injection tokens saved (real, measured)" sub-hero card (green/red sign-aware, same pattern as the output-tokens card), shown once at least one sample exists; "no data logged" placeholder otherwise. Scope note updated to describe the distinction between this (later-turn re-injection cost) and the existing thinking-tokens-consumed card (this-turn generation cost).
- [x] New test coverage: `test_dashboard.py` (+3 -- aggregation math, the empty-state placeholder, the populated render card). 346 tests total, ruff/mypy clean.

This completes both of CLAUDE.md's stage 9c line items as real, measured dashboard data -- the generation-time thinking-tokens-consumed card (from the prior session) and this later-turn re-injection-savings card, kept honestly separate throughout rather than combined into one number.

## Output/thinking/combined token charts (2026-09-06)

The user asked for charts covering output tokens and thinking tokens specifically, plus a combined view across request/output/thinking together.

- [x] **`dashboard/render.py`**: two new baseline-vs-optimized bar charts (`tkOutputTokensBar`, `tkThinkingTokensBar`), each shown only once its underlying `generation_usage`/thinking data actually exists (same "no fabricated zero" discipline as every other Tier 2 card). A new combined `tkCombinedTokensBar` grouped bar chart plots Request/Output/Thinking side by side (baseline vs. optimized each), via new `_combined_tokens_chart_data()` -- each category independently omitted (not shown as a fabricated 0/0) when its data doesn't exist yet. `_render_charts()` restructured from the old "insert before the closing brace" pattern (fragile once more than one conditional chart existed) to a `chart_calls` list built up conditionally and joined once.
- [x] **Honesty caveat, not glossed over**: the combined chart spans two different sample populations -- "Request" covers every logged run (free, from the stage table), "Output"/"Thinking" cover only the smaller subset that went through a real `--measure-usage` A/B. Documented in three places: a code comment on `_combined_tokens_chart_data()`, the chart's own Chart.js title text, and the page's scope note -- so nobody reads it as a precise ratio.
- [x] New test coverage: `test_dashboard.py` (+2 -- output/thinking charts appear only with real data and show correct baseline/optimized values, combined chart omits categories with no underlying data and disappears entirely when there's no stage data at all). 348 tests total, ruff/mypy clean.

## A real methodological limitation found in `--measure-thinking-reinjection` (2026-09-06)

The user ran `--measure-thinking-reinjection` four times and asked why the total came out negative (-23 tokens, "RE-INJECTION COST"). Investigated with the real per-sample data rather than guessing: the four deltas were 1, 1, 0, and 21 -- no consistent direction, mostly near-zero. Root cause: each sample's `omitted` and `summarized` threads make INDEPENDENT real completions for turn 1 (extended thinking can't use `temperature=0`), so the model's exact reply text -- and therefore turn 2's full-conversation `input_tokens` count -- varies a little between the two threads for reasons that have nothing to do with `display`. At low n, that natural reply-length variance is comparable to or larger than the real re-injection effect being measured, so a handful of samples isn't reliable evidence of the size OR sign of the real saving.

- [x] **`scripts/dev_demo.py`**: `_measure_thinking_reinjection()`'s docstring now documents this noise floor explicitly, with the real confirming data (1/1/0/21 deltas) named rather than hand-waved.
- [x] **`dashboard/render.py`**: the "Thinking re-injection tokens saved" card now shows an explicit caveat below `_NOISE_FLOOR_SAMPLE_THRESHOLD = 10` samples, explaining why the number is speculative at low n -- disappears automatically once enough samples accumulate. A judgment-call threshold, not statistically derived.
- [x] New test coverage: `test_dashboard.py` (+1 -- caveat present below the threshold, absent above it). 349 tests total, ruff/mypy clean.

Not treated as a code bug to "fix" -- the measurement is doing exactly what it was built to do (a real, honest A/B), it's just revealing that the true effect is smaller than the noise floor at this sample size. The honest fix is documentation + more samples, not a design change to force determinism that the API doesn't support.

## A real mislabeling found on the "Thinking tokens consumed" card (2026-09-06)

The user asked whether running more `caller_sets_thinking`-style scenarios would raise the "Thinking tokens consumed" number, which prompted checking what was actually driving that number rather than assuming the dashboard's own label was correct. It wasn't: the card's subtext claimed this was "adaptive_budget's effort knob" cost, but `adaptive_budget`'s own thinking-effort opt-in had NEVER actually fired anywhere in the log -- `caller_sets_thinking` is specifically the corner case that proves the stage backs OFF when the caller already sets `thinking`, so its real thinking tokens come entirely from the caller's own config, not the stage. Worse: several scenarios with NO `thinking` config anywhere (neither caller-set nor `adaptive_budget`-set) still reported real nonzero `thinking_tokens` on their completely untouched baseline call -- e.g. the plain `code` scenario measured 45 real thinking tokens with no `thinking` parameter sent at all. The model can apparently report nonzero `output_tokens_details.thinking_tokens` independent of whether anyone explicitly requested extended thinking.

- [x] **`scripts/dev_demo.py`**: new `thinking_effort_code` scenario -- no caller-set `thinking`, `stage_config={"adaptive_budget": {"enable_thinking_effort": True}}` so a genuinely bounded `code` task_type actually drives the stage's own `_EFFORT_BY_TASK_TYPE["code"] = "high"` choice. Meant to be run with `--disable brevity_injector` (now a default for bounded `code` requests) to isolate the effort knob's real cost from every other stage's own token effect.
- [x] **`dashboard/render.py`/`dashboard/aggregate.py`**: the "Thinking tokens consumed" card's subtext and `GenerationUsageStats`'s docstring corrected -- no longer claim this total is "adaptive_budget's effort knob" cost; now honestly state the real finding (nonzero thinking tokens can appear with no thinking config anywhere) and point to `thinking_effort_code` as the scenario that actually isolates the knob's real incremental cost.
- [x] 349 tests still pass (no test asserted the old, now-corrected label text), ruff/mypy clean.

## Executive-facing overview charts + page restructure (2026-09-06)

The user asked for pie charts covering request/output/thinking tokens specifically, placed at the beginning of the dashboard, and for the page overall to read well for an executive audience rather than an analyst one.

- [x] **`dashboard/render.py`**: new `_render_overview_charts()` -- three Chart.js doughnuts (Request/Output/Thinking tokens), each a simple baseline-vs-optimized 2-slice split, shown only once its own underlying data exists (same no-fabricated-data discipline as everything else). `_overview_donut_title()` phrases the split honestly for all three real shapes: a genuine before/after ("X% saved" / "X% more spent"), a pure-cost metric with no baseline to compare against ("real cost, no baseline usage" -- thinking tokens usually start from a zero baseline, so this avoids a misleading "0.0%"), and the empty case.
- [x] **Page restructured** so the overview donuts sit right after the hero + summary cards, BEFORE the detailed stage table -- the exec-facing "one glance" section now comes first, with the denser technical material (stage table, per-stage min/median/max, the existing analyst-oriented pie/bar/donut charts, Tier 2 cards) pushed below it and given clearer section labels ("Detailed stage breakdown", "Detailed charts" -- renamed from the previous bare "Charts") so the page reads as summary-then-detail rather than one flat list.
- [x] New CSS: `.overview-wrap`/`.chart-card.overview` -- bigger, centered cards with more shadow/border presence than the detail charts, visually signaling "look here first."
- [x] New test coverage: `test_dashboard.py` (+2 -- overview section renders before both detail sections in document order; each donut appears only when its own data exists, with correct real values and the "no baseline usage" phrasing for the zero-baseline case). Existing chart tests updated to target the correct (now second) `<script>` block by position rather than assuming there's only one. 351 tests total, ruff/mypy clean.

## Relocating output/thinking donuts to their own sections + rename (2026-09-06)

Follow-up to the executive-overview work above: the user asked for the output and thinking donuts to live in their own relevant sections (next to the Output tokens saved / Thinking tokens consumed cards in Tier 2), the same way the request-tokens donut sits with the request-tokens hero -- and for the top section to be renamed "Savings overview".

- [x] **`dashboard/render.py`**: `_render_overview_charts()` split into `_render_savings_overview()` (request-tokens donut only, section retitled "Savings overview") and a new reusable `_render_donut_card()` (a self-contained canvas+script fragment, safe to drop anywhere in the page since the canvas always precedes its own script tag). Output/thinking donuts (`tkOutputDonut`/`tkThinkingDonut`) now render directly inside `_render_tier2_section()`, immediately after their respective cards.
- [x] **A real, caught-before-it-mattered test gap**: the first version of the "section appears before X/Y" test still referenced the OLD title string ("Executive overview") and kept passing after the rename -- not because the section still said that, but because a leftover CSS *comment* in `<style>` still contained the same words, and CSS comments are part of the raw HTML text a substring check sees. Fixed by renaming the CSS comment too and rewriting the test to check the exact rendered `<div class="section-title ...">` tag, not a bare substring -- the kind of false-pass this project's own testing discipline exists to catch.
- [x] Other tests updated to locate chart data by content proximity (e.g. searching near `id="tkOutputDonut"`) rather than assuming a fixed script position on the page, now that there are up to 4 independent `<script>` blocks depending on what data exists.
- [x] 351 tests total, ruff/mypy clean.

## Section title cleanup (2026-09-06)

The user flagged the section titles as sounding off -- "Detailed stage breakdown", "Detailed charts", and especially "Tier 2 / cache (from log_event())" (literally exposing an internal project-tier label and a Python function name as a page heading). Renamed for a page meant to read cleanly to a non-technical viewer:

- [x] "Tier 2 / cache (from log_event())" → **"Advanced optimizations"**
- [x] "Detailed charts" → **"Additional charts"**
- [x] "Detailed stage breakdown" → **"Pipeline stage breakdown"**
- [x] Footer scope-note copy updated to match the new names (still keeps the `log_stage()`/`log_event()` function references there specifically, since that's a small-print sourcing footnote aimed at whoever wants the implementation detail, not a page heading).
- [x] Tests updated to the new title strings. 351 tests total, ruff/mypy clean.

## Full dashboard restructure into token-type sections (2026-09-06)

The user asked for a more thorough reorganization: Savings overview (now 4 donuts -- Request, Output, Thinking, and a combined Request-vs-Output-vs-Thinking comparison) at the very top before any metric cards, then dedicated "Request tokens" / "Output tokens" / "Thinking tokens" sections each holding their own relevant cards and charts.

- [x] **Savings overview** now renders up to 4 donuts (`_render_savings_overview`): Request, Output, Thinking (each only when their own data exists), plus a combined donut (`_combined_donut_js`) showing all 6 baseline/optimized values as one 6-slice chart, colored in matched hue-pairs per category -- shown only once at least 2 categories have data, since a 1-category "combined" chart would just duplicate the individual donut above it. This section is now the literal first thing in `body`, before the hero.
- [x] **Request tokens section**: hero + summary cards + min/median/max (unchanged content, now under an explicit "Request tokens" heading) + the cache read/write donut (moved here from the old combined Tier 2 section -- cache_control affects price-per-token on the INPUT side, so this is a more honest home for it than Output) + Pipeline stage breakdown table + one %-based "tokens saved by stage" pie chart.
- [x] **Stage-savings chart consolidated**: the old pair (an absolute-tokens pie + a separate "% saved by stage" bar chart) is now ONE pie chart using `pct_saved` instead of `total_saved` -- the bar chart is gone entirely, per the user's explicit ask.
- [x] **Output tokens section**: the Output tokens saved sub-hero + its own donut, nothing else -- self-contained, shows an honest "no data logged" placeholder when empty.
- [x] **Thinking tokens section**: the Thinking tokens consumed card (or the Est. fallback) + its own donut + the Thinking re-injection sub-hero card, consolidated here since all three are thinking-token metrics.
- [x] **Remaining Tier 2 mechanisms** (TALE, compress, semantic-cache -- none of which are cleanly request/output/thinking-specific) kept in a renamed "Other optimizations" section at the bottom.
- [x] `_render_tier2_section`/`_render_charts`/`_before_after_chart_js`/`_bar_chart_data` all removed entirely -- their content was fully redistributed into the new per-token-type section functions, nothing left over.
- [x] Test suite rewritten to match: chart assertions now locate charts by canvas id / section-title proximity rather than assuming a fixed script position (there can be several independent `<script>` blocks now), no-data placeholder text split per new section, and the combined-donut test updated for the new "needs >=2 categories" gate. 351 tests total, ruff/mypy clean.

## Request tokens section donut swap (2026-09-06)

The user asked to replace the Request tokens section's cache read/write donut with a request-tokens baseline-vs-optimized donut instead -- matching the pattern Output/Thinking sections already have (each with exactly one donut for their own metric).

- [x] **`dashboard/render.py`**: new `_render_request_tokens_donut()` renders `tkRequestDonut` (same baseline-vs-optimized shape as `tkOutputDonut`/`tkThinkingDonut`), placed right after the hero/summary cards. `_render_cache_subsection()` no longer renders a donut -- cache data now shows as cards only (lookups, hit rate, read tokens, write tokens), which is still real, honest data, just no longer charted.
- [x] Test updated (`test_render_html_cache_cards_only_appear_with_real_cache_data`, renamed from the old donut-specific test) to confirm cache cards still render with real data, `tkCacheDonut` no longer appears anywhere, and `tkRequestDonut` appears alongside it. 351 tests total, ruff/mypy clean.

## A real technique to reduce thinking-token generation: bounded-shape effort downgrade (2026-09-06)

The user pushed back that the dashboard's "Thinking tokens consumed" card reads as an uncounted extra loss, then asked directly whether any technique exists to actually SAVE thinking tokens (not just track their cost). Investigated first: confirmed `thinking_tokens` is documented as "Always &le; `output_tokens`" (a subset, not additive) -- fixed the dashboard's wording so this card no longer reads as a second loss stacked on the real output-token savings. Then addressed the real ask: the only lever the Anthropic API exposes for reducing thinking-token *generation* (not just tracking it) with `type: "adaptive"` thinking is `output_config.effort` -- already implemented, but only chosen by task_type, a coarse per-category default with no regard for whether a given request actually needs that much depth.

- [x] **`stages/adaptive_budget.py`**: `request.meta.bounded_shape` (the same signal `brevity_injector`'s AGGRESSIVE default already uses) now downgrades the task-type effort by one level for a short/single-question/self-contained request -- `high`->`medium`, `medium`->`low`, `low` stays `low`. Ambiguous or unbounded shape leaves the task-type default untouched (conservative, matches the project's standing default-to-safe rule).
- [x] New test coverage: `test_adaptive_budget.py` (+4 -- downgrade fires correctly at each level, `low` has nowhere lower to go, an unbounded or ambiguous shape is NOT downgraded).
- [x] **Real validation run** (`benchmark_runner.py quality-check --stage adaptive_budget --confirm-spend`, ~$0.006): `quality_adaptive_budget_001` -- the deliberate risk case, a short-looking prompt (an edge-case bugfix) that genuinely needs real reasoning depth, now downgraded from `high` to `medium` since its `bounded_shape` is True -- both the WITH and WITHOUT-adaptive_budget real responses correctly diagnosed the `<2 unique values` edge case AND gave a working fix. The harness's initial "failed" reports on both sides were the SAME recurring exact-substring grading fragility this project has hit repeatedly before (real response said "<2 elements (IndexError)" and "fewer than 2 elements" -- correct, just phrased differently than the rubric anticipated), not a real quality regression from the downgrade. Verified offline against the exact captured response text after broadening `required_elements` (added `"<2 elements"`/`"< 2 elements"` to the first OR-group, converted the mandatory `"IndexError"` into an OR-group with `"crash"`/`"crashes"`) -- both now correctly pass.
- [x] `quality_adaptive_budget_002` passed cleanly both ways (1/1), unaffected.
- [x] 355 tests total, ruff/mypy clean.

**Broadened to n=3 (2026-09-06), per the user asking to expand testing.** Two new real risk-case samples added: `quality_adaptive_budget_003` (a different `code` bug shape than _001 -- a linked-list reversal whose failure is silent/logical, not a loud exception, `high`->`medium`) and `quality_adaptive_budget_004` (extending to `conversational`, `medium`->`low` -- the classic bat-and-ball cognitive-reflection-test question, chosen specifically because it's famous for tricking reasoners into the fast wrong answer of $0.10 instead of the correct $0.05). Both offline-verified (stub client, free) to classify correctly and receive the expected downgraded effort level before any real spend. **Real run: 4/4 passed with the downgrade active, 4/4 passed with adaptive_budget disabled entirely** -- clean across both `code` bug shapes and the `conversational` puzzle. 355 tests total, ruff/mypy clean.

**Still-honest remaining scope**: this validates QUALITY isn't hurt by the downgrade at n=3. It does NOT yet validate the ECONOMICS of going further (see below).

### Real dollar-cost evidence (2026-09-06)

Per the user asking to expand testing to economics, not just quality: real `dev_demo.py --measure-usage` runs, both cleanly isolated (`--disable brevity_injector`, so ONLY the effort downgrade is measured, not blended with AGGRESSIVE brevity's own token effect):

| Scenario | Transition | Input Δ | Output saved | Net $/call (Sonnet 5: $2/$10 per MTok) |
|---|---|---|---|---|
| `thinking_effort_code` | `high`→`medium` | 0 | 138 tokens | +$0.00138 |
| `thinking_effort_conversational` | `medium`→`low` | 0 | 193 tokens | +$0.00193 |

Both clean wins -- zero cost on any axis (input or thinking), real output-token savings on both. Notably, `thinking_effort_conversational`'s baseline (untouched, no thinking config at all) showed 41 real spontaneous thinking tokens on its own, while the downgraded `"low"`-effort pipeline run showed 0 -- the downgrade didn't just reduce cost relative to a hypothetical higher-effort run, it used LESS reasoning than the model's own unprompted baseline behavior for this question.

**Where this leaves the "should this become a default" question**: real economics are positive at n=2 clean dollar samples, on top of quality holding at n=3 real quality-check samples -- genuinely promising, but still thin evidence relative to how thoroughly AGGRESSIVE brevity was validated (many real samples per category, both quality AND output-token measurement) before it earned its narrow default. Recommendation if this gets picked up again: broaden both n's further (a `tool-heavy`/`extraction` real economics check, even though their effort stays at `"low"` either way since bounded_shape doesn't change anything there -- worth confirming no regression rather than assuming; and 1-2 more `code`/`conversational` real dollar samples) before considering `enable_thinking_effort` + the bounded-shape downgrade a default for bounded requests specifically. Not decided either way in this session -- left as an open, evidence-backed option for later.

### Broadened further, then defaulted (2026-09-06, same day)

Per the user asking to actually do the broadening: 3 new quality-check samples added (`_005` tool-heavy, `_006` extraction -- specifically confirming no HIDDEN downside where bounded_shape doesn't change the already-`"low"` effort; `_007` a second conversational risk case, the "5 machines/5 minutes" cognitive-reflection trap) and 2 new `dev_demo.py` scenarios (`thinking_effort_tool_heavy`, `thinking_effort_extraction`) for real dollar evidence on the remaining two task types. All offline-verified (classification + expected effort level, stub client, free) before any real spend.

**Real results**: quality 7/7 clean with the stage enabled (the one "failure" was `_002` WITHOUT adaptive_budget -- its own pre-existing, documented max_tokens-truncation demo, unrelated to today's work). Real dollar evidence now spans all 4 task types, `adaptive_budget`'s own contribution positive in every one:

| Task type | Transition | adaptive_budget's own contribution | Net |
|---|---|---|---|
| `code` | high->medium | 138 output tokens saved, 0 input cost | +$0.00138 |
| `conversational` | medium->low | 193 output tokens saved, 0 input cost | +$0.00193 |
| `tool-heavy` | low->low | 74 output tokens saved, 0 input cost | +$0.00074 |
| `extraction` | low->low | 19 output tokens saved, 0 input cost | +$0.00019 |

`extraction`'s combined-scenario total looked negative (490->619, -26.3%), but the stage trace attributes that entirely to `structured_output` forcing `tool_choice` (+129 tokens, the same real cost this project already found and fixed the tokenizer's undercounting of earlier in this session) -- a separate, pre-existing mechanism, not a new problem from today's work. Also notable: on both `tool-heavy` and `conversational`, the untouched baseline's own SPONTANEOUS thinking (61 and 41 tokens respectively, no thinking config sent at all) exceeded what the downgraded `"low"`-effort pipeline used (0 both times) -- the downgrade uses less reasoning than the model's own unprompted baseline, not just less than a hypothetical higher-effort run.

**Decision: defaulted.** Evidence now matches the breadth that justified AGGRESSIVE brevity's own default (all 4 task types, both quality and real dollar evidence, no downside found on any axis). `stages/adaptive_budget.py`'s `_apply_thinking_effort` now engages thinking-effort (always at the downgraded level) whenever `meta.bounded_shape=True`, with NO `enable_thinking_effort` opt-in required for that specific case. Unbounded or ambiguous-shape requests are completely unaffected -- `enable_thinking_effort=True` remains required there, exactly as before, since that's precisely the content mix (bounded and unbounded together, at full undowngraded effort) the original 2-3x regression was measured against. `self.note()` now tags `thinking_effort_source` (`"bounded_shape_default"` vs `"opt_in"`) so the two paths stay distinguishable in the cost log.

- [x] New test coverage: `test_adaptive_budget.py` (+5 -- default fires without opt-in, still respects a caller's own `thinking` config, still fails open on an unsupported model, still leaves an unclassified request untouched, `thinking_effort_source` correctly distinguishes the two paths). `dev_demo.py`'s `code` scenario description corrected (its own prompt is bounded, so thinking effort now fires there without opting in -- the old description claiming it "stays untouched by default" was stale the moment the default landed). 360 tests total, ruff/mypy clean.

## Min/median/max cards for Output and Thinking tokens (2026-09-06)

The user asked for the same min/median/max range reporting the Request tokens section already has, extended to Output and Thinking tokens -- per CLAUDE.md's "report savings as ranges, never a single flat percentage" rule.

- [x] **`dashboard/aggregate.py`**: `GenerationUsageStats` gained `per_sample_output_pct_saved`/`per_sample_thinking_tokens_delta` lists (populated per real sample in `aggregate_events()`) and `min_output_pct_saved`/`median_output_pct_saved`/`max_output_pct_saved` (float % properties, same shape as the existing request-side ones) plus `min_thinking_tokens_delta`/`median_thinking_tokens_delta`/`max_thinking_tokens_delta`. Thinking uses a RAW TOKEN DELTA, not a %, deliberately -- `baseline_thinking_tokens` is 0 on most samples (no thinking config sent at all), so a %-saved figure would be undefined (divide by zero) far too often to summarize meaningfully.
- [x] **`dashboard/render.py`**: new cards in both `_render_output_tokens_section()` (Min/Median/Max sample saved %) and `_render_thinking_tokens_section()` (Min/Median/Max sample delta in tokens), shown only once at least one real sample exists for that metric -- same no-fabricated-data discipline as every other card.
- [x] New test coverage: `test_dashboard.py` (+6 -- aggregate range math for both metrics including the "no phantom zero from a sample with no thinking data" edge case, render cards appear/disappear correctly with real values shown). 365 tests total, ruff/mypy clean.

## MCP server: opt-in dashboard auto-open (2026-09-06)

The user asked for the MCP server to open the dashboard when run. Added as a SECOND opt-in on top of the existing `TOKENETICS_MCP_LOG_FILE` persistence flag, following the exact same "stdio server, no surprising side effects by default" reasoning that flag was already built around -- opening a browser and binding a port is a bigger side effect than writing a file, so it gets its own explicit opt-in rather than riding along automatically whenever persistence is on.

- [x] **`mcp_server.py`**: new `TOKENETICS_MCP_OPEN_DASHBOARD` env var (any truthy value). When set (and `TOKENETICS_MCP_LOG_FILE` is also set -- no log file means nothing to show), `main()` starts the dashboard server via `tokenetics.dashboard.server.run_server` (imported directly, not shelled out to `scripts/dashboard.py`) in a background daemon thread, then opens it in the default browser via `webbrowser.open`. Port defaults to 8765 (matching `dashboard.py`'s own default), overridable via `TOKENETICS_MCP_DASHBOARD_PORT`.
- [x] **Respects the module's hard stdio constraint**: nothing here writes to stdout (which would corrupt the JSON-RPC stream) -- failures (port already bound, no browser/display in a headless environment) are logged via the stdlib `logging` module, which defaults to stderr, and never block or crash the MCP server itself.
- [x] **Verified for real, not just offline logic**: no-env-vars case is a true no-op; log-file-missing case logs and returns without crashing; the full real path (env vars set, `run_server` actually binding a real port, a background thread actually serving, `webbrowser.open` actually called with the right URL) was run end-to-end with `webbrowser.open` monkeypatched to capture the URL instead of launching a real browser in this headless environment, and a real `urllib.request` GET against the bound port confirmed the dashboard responds (`200`).
- [x] `ARCHITECTURE.md` updated to document the new env var alongside the existing persistence one. No new pytest coverage added -- matches this file's existing convention (it had zero test coverage before this change either; validated via the real MCP client smoketest script and, for this change, the manual end-to-end check above). 365 tests total (unchanged), ruff/mypy clean.

## Revisiting this roadmap

- **2026-07-26** — First re-forecast, based on phases 0–3 finishing in 7 actual days against a 4.5-week estimate (~4.5x faster than the original human-solo-dev pacing model). Applied 4.5x to phases 4–8 (same kind of work as 0–3) and a more conservative 2x to phases 9–13 (partly bound by real API costs, new dependencies, and external publish steps — see "Re-forecast" under Assumptions above). Tier 0 freeze moved from ~Oct 10 to ~Aug 6; v1 shippable moved from ~Dec 3 to ~Aug 31.
- Phases 9–13 remain the least confident estimates here since they're furthest out and least like the work actually measured so far. Once Tier 0 is frozen (now ~Aug 6), re-forecast the rest of the roadmap again using actual velocity from phases 0–8 rather than trusting this second guess either.
